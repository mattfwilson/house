// Public surface of @house/core.
// Re-exports are added by later plans (Money, CalendarDate, AssumptionSet,
// EngineInput, the configured Decimal clone, and the reproducibility harness).
// Wave 0 (Plan 01-01) only establishes the package boundary + build enforcement.
export {};
