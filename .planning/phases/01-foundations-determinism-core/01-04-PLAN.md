---
phase: 01-foundations-determinism-core
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/core/src/serialize/canonical-json.ts
  - packages/core/src/serialize/canonical-json.test.ts
  - packages/core/src/engine/canary.ts
  - packages/core/src/engine/canary.test.ts
  - packages/core/src/__fixtures__/golden-snapshot.json
  - packages/core/src/golden.test.ts
  - packages/core/src/index.ts
  - packages/core/src/index.test.ts
  - eslint.config.ts
autonomous: false
requirements: [PROF-04]

must_haves:
  truths:
    - "A deterministic canary computation does real Decimal compounding + banker's rounding + an AssumptionSet read against a frozen EngineInput"
    - "The canary result serializes to canonical JSON: Money values as decimal strings, object keys sorted — deterministically"
    - "A frozen golden-master JSON fixture is committed; recomputing the canary deep-equals it (cent-identical)"
    - "The golden master is regenerated ONLY via the gated UPDATE_GOLDEN=1 command (not Vitest toMatchSnapshot); mutating one cent makes the test fail"
    - "A snapshot serialize -> deserialize round-trip of the EngineInput is lossless and re-runs to the same result"
    - "The package public surface (index.ts) re-exports Money, CalendarDate, AssumptionSet, EngineInput, defaults, the canary, and canonicalJson"
  artifacts:
    - path: "packages/core/src/serialize/canonical-json.ts"
      provides: "canonicalJson(value): sorted keys + Money->decimal string"
      contains: "canonicalJson"
    - path: "packages/core/src/engine/canary.ts"
      provides: "runCanary(input: EngineInput) — deterministic compounding + AssumptionSet read"
      contains: "runCanary"
    - path: "packages/core/src/__fixtures__/golden-snapshot.json"
      provides: "Committed frozen golden master"
      contains: "{"
    - path: "packages/core/src/golden.test.ts"
      provides: "Gated golden-master test (UPDATE_GOLDEN=1) with deep-equal on canonical JSON"
      contains: "UPDATE_GOLDEN"
    - path: "packages/core/src/index.ts"
      provides: "Public surface re-exporting all phase primitives"
      contains: "export"
  key_links:
    - from: "packages/core/src/golden.test.ts"
      to: "packages/core/src/__fixtures__/golden-snapshot.json"
      via: "deep-equal on canonical JSON; regen gated behind UPDATE_GOLDEN=1"
      pattern: "golden-snapshot|UPDATE_GOLDEN"
    - from: "packages/core/src/engine/canary.ts"
      to: "packages/core/src/assumptions/assumption-set.ts"
      via: "reads an assumption slice (e.g. returns.realAnnual) for the compounding"
      pattern: "assumptions|returns|swr"
    - from: "packages/core/src/serialize/canonical-json.ts"
      to: "packages/core/src/money/money.ts"
      via: "serializes Money via toDecimalString"
      pattern: "Money|toDecimalString"
---

<objective>
Close the reproducibility loop (PROF-04): a deterministic "canary" computation (real `Decimal` compounding + banker's rounding + an `AssumptionSet` read) over a frozen `EngineInput`, serialized through a canonical JSON encoder (Money→decimal strings, sorted keys), deep-equal-compared against a committed golden-master fixture that is regenerated ONLY behind `UPDATE_GOLDEN=1` — explicitly NOT Vitest `toMatchSnapshot` (which auto-writes / re-blesses silently). Plus a snapshot serialize→deserialize→recompute round-trip proving data reproducibility, and the public `index.ts` barrel assembling the phase's primitives into a stable import surface for every downstream phase.

Purpose: This harness is the substrate the future FI-oracle test (FI-05) will trust. It must prove the machinery — real compounding + rounding + assumptions read — not a trivial echo, so determinism is provable before any persistence exists.
Output: `serialize/`, `engine/canary.ts`, the committed golden fixture, the gated golden test, and `packages/core/src/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundations-determinism-core/01-CONTEXT.md
@.planning/phases/01-foundations-determinism-core/01-RESEARCH.md
@packages/core/src/money/money.ts
@packages/core/src/engine/engine-input.ts
@packages/core/src/assumptions/assumption-set.ts
</context>

<artifacts_this_phase_produces>
- **`canonicalJson(value): string`** — deterministic serializer: `Money` → `toDecimalString()`, object keys sorted; the canonical form for golden compare (D-10).
- **`runCanary(input: EngineInput)`** — deterministic computation doing real `Dec` compounding (e.g. `(1 + returns.realAnnual)^n`) + `Money` arithmetic + banker's rounding at the boundary + an `AssumptionSet` slice read (D-08). Representative of FI compounding, not a trivial echo.
- **`golden-snapshot.json`** — committed frozen golden master in `src/__fixtures__/` (D-09).
- **`golden.test.ts`** — gated harness: `UPDATE_GOLDEN=1` regenerates (reviewable diff), otherwise deep-equal on canonical JSON; PLUS a snapshot serialize→deserialize→recompute round-trip assertion (D-08/D-09/D-10).
- **`packages/core/src/index.ts`** — the package public surface re-exporting `Money`, `CalendarDate`/`calendarDate`, `AssumptionSet` types + `DEFAULT_ASSUMPTIONS` + `migrate`, `EngineInput`/`engineInput`, `runCanary`, `canonicalJson`. (`Dec` may stay internal; do NOT export raw `Decimal`.)
- **ESLint override** (in `eslint.config.ts`): a narrowly-scoped exception allowing `golden.test.ts` to read `process.env.UPDATE_GOLDEN` only (test harness, not core runtime).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canonicalJson serializer + deterministic canary + public index barrel</name>
  <files>packages/core/src/serialize/canonical-json.ts, packages/core/src/serialize/canonical-json.test.ts, packages/core/src/engine/canary.ts, packages/core/src/engine/canary.test.ts, packages/core/src/index.ts, packages/core/src/index.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-determinism-core/01-RESEARCH.md ("Pattern 5: Golden-master harness", "System Architecture Diagram" — primary canary trace)
    - .planning/phases/01-foundations-determinism-core/01-CONTEXT.md (D-08, D-10; "Specific Ideas" — canary must be representative of FI compounding, not a trivial echo)
    - packages/core/src/money/money.ts, packages/core/src/engine/engine-input.ts, packages/core/src/assumptions/assumption-set.ts (read exported signatures from Plans 02/03)
  </read_first>
  <behavior>
    - canonicalJson serializes a Money to its decimal string (toDecimalString), not a number; sorts object keys recursively; identical inputs always produce byte-identical output.
    - canonicalJson never emits a JS number for a Money value (no float leak) and is order-independent across differently-key-ordered equivalent objects.
    - runCanary(frozenInput) is pure and deterministic: same EngineInput in -> same result out across repeated calls; it reads at least one AssumptionSet slice (e.g. returns.realAnnual) and does multi-period Decimal compounding rounded at the boundary.
    - index.ts re-exports Money, CalendarDate/calendarDate, AssumptionSet types + DEFAULT_ASSUMPTIONS + migrate, EngineInput/engineInput, runCanary, canonicalJson; it does NOT export raw Decimal or Dec's raw API.
  </behavior>
  <action>
    Create `serialize/canonical-json.ts` exporting `canonicalJson(value: unknown): string` — a `JSON.stringify` replacer that maps `Money` instances to `toDecimalString()` and returns objects with sorted keys (recursively; per Pattern 5). Create `engine/canary.ts` exporting `runCanary(input: EngineInput)` that reads an assumption slice (e.g. `input.assumptions.returns.realAnnual`), parses it via `Dec`, does a representative multi-period compounding (e.g. grow a starting `Money` by `(1+r)` over N periods), and returns a plain result object containing `Money` values (rounded at the boundary only). Keep it deterministic — asOf comes from `input.asOf`, never `Date.now`. Create `packages/core/src/index.ts` re-exporting the public surface listed in artifacts (NO raw `Decimal`/`Dec`). Write `canonical-json.test.ts` (Money→string, sorted keys, order-independence, no float leak), `canary.test.ts` (determinism: two runs equal; reads an assumption slice; output contains Money), and `index.test.ts` (the expected symbols are exported; `Decimal`/`Dec` are NOT). TDD: failing tests first.
  </action>
  <verify>
    <automated>npm run test -w packages/core -- src/serialize src/engine/canary src/index</automated>
  </verify>
  <acceptance_criteria>
    - `canonical-json.ts` exports `canonicalJson`; serializing a Money yields its decimal string (asserted), never a number.
    - `canary.ts` exports `runCanary`; two calls on the same frozen EngineInput produce deep-equal results; the canary reads `assumptions.returns` (or another slice) — asserted in `canary.test.ts`.
    - `index.ts` re-exports Money, calendarDate, DEFAULT_ASSUMPTIONS, engineInput, runCanary, canonicalJson; `index.test.ts` asserts `Decimal`/`Dec` are NOT exported.
    - `npm run test -w packages/core -- src/serialize src/engine/canary src/index` exits 0.
  </acceptance_criteria>
  <done>The canary does real deterministic compounding reading an AssumptionSet slice; canonicalJson produces stable Money-as-string sorted-key output; the public index barrel exposes the phase primitives without leaking raw Decimal.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + commit the golden master; gated reproducibility test + ESLint exception</name>
  <files>packages/core/src/golden.test.ts, packages/core/src/__fixtures__/golden-snapshot.json, eslint.config.ts</files>
  <read_first>
    - .planning/phases/01-foundations-determinism-core/01-RESEARCH.md ("Pattern 5: Golden-master harness", "Root package.json" — update-golden script + Windows/cross-env note, Anti-Patterns — toMatchSnapshot)
    - .planning/phases/01-foundations-determinism-core/01-CONTEXT.md (D-08, D-09, D-10; PROF-04)
    - packages/core/src/engine/canary.ts, packages/core/src/serialize/canonical-json.ts (from Task 1)
    - eslint.config.ts (the core override block from Plan 01 — to add the golden.test.ts exception)
  </read_first>
  <action>
    Create `golden.test.ts`: build a FIXED `EngineInput` (frozen `asOf` via `calendarDate('2026-01-01')` + `DEFAULT_ASSUMPTIONS`), call `runCanary`, serialize with `canonicalJson`. When `process.env.UPDATE_GOLDEN === '1'`, write the produced canonical JSON to `src/__fixtures__/golden-snapshot.json` and return; otherwise read the committed fixture and `expect(produced).toBe(golden)` (deep-equal on canonical JSON, D-10). ALSO assert a snapshot round-trip: serialize the EngineInput to canonical JSON, parse it back through `parseAssumptionSet`/`calendarDate`, re-run `runCanary`, and assert the recomputed canonical result equals the first (D-08 data reproducibility). Do NOT use `toMatchSnapshot` (auto-writes / re-blessable, violates D-09). Add an ESLint override in `eslint.config.ts` scoped to `packages/core/**/golden.test.ts` that relaxes ONLY the `no-restricted-globals`/`no-restricted-properties` `process.env` ban for the `UPDATE_GOLDEN` read (document inline why: test harness, not core runtime). Generate the fixture once via `npm run update-golden` (cross-env-backed, PowerShell-portable), then commit `golden-snapshot.json`. Re-run the suite WITHOUT the flag to confirm green.
  </action>
  <verify>
    <automated>npm run update-golden &amp;&amp; npm run test -w packages/core -- src/golden</automated>
  </verify>
  <acceptance_criteria>
    - `golden.test.ts` reads/writes `src/__fixtures__/golden-snapshot.json` gated on `process.env.UPDATE_GOLDEN === '1'`; uses `expect(...).toBe(...)` deep-equal (NOT `toMatchSnapshot`).
    - The committed `golden-snapshot.json` exists; `npm run test -w packages/core -- src/golden` exits 0 against it.
    - Manually altering one digit in `golden-snapshot.json` makes `npm run test -w packages/core -- src/golden` FAIL (tamper-detection — note this manual check in the SUMMARY).
    - `eslint.config.ts` contains a `golden.test.ts`-scoped override permitting only the `UPDATE_GOLDEN` env read; `npm run lint` stays green.
  </acceptance_criteria>
  <done>The frozen golden master is committed and the gated reproducibility test passes cent-identically; regeneration requires the explicit UPDATE_GOLDEN=1 command; mutating a cent fails the test (PROF-04 proven before persistence exists).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed golden fixture → test comparison | The fixture is trusted-on-commit; silent drift is the threat the gated regen + reviewable diff defends against. |
| serialized EngineInput snapshot → deserialized recompute | Round-trip validates reproducibility before persistence exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Silent re-blessing of the golden master | mitigate | Regen gated behind `UPDATE_GOLDEN=1` producing a reviewable git diff; NOT `toMatchSnapshot` (auto-writes / `-u` re-bless). Tamper-detection check: mutating a cent fails the test. |
| T-04-02 | Repudiation (of correctness) | Non-deterministic / non-canonical serialization causing false pass/fail | mitigate | `canonicalJson` sorts keys + Money→decimal string; canary reads asOf from EngineInput, never Date.now; round-trip assertion. |
| T-04-03 | Tampering (of precision) | Float leaking into the serialized golden master | mitigate | Money serialized via `toDecimalString` (string), assumption values are decimal strings end to end; no `z.number()` anywhere. |
| T-04-04 | Tampering | ESLint env-read exception widening beyond UPDATE_GOLDEN | mitigate | Override scoped to `golden.test.ts` only and to the single `UPDATE_GOLDEN` read; core runtime env ban (Plan 01) unchanged. |
</threat_model>

<verification>
- `npm run test -w packages/core -- src/serialize src/engine/canary src/index` passes (canary deterministic; canonicalJson stable; index exports correct, no raw Decimal).
- `npm run update-golden` regenerates the fixture; `npm run test -w packages/core -- src/golden` passes against the committed fixture.
- Mutating one digit in `golden-snapshot.json` makes the golden test fail (cent-identical enforcement).
- `npm run lint` green (golden.test.ts env-read exception scoped; no other env reads).
- Phase gate: `npm test` + `npm run lint` + `tsc -b` all green; core coverage ≥ thresholds.
</verification>

<success_criteria>
- PROF-04: a frozen snapshot recomputes cent-identically vs the committed golden master; the round-trip serialize→deserialize→recompute is lossless; regeneration is explicit and reviewable — proving determinism before persistence exists.
- The public `index.ts` surface exposes the phase's primitives as a stable import boundary for all downstream phases without leaking raw `Decimal`.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-determinism-core/01-04-SUMMARY.md` when done
</output>
