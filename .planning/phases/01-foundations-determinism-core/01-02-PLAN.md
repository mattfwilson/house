---
phase: 01-foundations-determinism-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/core/src/money/decimal-config.ts
  - packages/core/src/money/money.ts
  - packages/core/src/money/money.test.ts
  - packages/core/src/time/calendar-date.ts
  - packages/core/src/time/calendar-date.test.ts
  - packages/core/src/determinism/guard.ts
  - packages/core/src/determinism/guard.setup.ts
  - packages/core/src/determinism/guard.test.ts
autonomous: true
requirements: [CORE-02, CORE-03]
user_setup: []

must_haves:
  truths:
    - "Money arithmetic retains full decimal precision through intermediate math and rounds to cents ONLY at the output boundary (toCents)"
    - "Money rounding uses banker's rounding (HALF_EVEN): summing 1000 individually-roundable values via Money equals the full-precision sum rounded once"
    - "Bare-number dollar math through the Money public API is a TypeScript error (verified by @ts-expect-error tests)"
    - "A CalendarDate is a branded YYYY-MM-DD string that rejects invalid dates and is never converted to a JS Date inside the core"
    - "The determinism runtime guard makes Date.now and Math.random throw when reached during a test"
  artifacts:
    - path: "packages/core/src/money/decimal-config.ts"
      provides: "The single frozen Decimal.clone({ precision: 34, rounding: ROUND_HALF_EVEN }) constructor `Dec`"
      contains: "ROUND_HALF_EVEN"
    - path: "packages/core/src/money/money.ts"
      provides: "Immutable Money class with closed API (of/zero/add/sub/mul/percentOf/toCents/toDecimalString/toString)"
      contains: "class Money"
    - path: "packages/core/src/time/calendar-date.ts"
      provides: "Branded CalendarDate type + calendarDate() validator"
      contains: "CalendarDate"
    - path: "packages/core/src/determinism/guard.ts"
      provides: "installDeterminismGuard() that makes Date.now/Math.random throw"
      contains: "installDeterminismGuard"
    - path: "packages/core/src/determinism/guard.setup.ts"
      provides: "Vitest setupFile wiring the guard into the core test project"
      contains: "installDeterminismGuard"
  key_links:
    - from: "packages/core/src/money/money.ts"
      to: "packages/core/src/money/decimal-config.ts"
      via: "Money wraps a Dec instance from the single configured clone (never the global Decimal)"
      pattern: "import.*decimal-config|Dec"
    - from: "packages/core/vitest.config.ts"
      to: "packages/core/src/determinism/guard.setup.ts"
      via: "setupFiles entry installs the runtime determinism guard"
      pattern: "guard.setup"
---

<objective>
Implement the decimal-precise money primitive and determinism enforcement at runtime — the heart of CORE-02 and the runtime half of CORE-03. Build the single frozen `Decimal.clone` constructor (banker's rounding, precision 34), an immutable `Money` class with a closed API that makes bare-`number` dollar math impossible, a branded `CalendarDate` so the core never touches a JS `Date`, and the runtime determinism guard wired into the Vitest setup. TDD throughout: the tests ARE the product (a Money that can be bypassed is worthless).

Purpose: Lock the existential money/time/determinism mistakes out of every downstream phase at the type and runtime level, complementing the lint guards from Plan 01.
Output: `money/`, `time/`, `determinism/` modules with full unit tests, and the Vitest setupFile installing the guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundations-determinism-core/01-CONTEXT.md
@.planning/phases/01-foundations-determinism-core/01-RESEARCH.md
@packages/core/tsconfig.json
@packages/core/vitest.config.ts
</context>

<artifacts_this_phase_produces>
- **`Dec`** — the single frozen `Decimal.clone({ precision: 34, rounding: Decimal.ROUND_HALF_EVEN /* ===6 */ })` constructor; `DecimalInstance` type alias. Global `Decimal` is NEVER `.set()` (D-14).
- **`Money`** class (immutable, branded) — entry points `Money.of(decimalString: string)`, `Money.zero()`; ops `add(Money)`, `sub(Money)`, `mul(rateString)`, `percentOf(rateString)`; output boundary `toCents(): bigint`, `toDecimalString(): string` (full precision, for canonical JSON), `toString(): string` (2dp display). No `number`-accepting dollar constructor, no `valueOf`/`toJSON` returning a number (D-01/D-03).
- **`CalendarDate`** — branded `YYYY-MM-DD` string type + `calendarDate(s: string): CalendarDate` validator (D-13). No JS `Date` anywhere.
- **`installDeterminismGuard()`** + `guard.setup.ts` Vitest setupFile (D-12 runtime half).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frozen Decimal clone + immutable Money class</name>
  <files>packages/core/src/money/decimal-config.ts, packages/core/src/money/money.ts, packages/core/src/money/money.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-determinism-core/01-RESEARCH.md ("Pattern 1: Frozen Decimal clone", "Pattern 2: Immutable Money class", "Pitfall 3", "Pitfall 4")
    - CLAUDE.md ("What NOT to Use" — bare number for money, dinero in core; decimal.js rationale)
    - .planning/phases/01-foundations-determinism-core/01-CONTEXT.md (D-01, D-02, D-03, D-14)
  </read_first>
  <behavior>
    - Dec is a Decimal.clone with precision 34 and rounding ROUND_HALF_EVEN (=6); the global Decimal default rounding (HALF_UP=4) is NOT used.
    - Money.of('0.035') and Money.zero() construct; there is no public constructor and no number-accepting dollar entry point.
    - add/sub/mul/percentOf return new Money (immutable) and retain full precision (no per-op rounding).
    - toCents() rounds to cents using HALF_EVEN at the boundary: Money.of('2.005').toCents() === 200n and Money.of('2.015').toCents() === 202n (banker's rounding to even).
    - Summing 1000 values of '0.005' (or a mix needing rounding) via repeated add then toCents equals the full-precision sum rounded once — proves "round at boundary only" (D-03) and HALF_EVEN avoids HALF_UP drift (D-04 pitfall).
    - @ts-expect-error: Money.add(5 as never)-style misuse and `money * 1.05` arithmetic are type errors; Money has no valueOf/toJSON returning a number.
    - toDecimalString() preserves full precision; toString() yields a 2dp display string.
  </behavior>
  <action>
    Create `decimal-config.ts` exporting `Dec = Decimal.clone({ precision: 34, rounding: Decimal.ROUND_HALF_EVEN })` and `type DecimalInstance = InstanceType<typeof Dec>`. Never call `Decimal.set`. Create `money.ts`: an immutable `Money` class wrapping a `Dec` instance, with a `unique symbol` brand and private constructor; static `of(decimalString: string)` and `zero()`; instance `add`/`sub` taking `Money`, `mul`/`percentOf` taking a dimensionless rate STRING (never a Money, never a number); `toCents(): bigint` as the ONLY rounding boundary (via `.times(100).toDecimalPlaces(0).toFixed(0)` → BigInt), `toDecimalString()` full precision, `toString()` 2dp. Do NOT add `valueOf`/`toJSON` returning numbers (keeps the bare-number hole closed). Write `money.test.ts` covering every behavior above including the `@ts-expect-error` cases and the sum-of-1000-rounded == full-precision-rounded-once invariant. Follow RED→GREEN→REFACTOR; commit a failing test first.
  </action>
  <verify>
    <automated>npm run test -w packages/core -- src/money</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/money/decimal-config.ts` exports `Dec` configured with `ROUND_HALF_EVEN` and `precision: 34`; no `Decimal.set` call exists anywhere in the file.
    - `packages/core/src/money/money.ts` exports `class Money` with no number-accepting dollar constructor and no `valueOf`/`toJSON`.
    - `npm run test -w packages/core -- src/money` exits 0; the sum-of-1000 invariant test and the `@ts-expect-error` bare-number cases pass.
    - Changing the clone rounding to HALF_UP makes the `2.005`→`200n` / banker's-rounding test fail (demonstrable).
  </acceptance_criteria>
  <done>Money is immutable, decimal-precise, rounds cents only at toCents using banker's rounding, and rejects bare-number dollar math at the type level — all proven by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branded CalendarDate + determinism runtime guard (+ Vitest setup wiring)</name>
  <files>packages/core/src/time/calendar-date.ts, packages/core/src/time/calendar-date.test.ts, packages/core/src/determinism/guard.ts, packages/core/src/determinism/guard.setup.ts, packages/core/src/determinism/guard.test.ts, packages/core/vitest.config.ts</files>
  <read_first>
    - .planning/phases/01-foundations-determinism-core/01-RESEARCH.md ("Pattern 3: Branded CalendarDate", "Pattern 6: Determinism — lint + runtime guard", "Root vitest config" — setupFiles)
    - .planning/phases/01-foundations-determinism-core/01-CONTEXT.md (D-11, D-12, D-13)
    - packages/core/vitest.config.ts (the TODO left by Plan 01 to add the determinism setupFile)
  </read_first>
  <behavior>
    - calendarDate('2026-06-23') returns a branded CalendarDate (assignable where CalendarDate is required); a plain string is NOT assignable to CalendarDate without going through calendarDate().
    - calendarDate('2026-13-01'), calendarDate('2026-6-3'), calendarDate('not-a-date') each throw (regex enforces YYYY-MM-DD with valid month/day ranges).
    - No JS Date is constructed anywhere in calendar-date.ts (pure string ops).
    - After installDeterminismGuard(), calling Date.now() throws an Error mentioning Date.now, and Math.random() throws mentioning Math.random.
    - The guard is installed via the core Vitest setupFile so any core test that reaches for Date.now/Math.random fails loudly.
  </behavior>
  <action>
    Create `calendar-date.ts`: a `unique symbol`-branded `CalendarDate = string & { ... }` type and `calendarDate(s: string): CalendarDate` that validates against `/^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$/` and throws on mismatch — never touching `Date` (D-13). Create `guard.ts` exporting `installDeterminismGuard(): void` that overwrites `Date.now` and `Math.random` with functions that throw `Error('Nondeterminism in core: ...')` (D-12 runtime half; test-time safety net only — do not call this from shipped library code). Create `guard.setup.ts` that calls `installDeterminismGuard()` (this is the Vitest setupFile). Edit `packages/core/vitest.config.ts` to add `setupFiles: ['./src/determinism/guard.setup.ts']` to the `core` project (resolving the TODO from Plan 01). Write `calendar-date.test.ts` (valid parse, invalid throws, brand non-assignability via `@ts-expect-error`) and `guard.test.ts` (Date.now throws, Math.random throws after install). TDD: failing tests first.
  </action>
  <verify>
    <automated>npm run test -w packages/core -- src/time src/determinism</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/time/calendar-date.ts` exports `CalendarDate` + `calendarDate`; no `new Date`/`Date(` appears in the file.
    - `packages/core/src/determinism/guard.ts` exports `installDeterminismGuard`; after it runs, `Date.now()` and `Math.random()` throw (proven in `guard.test.ts`).
    - `packages/core/vitest.config.ts` lists `./src/determinism/guard.setup.ts` in `setupFiles`.
    - `npm run test -w packages/core -- src/time src/determinism` exits 0.
  </acceptance_criteria>
  <done>CalendarDate is a validated branded string with no JS Date usage; the runtime determinism guard throws on ambient time/randomness and is wired into the core test setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-supplied numeric literals → Money/CalendarDate constructors | Only validated decimal strings / ISO date strings cross; bare numbers and JS Dates are rejected by API shape. |
| core runtime → ambient globals (Date/Math) | The determinism guard severs this at test time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (of precision) | Float re-entering money math via bare number / valueOf | mitigate | Closed Money API (string-only entry), no valueOf/toJSON returning number, `@ts-expect-error` tests; single frozen Dec clone (no global Decimal.set). |
| T-02-02 | Repudiation (of correctness) | HALF_UP upward bias accumulating over a multi-decade projection | mitigate | Banker's rounding (ROUND_HALF_EVEN) in the clone + round only at toCents boundary; sum-of-1000 invariant test. |
| T-02-03 | Repudiation (of correctness) | Ambient nondeterminism (Date.now/Math.random) reached at runtime | mitigate | `installDeterminismGuard` makes them throw; wired into Vitest setupFile (complements Plan 01 lint rule). |
| T-02-04 | Tampering | JS Date timezone/locale nondeterminism via asOf | mitigate | CalendarDate is a pure branded string; lint bans `new Date`; no Date constructed in core. |
</threat_model>

<verification>
- `npm run test -w packages/core -- src/money` passes including banker's-rounding and sum-of-1000 invariants and `@ts-expect-error` bare-number cases.
- `npm run test -w packages/core -- src/time src/determinism` passes; guard makes Date.now/Math.random throw.
- `npm run lint` still green on this real source (no Date.now/Math.random/new Date used).
</verification>

<success_criteria>
- CORE-02: decimal-precise Money, documented banker's-rounding policy at the toCents boundary, bare-number dollar math rejected by tests.
- CORE-03 (runtime half): determinism guard throws on ambient time/randomness; CalendarDate keeps JS Date out of the core.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-determinism-core/01-02-SUMMARY.md` when done
</output>
