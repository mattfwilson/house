---
phase: 02-tco-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/assumptions/schema.ts
  - packages/core/src/assumptions/defaults.ts
  - packages/core/src/assumptions/migrate.ts
  - packages/core/src/assumptions/migrate.test.ts
  - packages/core/src/assumptions/schema.test.ts
  - packages/core/src/engine/engine-input.ts
  - packages/core/src/towns/town-table.schema.ts
  - packages/core/src/towns/town-table.ts
  - packages/core/src/towns/town-table.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [TCO-02]
must_haves:
  truths:
    - "An AssumptionsV2 exists carrying appreciation.realAnnual, transaction.sellCostPct, rent.realGrowthAnnual, closing.rateOfPrice, and tax.assessmentRatio, all as canonical decimal strings"
    - "migrate(V1) returns a V2 set with the new slices defaulted and schemaVersion 2 (the V1->V2 path is exercised, not identity)"
    - "ScenarioInputs carries the house-scenario fields (price, downPaymentPct, annualRate, termMonths, holdingYears, town, insuranceAnnual, hoaMonthly, monthlyRent, optional overrides)"
    - "resolveMillRate(town) returns { residentialMillRate, fy } from a seeded ~20-40 greater-Boston town table, throwing on an unknown town"
  artifacts:
    - path: "packages/core/src/towns/town-table.ts"
      provides: "Seeded FY-stamped MA mill-rate table + resolveMillRate resolver"
      min_lines: 40
    - path: "packages/core/src/towns/town-table.schema.ts"
      provides: "Zod row schema (decStr millRate, int fy), .strict()"
      contains: "decStr"
    - path: "packages/core/src/assumptions/schema.ts"
      provides: "AssumptionsV2 appended to the discriminatedUnion; CURRENT_VERSION = 2"
      contains: "AssumptionsV2"
  key_links:
    - from: "packages/core/src/assumptions/migrate.ts"
      to: "v1ToV2"
      via: "case 1 returns v1ToV2(set)"
      pattern: "v1ToV2"
    - from: "packages/core/src/index.ts"
      to: "town-table.ts"
      via: "barrel export of resolveMillRate + ScenarioInputs widening"
      pattern: "resolveMillRate"
---

<objective>
Lay the Wave 1 foundation every TCO calc module depends on: bump the assumptions schema to a real **AssumptionsV2** (new tunable slices) with a working **v1ToV2 migrate arm**, **widen `ScenarioInputs`** into the real house-scenario shape, and introduce the new **`towns/` module** — a seeded, FY-stamped greater-Boston mill-rate table with a `resolveMillRate` resolver behind a Zod row schema.

Purpose: The `tco/` calc modules (Plans 02-05) read these widened assumption slices and scenario fields and resolve the town mill rate; none can compile until this contract lands. This plan also satisfies the data half of TCO-02 (seeded MA mill-rate table). Exercising a real V1->V2 migration now proves the migration machinery before Phase 6 persistence depends on it (per RESEARCH Alternatives Considered A4).

Output: Extended `assumptions/{schema,defaults,migrate}.ts`, widened `engine/engine-input.ts`, new `towns/{town-table.schema,town-table,town-table.test}.ts`, updated public barrel `index.ts`, and updated `migrate.test.ts` / `schema.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tco-engine/02-CONTEXT.md
@.planning/phases/02-tco-engine/02-RESEARCH.md
@.planning/phases/02-tco-engine/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces (later plans add the `tco/` calc symbols):
- **Types:** `AssumptionsV2` (Zod schema + inferred type), widened `ScenarioInputs`, `TownRateRow` (Zod-inferred), `ResolvedMillRate` (`{ residentialMillRate: string; fy: number }`).
- **Functions:** `v1ToV2(set)`, `resolveMillRate(town: string): ResolvedMillRate`, `townTableSchema` (Zod), `DEFAULT_ASSUMPTIONS_V2`.
- **Modified symbols:** `AssumptionSetSchema` (union now `[AssumptionsV1, AssumptionsV2]`), `CURRENT_VERSION = 2`, `CurrentAssumptionSet = z.infer<typeof AssumptionsV2>`, `DEFAULT_ASSUMPTIONS` (now V2-shaped), `migrate` (`case 1` now returns `v1ToV2`).
- **New files:** `packages/core/src/towns/town-table.schema.ts`, `town-table.ts`, `town-table.test.ts`.
- **New public barrel exports:** `AssumptionsV2`, widened `ScenarioInputs`, `resolveMillRate`, `type ResolvedMillRate`, `type TownRateRow`. (Raw `Dec`/`Decimal` remain unexported.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add AssumptionsV2 schema, V2 defaults, and the v1ToV2 migrate arm</name>
  <files>packages/core/src/assumptions/schema.ts, packages/core/src/assumptions/defaults.ts, packages/core/src/assumptions/migrate.ts, packages/core/src/assumptions/migrate.test.ts, packages/core/src/assumptions/schema.test.ts</files>
  <read_first>
    - packages/core/src/assumptions/schema.ts (the AssumptionsV1 object, the `group`/`decStr`/`.strict()` idiom, the discriminatedUnion at line 83, CURRENT_VERSION, CurrentAssumptionSet)
    - packages/core/src/assumptions/defaults.ts (DEFAULT_ASSUMPTIONS shape — must stay typed `: CurrentAssumptionSet`)
    - packages/core/src/assumptions/migrate.ts (the version-gated switch + assertNever exhaustiveness guard)
    - packages/core/src/assumptions/migrate.test.ts and schema.test.ts (existing assertions to extend)
    - packages/core/src/assumptions/assumption-set.ts (parseAssumptionSet / CurrentAssumptionSet re-export — do not break it)
    - .planning/phases/02-tco-engine/02-PATTERNS.md §"assumptions/schema.ts (MODIFY)" and §"defaults.ts (MODIFY)" and §"migrate.ts (MODIFY)"
  </read_first>
  <action>
    In `schema.ts`: define `AssumptionsV2` by copying the `AssumptionsV1` object, set `schemaVersion: z.literal(2)`, KEEP all V1 slices, and add new slices using the existing `group({...})` helper — every leaf a `decStr` (NEVER `z.number()`, per T-03-02): `appreciation: group({ realAnnual: decStr })` (D-04), `transaction: group({ sellCostPct: decStr })` (D-05), `rent: group({ realGrowthAnnual: decStr })` (D-06), `closing: group({ rateOfPrice: decStr })` (D-12); and ADD `assessmentRatio: decStr` to the existing `tax` group (D-07, RESEARCH Open Question 3). Change the union to `z.discriminatedUnion('schemaVersion', [AssumptionsV1, AssumptionsV2])`. Set `CURRENT_VERSION = 2 as const`. Change `CurrentAssumptionSet = z.infer<typeof AssumptionsV2>`. Keep `AssumptionsV1` exported (migration source + golden-fixture parseability).
    In `defaults.ts`: change `DEFAULT_ASSUMPTIONS` to a V2-shaped object (still typed `: CurrentAssumptionSet`), `schemaVersion: 2`, retaining the existing V1 values and adding conservative new seeds tagged `[ASSUMED]` in comments (per RESEARCH A2): `appreciation.realAnnual "0.0075"`, `transaction.sellCostPct "0.065"`, `rent.realGrowthAnnual "0"`, `closing.rateOfPrice "0.025"`, `tax.assessmentRatio "1.0"`. All canonical decimal STRINGS.
    In `migrate.ts`: add `function v1ToV2(set)` that takes a parsed V1 set and returns a V2 set — copy the V1 slices, add the new slices from the V2 defaults, set `schemaVersion: 2`, add `assessmentRatio` under `tax`. Change `case 1:` to `return v1ToV2(set);` and add `case 2: return set;` as the new current. Keep the `assertNever` exhaustiveness guard.
    In `migrate.test.ts`: add a test that `migrate(<a V1 set>)` yields `schemaVersion === 2` AND carries the new slices with the V2-default values (proves the V1->V2 path is exercised, not identity); keep the existing rejects-unknown-version test. In `schema.test.ts`: add a test that a V2 object parses and that a float (e.g. `appreciation.realAnnual: 0.0075` as a number) is REJECTED at the boundary.
    Do NOT place fenced code in this action — follow the analog idioms named in read_first.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/assumptions/migrate.test.ts packages/core/src/assumptions/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `schema.ts` source contains `AssumptionsV2`, the union is `z.discriminatedUnion('schemaVersion', [AssumptionsV1, AssumptionsV2])`, and `CURRENT_VERSION = 2 as const`.
    - Every new tunable is declared via `decStr` — grep `appreciation|transaction|rent|closing|assessmentRatio` in schema.ts shows NO `z.number(` on those leaves.
    - `migrate.test.ts`: a V1 input migrates to a set with `schemaVersion === 2` and `appreciation.realAnnual === "0.0075"` (exact string), `transaction.sellCostPct === "0.065"`, `rent.realGrowthAnnual === "0"`, `closing.rateOfPrice === "0.025"`, `tax.assessmentRatio === "1.0"`.
    - `schema.test.ts`: parsing an object with a bare-number `appreciation.realAnnual` THROWS (Zod rejects non-decStr).
    - `npx vitest run packages/core/src/assumptions/migrate.test.ts packages/core/src/assumptions/schema.test.ts` exits 0.
    - `npm run typecheck` passes (DEFAULT_ASSUMPTIONS still satisfies `CurrentAssumptionSet`; a shape mismatch would be a compile error).
  </acceptance_criteria>
  <done>AssumptionsV2 is the current version, V2 defaults are seeded, and migrate exercises a real V1->V2 transform proven by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the FY-stamped town mill-rate table behind a Zod schema with a resolver</name>
  <files>packages/core/src/towns/town-table.schema.ts, packages/core/src/towns/town-table.ts, packages/core/src/towns/town-table.test.ts</files>
  <read_first>
    - packages/core/src/assumptions/schema.ts (reuse the EXPORTED `decStr` and the `.strict()` idiom; do not redefine the canonical-decimal regex)
    - packages/core/src/assumptions/defaults.ts (the "PURE DATA. No process/env reads, no Date, no computation" header convention; values as canonical decimal strings, object satisfies the schema type at compile time)
    - packages/core/src/time/calendar-date.ts (the validate-and-throw-on-miss resolver idiom at lines 18-24)
    - .planning/phases/02-tco-engine/02-PATTERNS.md §"towns/town-table.schema.ts" and §"towns/town-table.ts" (RESEARCH Pattern 5; Assumption A3 units; Open Question 1 per-row FY)
    - .planning/phases/02-tco-engine/02-RESEARCH.md §"Pattern 5" + §"Manual-Only Verifications" (spot-check 3-5 towns against the DLS report)
  </read_first>
  <action>
    Create `town-table.schema.ts`: import `decStr` from `../assumptions/schema.js`; export `townRowSchema = z.object({ town: z.string(), fy: z.number().int(), residentialMillRate: decStr }).strict()` and `export type TownRateRow = z.infer<typeof townRowSchema>`. `residentialMillRate` is stored AS PUBLISHED by DOR — dollars per $1,000 of assessed value (Assumption A3) — NOT pre-divided.
    Create `town-table.ts`: a PURE-DATA module (header mirroring `defaults.ts`: no `process`/env, no `Date`, no computation). Seed ~20-40 curated greater-Boston towns (e.g. Boston, Cambridge, Somerville, Newton, Brookline, Quincy, Medford, Malden, Arlington, Belmont, Watertown, Waltham, Lexington, Needham, Wellesley, Dedham, Milton, Braintree, Weymouth, Framingham, Natick, Melrose, Winchester, Woburn) with REAL FY-stamped residential rates transcribed from the DLS "Tax Rates by Class" report, FY-stamped PER ROW (mixed vintage allowed — Open Question 1). Type the array so it satisfies `readonly TownRateRow[]` at compile time (shape change = type error). Export `resolveMillRate(town: string): ResolvedMillRate` where `ResolvedMillRate = { readonly residentialMillRate: string; readonly fy: number }`; it looks up by canonical town name and THROWS a meaningful error on a miss (mirror the `calendarDate` throw idiom). Export the `ResolvedMillRate` type.
    Create `town-table.test.ts`: import from `vitest`; assert (a) the seeded table has >= 20 rows, (b) every row parses through `townRowSchema` (no float millRate sneaks in), (c) `resolveMillRate("Cambridge")` (or any seeded town) returns `{ residentialMillRate, fy }` with the EXACT decStr value + int fy from the table — proving the resolved pair is captured for the snapshot (Pitfall 11 / D-08), (d) `resolveMillRate("Nonexistent")` THROWS.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/towns/town-table.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `town-table.schema.ts` source contains `.strict()` and uses imported `decStr` for `residentialMillRate` (no local regex redefinition).
    - `town-table.ts` seeds >= 20 rows; `town-table.test.ts` asserts the row count >= 20 and that every row passes `townRowSchema.parse`.
    - `resolveMillRate("<a seeded town>")` returns the exact `{ residentialMillRate, fy }` pair present in the table (string + int equality, not `toBeCloseTo`).
    - `resolveMillRate("Nonexistent")` throws.
    - `npx vitest run packages/core/src/towns/town-table.test.ts` exits 0; `npm run typecheck` passes.
    - Manual spot-check note recorded in SUMMARY: 3-5 towns' rate+FY verified against the DLS "Tax Rates by Class" report.
  </acceptance_criteria>
  <done>A seeded, FY-stamped greater-Boston mill-rate table exists behind a Zod boundary with a resolver that throws on unknown towns and returns the snapshot-capturable rate+FY pair.</done>
</task>

<task type="auto">
  <name>Task 3: Widen ScenarioInputs and update the public barrel</name>
  <files>packages/core/src/engine/engine-input.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/engine/engine-input.ts (the `ScenarioInputs` interface at lines 21-23, the comment at 18-20 explicitly inviting this widening, and `engineInput()` which already `Object.freeze`s scenario at lines 40-50)
    - packages/core/src/index.ts (the existing barrel — what is and is NOT exported; the lines 7-9 rationale for never exporting raw Dec)
    - .planning/phases/02-tco-engine/02-PATTERNS.md §"engine/engine-input.ts (MODIFY)" and §"index.ts (MODIFY)"
    - .planning/phases/02-tco-engine/02-CONTEXT.md §"Scenario Input Contract" (D-14, D-15, D-16) and §"One-Time & MA-Lumpy Costs" (D-12, D-13)
  </read_first>
  <action>
    Widen `ScenarioInputs` keeping EVERY field `readonly`. Dollar inputs cross the boundary as canonical decimal STRINGS (the same convention `Money.of` consumes — do NOT use bare `number` for any dollar), rates as decStr-shaped strings: `price: string` (dollar string), `downPaymentPct: string` (rate string — D-14, loan = price * (1 - pct)), `annualRate: string` (rate string), `termMonths: number`, `holdingYears: number` (D-03), `town: string` (resolved against the town table — D-08), `insuranceAnnual: string` (flat $/yr — D-15), `hoaMonthly: string` (flat $/mo — D-15), `monthlyRent: string` (rent path input — D-01/D-06), `closingCostsOverride?: string` (optional dollar override — D-12), `otherOneTimeCosts?: string` (optional generic lump — D-13). Keep `label` (existing). Document in a comment that Phase 2 is fixed-rate only (D-16). No change to `engineInput()` (it already freezes scenario).
    In `index.ts`: export the new `AssumptionsV2` from schema; the widened `ScenarioInputs` is already re-exported (line 34) — keep it. Add exports for `resolveMillRate`, `type ResolvedMillRate`, and `type TownRateRow` from the towns module. Do NOT export `Dec`/`Decimal`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `engine-input.ts` `ScenarioInputs` contains `price`, `downPaymentPct`, `annualRate`, `termMonths`, `holdingYears`, `town`, `insuranceAnnual`, `hoaMonthly`, `monthlyRent`, optional `closingCostsOverride`, optional `otherOneTimeCosts` — all `readonly`, all dollar/rate fields typed `string` or `number` (grep shows NO bare dollar field typed `number` except `termMonths`/`holdingYears`).
    - `index.ts` exports `resolveMillRate`, `ResolvedMillRate`, `TownRateRow`, and `AssumptionsV2`; grep confirms `Dec` / `Decimal` are NOT exported.
    - `npm run typecheck` passes for the whole core graph (including the existing type-test files).
    - Note: the existing `golden.test.ts` `scenario: { label: ... }` may now be missing required fields — if so, this surfaces as a typecheck failure to be reconciled in Plan 04 (golden fixture regeneration). Record any such break in SUMMARY rather than silencing it.
  </acceptance_criteria>
  <done>ScenarioInputs carries the full house-scenario contract and the public barrel exposes the new town resolver + V2 schema for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deserialized JSON -> AssumptionSet | Snapshot/assumptions JSON parsed into a trusted in-memory set |
| deserialized data -> TownRateRow | Town table row data parsed into a trusted resolved rate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | AssumptionsV2 new slices | mitigate | Every new tunable is a `decStr` (canonical-decimal Zod boundary), never `z.number()`; `.strict()` rejects unknown keys; schema.test.ts asserts a bare-number leaf is rejected. |
| T-02-02 | Tampering | town-table row schema | mitigate | `townRowSchema` uses imported `decStr` for `residentialMillRate` + `.strict()`; a float mill rate or extra key is rejected at parse. |
| T-02-03 | Tampering (integrity) | unknown schemaVersion via migrate | mitigate | `migrate` re-validates through `parseAssumptionSet`; `discriminatedUnion` + `assertNever` reject a forged version (inherited T-03-01 discipline, extended to V2). |
| T-02-04 | Repudiation (determinism) | new towns/ + assumptions code | mitigate | Pure data + pure resolver; no `Date`/`process`/`Math.random` (inherited ESLint `no-restricted-globals` + runtime guard cover new files automatically). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: "Phase 2 installs no external packages"). No supply-chain surface added. |
</threat_model>

<verification>
- `npx vitest run packages/core/src/assumptions packages/core/src/towns` is green.
- `npm run typecheck` passes (AssumptionsV2 shape, widened ScenarioInputs, type-test graph).
- migrate exercises a real V1->V2 transform (not identity), proven by an explicit test.
- The seeded town table parses through Zod and the resolver throws on unknown towns.
</verification>

<success_criteria>
- AssumptionsV2 is the current version; DEFAULT_ASSUMPTIONS is V2-shaped; migrate(V1) -> V2 with defaulted new slices.
- ScenarioInputs is widened to the full house-scenario contract (D-14/D-15/D-16/D-12/D-13).
- A seeded, FY-stamped ~20-40 greater-Boston mill-rate table with a `resolveMillRate` resolver (TCO-02 data half) exists behind a Zod boundary.
- Public barrel exposes AssumptionsV2, widened ScenarioInputs, resolveMillRate, ResolvedMillRate, TownRateRow.
</success_criteria>

<output>
Create `.planning/phases/02-tco-engine/02-01-SUMMARY.md` when done. Record: the exact V2-default values seeded (flagged `[ASSUMED]` pending user confirmation), the towns seeded + the 3-5 DLS spot-checks performed, and any downstream typecheck break left for Plan 04's golden regeneration.
</output>
