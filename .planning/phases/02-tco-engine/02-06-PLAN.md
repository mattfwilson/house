---
phase: 02-tco-engine
plan: 06
type: execute
wave: 1
depends_on: ["02-05"]
gap_closure: true
files_modified:
  - packages/core/src/tco/amortization.ts
  - packages/core/src/tco/amortization.test.ts
  - packages/core/src/tco/pmi.ts
  - packages/core/src/tco/pmi.test.ts
  - packages/core/src/tco/tco.ts
  - packages/core/src/tco/tco.test.ts
  - packages/core/src/tco/rent-vs-buy.ts
  - packages/core/src/tco/rent-vs-buy.test.ts
  - packages/core/src/__fixtures__/tco-golden-snapshot.json
autonomous: true
requirements: [TCO-01, TCO-02, TCO-03, TCO-04, TCO-06, TCO-07]
must_haves:
  truths:
    - "rentVsBuy returns a result (no TypeError) on a valid input where holdingYears * 12 > termMonths (e.g. termMonths 180 held 20 years); out-of-range months read as zero remaining balance (loan fully paid = full equity)"
    - "scheduledPayment and amortizationSchedule return a finite straight-line result when annualRate='0' (loan/termMonths per period), never a divide-by-zero throw"
    - "PMI is charged in the rent-vs-buy monthly outflow ONLY while month <= dropOffMonth; after drop-off the buy outflow no longer includes the PMI premium"
    - "computeTco annualizes PMI consistent with drop-off (not monthlyPremium x 12 held flat for the whole hold when dropOffMonth falls inside the hold)"
    - "The rent-vs-buy buy-path monthly outflow grows over the hold with the appreciating property-tax + maintenance components; a later-year buy outflow exceeds the year-0 outflow when appreciation > 0"
    - "The committed tco-golden-snapshot.json recomputes cent-identically after the semantic fixes, regenerated ONLY via the gated UPDATE_GOLDEN path; the anti-funnel rent-wins and a buy-wins case still hold their winners"
  artifacts:
    - path: "packages/core/src/tco/rent-vs-buy.ts"
      provides: "crash-clamped schedule index + PMI-stops-at-dropoff + appreciating buy outflow"
      min_lines: 50
    - path: "packages/core/src/tco/amortization.ts"
      provides: "zero-rate guard in scheduledPayment + amortizationSchedule"
      min_lines: 50
    - path: "packages/core/src/__fixtures__/tco-golden-snapshot.json"
      provides: "regenerated committed golden fixture reflecting the semantic fixes"
      contains: "."
  key_links:
    - from: "packages/core/src/tco/rent-vs-buy.ts"
      to: "schedule.rows"
      via: "clamped index access (month-1 < rows.length ? row : zero balance)"
      pattern: "rows.length"
    - from: "packages/core/src/tco/rent-vs-buy.ts"
      to: "computePmi.dropOffMonth"
      via: "PMI charged in buy outflow only while month <= dropOffMonth"
      pattern: "dropOffMonth"
    - from: "packages/core/src/tco/amortization.ts"
      to: "zero-rate path"
      via: "isZero guard returns loan/termMonths straight-line"
      pattern: "isZero"
---

<objective>
Close the four calculation-correctness gaps verification found in the Phase 2 TCO engine (02-VERIFICATION.md): the two BLOCKER crashes (CR-01 out-of-bounds in `rentVsBuy`, CR-02 zero-rate divide-by-zero in amortization) and the two correctness WARNINGs that bias the rent-vs-buy verdict (WR-02 PMI charged flat past its `dropOffMonth`, WR-03 buy outflow held flat while rent compounds). Each fix is locked by a test that reproduces the defect RED first, then passes. Because WR-02 and WR-03 change the rent-vs-buy numbers (and WR-02 changes TCO's PMI annualization for PMI scenarios), regenerate the committed `tco-golden-snapshot.json` via the gated `UPDATE_GOLDEN` path with a reviewable diff and confirm the anti-funnel winners still hold.

Purpose: the existing 02-01..02-05 implementation is correct except for these four defects; this plan makes the engine crash-proof on realistic inputs and removes the asymmetric biases (Pitfall 6 cuts both ways) so the "computed correctly down to the cent" claim holds before Phase 4 layers FI-impact on top of `rentVsBuy`.

Output: guarded `amortization.ts`, drop-off-aware `tco.ts` + `rent-vs-buy.ts`, an appreciating buy-path outflow, new locking tests, and a regenerated golden fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tco-engine/02-CONTEXT.md
@.planning/phases/02-tco-engine/02-RESEARCH.md
@.planning/phases/02-tco-engine/02-PATTERNS.md
@.planning/phases/02-tco-engine/02-VALIDATION.md
@.planning/phases/02-tco-engine/02-VERIFICATION.md
</context>

<artifacts_this_phase_produces>
- **New test files:** none — tests are added to the existing `amortization.test.ts`, `pmi.test.ts`, `tco.test.ts` (create if absent), and `rent-vs-buy.test.ts`.
- **New helpers:** an internal PMI month-aware accessor in `rent-vs-buy.ts` (charge PMI only while `month <= dropOffMonth`); an internal per-year/per-month appreciating buy-outflow derivation in `rent-vs-buy.ts` (the growing property-tax + maintenance components recomputed via `assessedValueAt` / `homeValueAt`). No new exported public symbols.
- **Modified:** `amortization.ts` (zero-rate guard), `pmi.ts` (expose what `rentVsBuy` needs to charge PMI through `dropOffMonth` — `dropOffMonth` already exists on `PmiResult`; surface it / its monthly premium to `rentVsBuy`), `tco.ts` (drop-off-aware PMI annualization), `rent-vs-buy.ts` (clamp + PMI-stop + appreciating outflow), regenerated `__fixtures__/tco-golden-snapshot.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard the two BLOCKER crashes — CR-01 (clamp schedule index in rentVsBuy) + CR-02 (zero-rate guard in amortization)</name>
  <files>packages/core/src/tco/amortization.test.ts, packages/core/src/tco/amortization.ts, packages/core/src/tco/rent-vs-buy.test.ts, packages/core/src/tco/rent-vs-buy.ts</files>
  <behavior>
    - CR-02: scheduledPayment('400000','0','360') returns Money equal to loan/termMonths straight-line (400000/360 = 1111.111...) instead of throwing; amortizationSchedule('360000','0',360) builds a full schedule whose final balance is exactly $0.00 and whose principal sum equals the original loan exactly, with every period's principal = loan/termMonths (straight-line) and interest $0.00
    - CR-01: rentVsBuy on an input with termMonths 180, holdingYears 20 (so totalMonths 240 > schedule.rows.length 180) returns a RentVsBuyResult (no TypeError); for months past the schedule the remaining loan balance is treated as $0.00 (loan fully paid = full equity)
    - CR-01 sanity: the same scenario's buyEndingNetWorth reflects full home equity (no remaining balance) in the post-payoff years — equity at year 20 = appreciated home value x (1 - sellCostPct) + buy side-portfolio
  </behavior>
  <read_first>
    - packages/core/src/tco/amortization.ts (scheduledPayment lines 57-62 and amortizationSchedule lines 74-119 — both use div(pow.minus(1)); monthlyRate() at line 42; toCentsDec() at line 47)
    - packages/core/src/tco/amortization.test.ts (the existing exact-bigint oracle/invariant tests to mirror — final balance === 0n, principal sum === loan, no toBeCloseTo)
    - packages/core/src/tco/rent-vs-buy.ts (line 175: `schedule.rows[month - 1]!.balance.toDecimalString()`; the year-boundary block lines 171-180; totalMonths = holdingYears * 12 at line 133)
    - packages/core/src/tco/rent-vs-buy.test.ts (the existing anti-funnel rent-wins / buy-wins / symmetry cases to mirror the input-builder shape and exact-bigint assertion style)
    - packages/core/src/money/money.ts + packages/core/src/money/decimal-config.ts (Money.of / toCents / toFixed; Dec.isZero / Dec.div)
    - .planning/phases/02-tco-engine/02-VERIFICATION.md (the `missing:` array spells out both fixes verbatim)
  </read_first>
  <action>
    FIRST (RED) add the failing/crashing tests: in `amortization.test.ts` a zero-rate test asserting `scheduledPayment('400000','0','360').toCents() === 1111111n`-style exact-cent expectation derived from loan/termMonths (compute the exact expected cents from 400000/360 rounded HALF_EVEN at the Money boundary — assert the exact bigint, NO toBeCloseTo) and an `amortizationSchedule('360000','0',360)` test asserting `rows[359].balance.toCents() === 0n`, `sum(principal).toCents() === Money.of('360000').toCents()`, and `rows[0].interest.toCents() === 0n`; in `rent-vs-buy.test.ts` a CR-01 test that builds an input with `termMonths: 180, holdingYears: 20` and asserts `rentVsBuy(input)` does NOT throw and returns a result with `holdingYears === 20`.
    THEN fix CR-02 in `amortization.ts`: in `scheduledPayment`, after computing `const r = monthlyRate(annualRate)`, add `if (r.isZero()) return Money.of(new Dec(loan).div(termMonths).toFixed());` BEFORE the closed-form `div(pow.minus(1))`. In `amortizationSchedule`, add the analogous zero-rate branch: when `r.isZero()`, the level payment is `loan/termMonths`, each period's `interest` is $0.00 and `principal` is the straight-line split, balance decremented per period, with the SAME reconciled final period (final principal = remaining balance so the final balance is exactly $0.00). Keep all the existing cents-pinning (`toCentsDec`) discipline so the principal sum equals the loan exactly.
    THEN fix CR-01 in `rent-vs-buy.ts` at the year-boundary block (~line 175): replace the unguarded `schedule.rows[month - 1]!` with the clamp `const row = month - 1 < schedule.rows.length ? schedule.rows[month - 1]! : undefined; const remainingBalance = row ? new Dec(row.balance.toDecimalString()) : new Dec(0);`. Out-of-range months (loan paid off before horizon) get a zero remaining balance.
    No fenced code in this action.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/tco/amortization.test.ts packages/core/src/tco/rent-vs-buy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The zero-rate and CR-01 tests are committed RED first (the zero-rate test throws / the CR-01 test throws TypeError) BEFORE the fixes, then pass after.
    - `scheduledPayment('400000','0','360')` returns the exact straight-line cents (assert the exact bigint via `.toCents()`, NO `toBeCloseTo`).
    - `amortizationSchedule('360000','0',360)`: `rows[359].balance.toCents() === 0n`, `sum(rows.principal).toCents() === Money.of('360000').toCents()`, `rows[0].interest.toCents() === 0n` (all exact bigint).
    - `rentVsBuy({ ...input, termMonths: 180, holdingYears: 20 })` does not throw and returns `holdingYears === 20`; the existing anti-funnel rent-wins and buy-wins tests still pass with unchanged winners.
    - `amortization.ts` uses `r.isZero()` guards; `rent-vs-buy.ts` references `schedule.rows.length` in the clamp.
    - `npx vitest run packages/core/src/tco/amortization.test.ts packages/core/src/tco/rent-vs-buy.test.ts` exits 0; `npm run typecheck` passes.
  </acceptance_criteria>
  <done>Neither realistic input crashes: a loan held past payoff (holdingYears*12 > termMonths) returns full-equity numbers, and a zero-rate loan amortizes straight-line to exactly $0.00.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop PMI at dropOffMonth (WR-02) and grow the buy outflow with appreciation (WR-03)</name>
  <files>packages/core/src/tco/pmi.test.ts, packages/core/src/tco/pmi.ts, packages/core/src/tco/tco.test.ts, packages/core/src/tco/tco.ts, packages/core/src/tco/rent-vs-buy.test.ts, packages/core/src/tco/rent-vs-buy.ts</files>
  <behavior>
    - WR-02 in rentVsBuy: for a 90%-LTV scenario whose PMI drops at month D (D < totalMonths), the buy monthly outflow INCLUDES the PMI premium while month <= D and EXCLUDES it for month > D; a test proves the post-drop-off buy outflow is lower than the pre-drop-off outflow by exactly the PMI premium (holding other growth aside, or via a dedicated PMI-only assertion)
    - WR-02 in computeTco: PMI annualization is consistent with drop-off — when dropOffMonth falls inside year 1 the annualized PMI is NOT monthlyPremium x 12 flat; document and test the chosen representation (e.g. annualized PMI for year 0 reflects only the months PMI is actually charged, OR PMI is exposed as a drop-off-aware stream the breakdown documents)
    - WR-03 in rentVsBuy: the buy-path monthly outflow GROWS over the hold — the property-tax and maintenance components are recomputed per year on the appreciating assessed/home value (assessedValueAt / homeValueAt), while P+I, insurance, HOA stay per their real semantics (flat); a later-year buy outflow exceeds the year-0 outflow when appreciation > 0
    - WR-03 invariant: with appreciation = 0 the buy outflow is flat across the hold (no spurious growth), proving the growth is driven only by appreciation
  </behavior>
  <read_first>
    - packages/core/src/tco/pmi.ts (PmiResult.dropOffMonth + monthlyPremium already computed at lines 25-34, 70-86; computePmi opts/basis)
    - packages/core/src/tco/tco.ts (PMI line lines 151-160 — `lineFromAnnual(pmiResult.monthlyPremium.mul('12'))` is the flat annualization to fix; lineFromAnnual / pinToCents helpers lines 81-95; the year-0 breakdown convention documented lines 17-22)
    - packages/core/src/tco/rent-vs-buy.ts (buyMonthlyOutflow lines 121-122 = `tco.total.monthly.sub(tco.amortizedClosing.monthly)` held FLAT; the monthly loop lines 150-181 where buyOut is used; computeTco / schedule already in scope)
    - packages/core/src/tco/property-tax.ts (assessedValueAt(price, assessmentRatio, appreciationRealAnnual, year) at lines 63-72; annualPropertyTax at lines 53-55)
    - packages/core/src/tco/carrying-costs.ts (homeValueAt at lines 36-38; maintenanceAnnual at lines 27-29 — these are the appreciating components to recompute per year)
    - packages/core/src/tco/rent-vs-buy.test.ts + pmi.test.ts (existing exact-bigint assertion style + input builders)
    - .planning/phases/02-tco-engine/02-VERIFICATION.md (WR-02 + WR-03 prose, Anti-Patterns Found rows for tco.ts:160 and rent-vs-buy.ts:121-122)
    - .planning/phases/02-tco-engine/02-CONTEXT.md (D-04 separate appreciation, D-15 maintenance tracks appreciating value, D-11 closing is t=0 lump)
  </read_first>
  <action>
    FIRST (RED) add the failing tests: in `rent-vs-buy.test.ts`, a WR-02 test on a 90%-LTV input (downPaymentPct '0.10') asserting the buy monthly outflow used at a month AFTER `computePmi(...).dropOffMonth` is strictly less than the outflow used at a month BEFORE drop-off, and that the difference equals the PMI premium (exact bigint cents); and a WR-03 test asserting the buy outflow in a later hold year strictly exceeds the year-0 buy outflow when `appreciation.realAnnual > 0`, PLUS an invariant test that with `appreciation.realAnnual = '0'` the buy outflow is flat year-over-year. In `tco.test.ts` (create if absent), a WR-02 test that for a PMI scenario whose `dropOffMonth` is inside year 0/early hold, the PMI annualized figure is NOT `monthlyPremium x 12` flat (assert the documented drop-off-aware value with exact bigint).
    THEN fix WR-02: decide and document the cleanest drop-off representation. In `rent-vs-buy.ts`, derive the PMI premium and `dropOffMonth` from `computePmi` (the schedule is already built in scope) and charge the PMI portion of the buy monthly outflow ONLY while `month <= dropOffMonth` (so build the buy outflow as non-PMI-base + a month-gated PMI premium, rather than the flat `tco.total.monthly`). In `tco.ts`, make the PMI annualization drop-off-aware (e.g. for the year-0 breakdown, annualize PMI over only the months in year 0 that PMI is charged, or expose the dropOffMonth on the breakdown so consumers charge it correctly) — document the chosen convention in the header comment consistent with the existing year-0-snapshot note.
    THEN fix WR-03 in `rent-vs-buy.ts`: instead of holding `buyMonthlyOutflow` flat, recompute the GROWING components per hold year — property tax via `annualPropertyTax(assessedValueAt(price, assessmentRatio, appreciation.realAnnual, year), millRate)` and maintenance via `maintenanceAnnual(homeValueAt(price, appreciation.realAnnual, year), maintenancePctOfValue)` — and keep P+I / insurance / HOA at their real (flat) semantics, plus the month-gated PMI from WR-02. Use the captured mill rate from `computeTco(input)` (`tco.resolvedMillRate`) so the rent-vs-buy property tax matches the TCO breakdown's rate. All compounding/comparison in Dec; dollars Money.
    No fenced code in this action.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/tco/rent-vs-buy.test.ts packages/core/src/tco/tco.test.ts packages/core/src/tco/pmi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - WR-02 + WR-03 tests committed RED first (asserting the OLD flat behavior fails) then pass after the fix.
    - PMI test: the buy monthly outflow at a month > `dropOffMonth` is strictly less than at a month <= `dropOffMonth`, and the difference equals the PMI premium (exact bigint cents); a PMI borrower is NOT charged PMI in year 20.
    - computeTco PMI annualization is drop-off-aware (the assertion proves it differs from `monthlyPremium x 12` when drop-off is inside the annualized window); the chosen convention is documented in the `tco.ts` header.
    - WR-03 test: later-year buy outflow `> ` year-0 buy outflow when appreciation > 0 (exact bigint comparison); with appreciation = '0' the buy outflow is flat year-over-year (exact bigint equality).
    - `rent-vs-buy.ts` references `dropOffMonth`, `assessedValueAt`/`homeValueAt`, and `tco.resolvedMillRate`; compounding in Dec, dollars Money; NO `toBeCloseTo`.
    - The existing anti-funnel rent-wins and buy-wins tests still pass; winners are unchanged after the semantic fixes (the bias corrections may move the magnitudes but the documented winners hold).
    - `npx vitest run packages/core/src/tco/rent-vs-buy.test.ts packages/core/src/tco/tco.test.ts packages/core/src/tco/pmi.test.ts` exits 0; `npm run typecheck` passes.
  </acceptance_criteria>
  <done>PMI stops at its drop-off month in both the TCO annualization and the rent-vs-buy outflow, and the buy-path monthly outflow grows with the appreciating tax + maintenance — removing both asymmetric biases (Pitfall 6).</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the committed golden fixture (gated) and confirm the anti-funnel winners hold</name>
  <files>packages/core/src/__fixtures__/tco-golden-snapshot.json</files>
  <read_first>
    - packages/core/src/golden.test.ts (the gated `UPDATE_GOLDEN === '1'` write branch lines 113-127; `canonicalTcoResult` lines 91-95 = `canonicalJson({ tco, rentVsBuy })`; the deliberate NON-use of toMatchSnapshot lines 16-24; FIXED_SCENARIO lines 49-60 — Newton $450k 20%-down so NO PMI, but the WR-03 appreciating-outflow + zero-rate-safe paths still move rentVsBuy numbers)
    - packages/core/src/__fixtures__/tco-golden-snapshot.json (the committed fixture to regenerate — note the current figures: total $3,280.61/mo, buy $168,035.61 vs rent $224,885.81 rent-wins)
    - package.json (the `update-golden` script wiring `UPDATE_GOLDEN=1`)
    - .planning/phases/02-tco-engine/02-PATTERNS.md §"golden.test.ts" (regenerate via npm run update-golden; NEVER toMatchSnapshot / -u)
    - .planning/phases/02-tco-engine/02-VERIFICATION.md (human_verification: the committed fixture sanity figures to re-confirm post-regen)
  </read_first>
  <action>
    After Tasks 1-2 change the semantics, the committed `tco-golden-snapshot.json` will move (WR-03 grows the buy outflow over the FIXED_SCENARIO's 10-year hold; the FIXED_SCENARIO is 20%-down so WR-02 PMI does not apply to it, but rentVsBuy's per-year recomputation still shifts the numbers). Regenerate the fixture ONLY via the gated path: run `npm run update-golden` (which sets `UPDATE_GOLDEN=1`) so `golden.test.ts` rewrites `tco-golden-snapshot.json`, producing a reviewable git diff. Do NOT use `toMatchSnapshot` or `vitest -u`. Inspect the diff: confirm the headline figures remain sane for a $450k Newton house (monthly P+I unchanged at $2,275.44 — P+I is unaffected by these fixes; property tax / maintenance / total may shift slightly; rentVsBuy buy-vs-rent numbers move) and that the FIXED_SCENARIO still has RENT winning (or document the new winner if the bias correction legitimately flips it — note it in the SUMMARY for the human sanity check). Then run the full suite WITHOUT `UPDATE_GOLDEN` to confirm the committed fixture deep-equals the recomputed result.
    No fenced code in this action.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/golden.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tco-golden-snapshot.json` is regenerated via `npm run update-golden` (gated `UPDATE_GOLDEN=1`) — NOT `toMatchSnapshot` / `-u`; the git diff is reviewable (a JSON value diff, not a structural rewrite).
    - Running `npx vitest run packages/core/src/golden.test.ts` WITHOUT `UPDATE_GOLDEN` is green against the regenerated fixture; the round-trip TCO assertion is green.
    - The regenerated fixture's P+I monthly is still $2,275.44 (unchanged — these fixes do not touch P+I); the rentVsBuy winner for FIXED_SCENARIO is recorded in the SUMMARY (rent-wins unless the bias correction legitimately flips it, in which case the flip is documented).
    - The anti-funnel rent-wins case and the buy-wins case in `rent-vs-buy.test.ts` still hold their winners.
    - `npx vitest run packages/core/src/golden.test.ts` exits 0; `npm run typecheck` passes.
  </acceptance_criteria>
  <done>The full TCO + rent-vs-buy golden fixture recomputes cent-identically against a gated, reviewable regeneration that reflects the four semantic fixes, with the anti-funnel verdict confirmed sane.</done>
</task>

</tasks>

<verification_phase_gate>
After this plan, run the full phase gate (VALIDATION.md §"Sampling Rate" -> Phase gate):
- `npm test` (full core suite, coverage-gated: lines 95 / functions 95 / branches 90 / statements 95) is green.
- `npm run typecheck` passes (all `*.type-test.ts` in the tsc -b graph).
- Golden fixture regenerated-and-reviewed; SC1 invariant + oracle tests still pass with EXACT equality (no `toBeCloseTo` anywhere in `tco/`).
- The four CR/WR locking tests are present and green.
</verification_phase_gate>

<human_verification>
<verify>
  <human-check>
    Per `human_verify_mode: end-of-phase` — at phase end confirm: (1) the regenerated `tco-golden-snapshot.json` headline numbers remain sane for the Newton $450k scenario (P+I $2,275.44 unchanged; total + rentVsBuy figures in a believable range), and (2) the rent-vs-buy winner for FIXED_SCENARIO and the anti-funnel rent-wins case still feel like plausible greater-Boston outcomes after the bias corrections. Reasonableness sanity check, not a re-derivation.
  </human-check>
</verify>
</human_verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EngineInput -> RentVsBuyResult / TcoBreakdown | Frozen snapshot consumed; Money-typed result. A malformed schedule index or zero rate must not crash the calc. |
| deserialized golden JSON -> recompute | Round-trip parses assumptions through Zod before recompute (unchanged here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service (crash) | rentVsBuy out-of-bounds schedule access when holdingYears*12 > termMonths | mitigate | Clamp the schedule index; out-of-range months read as zero remaining balance; CR-01 locking test with termMonths 180 / holdingYears 20. |
| T-06-02 | Denial of Service (crash) | amortization divide-by-zero on annualRate='0' | mitigate | `r.isZero()` guard returning loan/termMonths straight-line in scheduledPayment + amortizationSchedule; zero-rate locking test. |
| T-06-03 | Tampering (correctness bias) | PMI charged flat past dropOffMonth overstates ownership cost (biases "rent") | mitigate | Charge PMI only while month <= dropOffMonth in both tco.ts annualization and rentVsBuy outflow; post-drop-off test. |
| T-06-04 | Tampering (correctness bias) | buy outflow flat while rent compounds (biases "buy") | mitigate | Recompute growing property-tax + maintenance per year on the appreciating value; later-year-exceeds-year-0 test + appreciation=0 flat invariant. |
| T-06-05 | Tampering (silent drift) | golden fixture auto-bless | mitigate | Gated `UPDATE_GOLDEN=1` write only (reviewable git diff); deep-equal against committed fixture; deliberately NOT `toMatchSnapshot`. |
| T-06-06 | Tampering (integrity) | float reintroduction in the new growth/PMI math | mitigate | All compounding/comparison in `Dec`; dollars `Money` via `.toFixed()`; exact-bigint tests; inherited `tco.type-test.ts` no-bare-number guard. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit; this plan adds no dependencies). |
</threat_model>

<verification>
- `npx vitest run packages/core/src/tco/amortization.test.ts packages/core/src/tco/pmi.test.ts packages/core/src/tco/tco.test.ts packages/core/src/tco/rent-vs-buy.test.ts packages/core/src/golden.test.ts` is green.
- CR-01 (no crash on holdingYears*12 > termMonths), CR-02 (zero-rate straight-line), WR-02 (PMI stops at dropOffMonth), WR-03 (buy outflow grows with appreciation) all proven by exact-bigint locking tests.
- Golden fixture regenerated via the gated path; full TCO + rent-vs-buy recomputes cent-identically; anti-funnel winners hold.
- `npm test` full suite + coverage gate green; `npm run typecheck` passes.
</verification>

<success_criteria>
- The two BLOCKER crashes (CR-01, CR-02) are guarded with reproducing-then-passing tests.
- The two correctness WARNINGs (WR-02 PMI-stops-at-dropoff, WR-03 appreciating buy outflow) are fixed in both `tco.ts` and `rent-vs-buy.ts`, removing the asymmetric biases.
- The committed `tco-golden-snapshot.json` is regenerated via gated `UPDATE_GOLDEN` only, recomputes cent-identically, and the anti-funnel verdict is confirmed sane.
</success_criteria>

<output>
Create `.planning/phases/02-tco-engine/02-06-SUMMARY.md` when done. Record: the exact zero-rate and CR-01 expected figures asserted, the chosen WR-02 PMI-annualization representation, the FIXED_SCENARIO rentVsBuy winner after regeneration (and whether it flipped), and confirmation the full core suite + coverage gate + typecheck pass (phase gate).
</output>
