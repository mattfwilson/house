---
phase: 06-persistence-listings-adapter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/package.json
  - packages/app/tsconfig.json
  - packages/app/vitest.config.ts
  - packages/app/drizzle.config.ts
  - tsconfig.json
  - .gitignore
  - package.json
  - package-lock.json
autonomous: false
requirements: [PROF-01, PROF-02, PROF-03]
user_setup: []
must_haves:
  truths:
    - "packages/app exists as an npm workspace package (@house/app) consuming @house/core via the workspace protocol (D-01/D-04)"
    - "better-sqlite3@12, drizzle-orm@0.45 are runtime deps and drizzle-kit@0.31 + @types/better-sqlite3 are dev deps of @house/app ONLY — never added to packages/core (D-01/D-02)"
    - "better-sqlite3 loads on this machine: `new Database(':memory:')` succeeds (Node 24 prebuilt binary, Pitfall 3)"
    - "packages/app/vitest.config.ts uses mergeConfig(defineProject, sharedTest) name:'app' environment:'node' — it does NOT extend the root and does NOT load core's determinism guard.setup"
    - "*.sqlite / *.db are gitignored; committed drizzle SQL is NOT ignored (Pitfall 4)"
    - "root tsconfig references ./packages/app so tsc -b builds the new package"
  artifacts:
    - path: "packages/app/package.json"
      provides: "@house/app workspace manifest with core + sqlite deps"
      contains: "@house/core"
    - path: "packages/app/vitest.config.ts"
      provides: "app vitest project (node env)"
      contains: "mergeConfig"
    - path: "packages/app/drizzle.config.ts"
      provides: "drizzle-kit config (dialect sqlite, schema path, out ./drizzle)"
      contains: "dialect"
  key_links:
    - from: "tsconfig.json"
      to: "packages/app"
      via: "references entry"
      pattern: "packages/app"
    - from: "packages/app/vitest.config.ts"
      to: "vitest.shared.ts"
      via: "sharedTest import"
      pattern: "sharedTest"
---

<objective>
Scaffold `packages/app` — the first non-core workspace package and the home of the imperative shell (D-01). Create its `package.json`/`tsconfig.json`/`vitest.config.ts`/`drizzle.config.ts`, wire it into the root project graph (`tsconfig.json` references, root `.gitignore`), and install the SQLite stack (better-sqlite3, drizzle-orm, drizzle-kit, @types/better-sqlite3) into `@house/app` only.

Purpose: Every Wave-2/3 adapter, repository, and service needs this package to exist, build, and test before it can be written. This plan is pure scaffolding — no schema, no adapters yet (those are 06-03+). The `@types/better-sqlite3` package is `[ASSUMED]` in the RESEARCH Package Legitimacy Audit, so a blocking human checkpoint verifies it before any install (package legitimacy gate).
Output: A buildable, testable empty `@house/app` package + the SQLite toolchain installed and smoke-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-persistence-listings-adapter/06-RESEARCH.md
@.planning/phases/06-persistence-listings-adapter/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
NEW files created here: `packages/app/{package.json,tsconfig.json,vitest.config.ts,drizzle.config.ts}`. Modified: root `tsconfig.json` (references), root `.gitignore`, root `package.json`/`package-lock.json` (workspace dep tree). No source symbols yet — schema/db/adapters land in 06-03+.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @house/app config files + root project-graph edits</name>
  <files>packages/app/package.json, packages/app/tsconfig.json, packages/app/vitest.config.ts, packages/app/drizzle.config.ts, tsconfig.json, .gitignore</files>
  <read_first>
    - packages/core/package.json (exact manifest shape: type module, exports ".": "./src/index.ts", scripts test/typecheck)
    - packages/core/tsconfig.json (extends ../../tsconfig.base.json, rootDir/outDir/composite, exclude test files)
    - packages/core/vitest.config.ts (mergeConfig(defineProject(...), sharedTest) shape)
    - vitest.shared.ts, tsconfig.base.json (exactOptionalPropertyTypes + NodeNext already enforced)
    - tsconfig.json (root references array), .gitignore (existing entries), package.json (root workspaces + scripts)
    - .planning/phases/06-persistence-listings-adapter/06-PATTERNS.md (§ Config-file Patterns — copy-from-existing instructions)
    - .planning/phases/06-persistence-listings-adapter/06-RESEARCH.md (drizzle.config.ts sketch lines 307-316, A5)
  </read_first>
  <action>
    Create `packages/app/package.json`: name `@house/app`, `"type": "module"`, `"private": true`, `exports { ".": "./src/index.ts" }`, scripts `test` (vitest run) + `typecheck` (tsc -b). Declare dependencies `@house/core: "*"` (workspace protocol), `better-sqlite3: "^12.11.1"`, `drizzle-orm: "^0.45.2"`; devDependencies `drizzle-kit: "^0.31.10"`, `@types/better-sqlite3` (latest). (The actual install happens in Task 3 after the legitimacy checkpoint.)
    Create `packages/app/tsconfig.json` by copying `packages/core/tsconfig.json` with ONE change: `"types": ["node", "better-sqlite3"]` (core uses `[]`). Keep `extends: "../../tsconfig.base.json"`, `rootDir: "src"`, `outDir: "dist"`, `composite: true`, `include: ["src"]`, and exclude `src/**/*.test.ts`. Add `"references": [{ "path": "../core" }]` so app builds after core.
    Create `packages/app/vitest.config.ts` exactly per the core analog: `mergeConfig(defineProject({ test: { ...sharedTest, name: 'app', environment: 'node' } }), {})` importing `sharedTest` from `'../../vitest.shared.js'`. Do NOT add `setupFiles` (core's determinism guard is core-only).
    Create `packages/app/drizzle.config.ts` per RESEARCH A5: `defineConfig({ dialect: 'sqlite', schema: './src/adapters/persistence/schema.ts', out: './drizzle', dbCredentials: { url: process.env.DB_FILE_NAME ?? './house.sqlite' } })` from `'drizzle-kit'`.
    Edit root `tsconfig.json`: add `{ "path": "./packages/app" }` to `references`.
    Edit root `.gitignore`: add `*.sqlite`, `*.sqlite-wal`, `*.sqlite-shm`, `*.db`. Do NOT ignore `packages/app/drizzle/` (those committed SQL files are a reproducibility artifact, D-11).
  </action>
  <verify>
    <automated>node -e "const p=require('./packages/app/package.json'); if(p.name!=='@house/app') process.exit(1); if(!p.dependencies['@house/core']) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/package.json` name is `@house/app` with `@house/core`, `better-sqlite3`, `drizzle-orm` in deps and `drizzle-kit` in devDeps
    - `packages/app/vitest.config.ts` uses `mergeConfig` + `sharedTest`, `name: 'app'`, `environment: 'node'`, NO setupFiles
    - root `tsconfig.json` `references` includes `./packages/app` (source assertion: grep `packages/app` in tsconfig.json)
    - `.gitignore` contains `*.sqlite` (source assertion: grep `*.sqlite`); does NOT contain `drizzle/`
  </acceptance_criteria>
  <done>The @house/app package and its four config files exist and are wired into the root project graph; no deps installed yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [Package legitimacy gate] Verify @types/better-sqlite3 before install</name>
  <what-built>
    Config scaffolding for @house/app is complete. The next task installs the SQLite stack. The three runtime/dev packages (better-sqlite3, drizzle-orm, drizzle-kit) are slopcheck-verified `[OK]` in the RESEARCH Package Legitimacy Audit. `@types/better-sqlite3` is flagged `[ASSUMED]` (not slopcheck-scanned) — the legitimacy gate requires human verification before installing it. This checkpoint is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy gates).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@types/better-sqlite3
    2. Confirm it is published under the DefinitelyTyped umbrella (repository link points to github.com/DefinitelyTyped/DefinitelyTyped), has substantial weekly download volume (millions), and a recent publish date.
    3. Confirm it is the standard types package for `better-sqlite3` (description references better-sqlite3), not a typosquat.
  </how-to-verify>
  <action>
    Pause for the human to verify `@types/better-sqlite3` legitimacy on npmjs.com per the steps above. Do NOT run any install (Task 3) until the resume-signal is given — this is a blocking-human package-legitimacy gate and is NOT auto-approvable. On "approved", proceed to Task 3; on a reported anomaly (typosquat / abandoned / suspicious publisher), halt and surface it instead of installing.
  </action>
  <resume-signal>Type "approved" to proceed with the install, or report any anomaly (typosquat, abandoned, suspicious publisher) to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install the SQLite stack into @house/app + smoke-test the native binary</name>
  <files>package.json, package-lock.json, packages/app/package.json</files>
  <read_first>
    - .planning/phases/06-persistence-listings-adapter/06-RESEARCH.md (Installation block lines 90-99; Pitfall 3 native-module smoke check lines 383-388; Environment Availability lines 405-415)
    - packages/app/package.json (the dep declarations from Task 1)
  </read_first>
  <action>
    From the repo root, install the verified stack into the @house/app workspace ONLY (never into packages/core): `npm install better-sqlite3@12 drizzle-orm@0.45 -w @house/app` then `npm install -D drizzle-kit@0.31 @types/better-sqlite3 -w @house/app`. After install, run the Pitfall-3 smoke check: a one-liner that does `new Database(':memory:')` via the installed better-sqlite3 and exits 0 (proves the Node-24 prebuilt binary loaded — NOT a source compile). If it throws "Could not locate the bindings file", run `npm rebuild better-sqlite3`. Confirm `packages/core/package.json` was NOT modified (deps still only decimal.js + zod).
  </action>
  <verify>
    <automated>node -e "const Database=require('better-sqlite3'); const db=new Database(':memory:'); db.exec('CREATE TABLE t(x)'); db.prepare('INSERT INTO t(x) VALUES (?)').run(1); const r=db.prepare('SELECT x FROM t').get(); if(r.x!==1) process.exit(1); console.log('better-sqlite3 OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `new Database(':memory:')` + a real insert/select round-trip succeeds (CLI prints `better-sqlite3 OK`)
    - better-sqlite3 + drizzle-orm appear under `packages/app` deps; drizzle-kit + @types/better-sqlite3 under devDeps (source assertion: grep them in packages/app/package.json)
    - `packages/core/package.json` dependencies unchanged (still exactly decimal.js + zod — no SQLite deps leaked into core, D-02)
    - `npm test` (full suite, now including the empty/passing app project) does not error on project discovery
  </acceptance_criteria>
  <done>The SQLite toolchain is installed in @house/app and the native binary is proven to load and execute on this machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Installing third-party packages (incl. a native module with a postinstall) crosses a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | npm installs (better-sqlite3, drizzle-orm, drizzle-kit, @types/better-sqlite3) | mitigate | RESEARCH Package Legitimacy Audit: 3 primaries slopcheck `[OK]` + version-matched to CLAUDE.md; `@types/better-sqlite3` is `[ASSUMED]` → blocking-human checkpoint (Task 2) verifies on npmjs.com before install. better-sqlite3 postinstall is the standard `prebuild-install || build-from-source` (no network-call red flags). |
| T-06-04 | Tampering | dev .sqlite DB file path | accept/mitigate | DB path is app-supplied config (default `./house.sqlite`), not user input; `.sqlite`/`.db` gitignored so the local DB never enters version control (Pitfall 4). No path-traversal surface (no untrusted path input this phase). |
| T-06-05 | Elevation | core package purity | mitigate | SQLite deps installed with `-w @house/app` only; Task 3 asserts `packages/core/package.json` is unchanged so the zero-framework-dep core rule holds (D-02). |
</threat_model>

<verification>
- `node -e` smoke check: better-sqlite3 in-memory insert/select round-trip succeeds
- `npx tsc -b` builds (app tsconfig is valid even with an empty src — or src absent; if tsc requires at least one input, that is satisfied once 06-03 adds files; here the gate is config validity + dep install)
- `packages/core` deps unchanged; `.gitignore` covers `*.sqlite*`
</verification>

<success_criteria>
- @house/app is an installable, project-graph-wired npm workspace package with the SQLite stack present
- @types/better-sqlite3 legitimacy human-verified before install
- better-sqlite3 native binary loads and executes on this machine
</success_criteria>

<output>
Create `.planning/phases/06-persistence-listings-adapter/06-02-SUMMARY.md` when done
</output>
