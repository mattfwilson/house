---
phase: 03-affordability-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/engine/engine-input.ts
  - packages/core/src/engine/engine-input.test.ts
  - packages/core/src/engine/engine-input.type-test.ts
autonomous: true
requirements: [AFF-02]
must_haves:
  truths:
    - "EngineInput carries an optional household block validated at assembly exactly like scenario (D-09)"
    - "A forged household snapshot (extra key, non-canonical decStr, targetSavingsRate >= 1) is REJECTED at the boundary, not silently computed (V5 input validation)"
    - "household includes currentAnnualSavings so the savings-rate floor is well-defined (D-17)"
    - "TCO-only callers and the existing tco-golden-snapshot.json are unaffected (household is optional, A3)"
  artifacts:
    - path: "packages/core/src/engine/engine-input.ts"
      provides: "Household interface + HouseholdSchema + parseHousehold + optional household on EngineInput/engineInput()"
      contains: "HouseholdSchema"
    - path: "packages/core/src/engine/engine-input.test.ts"
      provides: "parseHousehold boundary validation cases (accept/reject) + engineInput household round-trip/freeze"
      contains: "parseHousehold"
    - path: "packages/core/src/engine/engine-input.type-test.ts"
      provides: "household shape no-bare-number / decStr-string guard"
  key_links:
    - from: "packages/core/src/engine/engine-input.ts engineInput()"
      to: "parseHousehold"
      via: "validate-when-present at assembly"
      pattern: "parts\\.household \\? Object\\.freeze\\(parseHousehold"
---

<objective>
Introduce the durable `household` (profile) input contract on `EngineInput` — the new
person-vs-house block (D-09) the affordability solvers in Waves 2-3 consume and Phase 6 will
persist. It is a Zod-validated trust boundary mirroring `ScenarioInputs`: `.strict()`,
`decStr` leaves, a `[0,1)` refine on `targetSavingsRate`, and a `parseHousehold` loader.
`household` is made OPTIONAL on `EngineInput`/`engineInput()` (A3) so existing TCO-only call
sites and the byte-identical `tco-golden-snapshot.json` need no change; the affordability
entry points (Waves 2-3) require/validate its presence.

This plan ships the household FIELDS — including `currentAnnualSavings` (D-17), the field that
makes the savings-rate floor well-defined — plus its Wave 0 boundary-validation test scaffolds.

Purpose: every downstream affordability function reads `input.household`; the contract and its
trust boundary must exist (and be proven to reject forged input) before any solver is written.
Output: an extended `engine-input.ts` (interface + schema + loader + optional wiring) and the
extended `engine-input.test.ts` / `engine-input.type-test.ts` proving the boundary.
</objective>

<artifacts_this_phase_produces>
This plan introduces (Phase 3 overall artifacts continue in plans 02-04):
- `Household` interface — fields: `grossAnnualIncome`, `existingMonthlyDebt`,
  `targetSavingsRate`, `availableNetWorth`, `currentRent`, `downPaymentCash`, `reserve`,
  `currentAnnualSavings` (all canonical decimal STRINGS) — on `packages/core/src/engine/engine-input.ts`.
- `HouseholdSchema` (Zod `.strict()`) + `parseHousehold(input: unknown): Household` loader.
- `household?: Household` added OPTIONALLY to the `EngineInput` interface and the `engineInput()`
  factory parts (validated-when-present at assembly).
Exports of these symbols from `packages/core/src/index.ts` land in Plan 04 (the barrel plan).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-affordability-engine/03-CONTEXT.md
@.planning/phases/03-affordability-engine/03-PATTERNS.md
@.planning/phases/03-affordability-engine/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Household interface, HouseholdSchema, and parseHousehold loader</name>
  <files>packages/core/src/engine/engine-input.ts</files>
  <read_first>
    - packages/core/src/engine/engine-input.ts (the in-file twin: the `ScenarioInputs`
      interface L28-53, `ScenarioInputsSchema` L73-94 incl. the `downPaymentPct` `[0,1)`
      `.refine` L77-83, and `parseScenarioInputs` L103-105 — copy this triad's structure)
    - packages/core/src/assumptions/schema.ts (confirm `decStr` is the canonical-decimal-string
      validator already imported at engine-input.ts L16)
    - .planning/phases/03-affordability-engine/03-PATTERNS.md (`engine/engine-input.ts (MODIFY)`
      assignment — the (1) interface / (2) schema / (3) loader breakdown and the exact field list)
  </read_first>
  <action>
    Add a `Household` interface mirroring `ScenarioInputs`: every dollar leaf a canonical decimal
    STRING (never a bare number), `readonly`. Fields per D-10 + D-17: `grossAnnualIncome`,
    `existingMonthlyDebt` (single monthly minimum-obligations total, NOT balances — D-10),
    `targetSavingsRate` (vs gross, D-02/D-04), `availableNetWorth` (investable NW for the
    cash-on-hand gate, D-05), `currentRent` (household-level monthly, distinct from
    `scenario.monthlyRent`, D-11), `downPaymentCash` (fixed dollar down payment, D-07), `reserve`
    (cash buffer subtracted in the gate, D-05; consumed as-is, no engine default per A1), and
    `currentAnnualSavings` (D-17 — baseline annual savings while renting; the savings-rate floor's
    denominator-baseline). Document gross-not-net on `grossAnnualIncome` (Pitfall 2) and the
    distinct-from-scenario.monthlyRent note on `currentRent` (D-11).

    Add `HouseholdSchema = z.object({...}).strict()` mirroring `ScenarioInputsSchema`: each dollar
    field `decStr`; `targetSavingsRate` is `decStr.refine((s) => { const n = Number(s); return n >= 0 && n < 1; }, { message: 'targetSavingsRate must be in [0,1)' })`
    — the same boundary-range idiom as `downPaymentPct` (range check on the already-canonical
    string, NOT money math, so `Number(s)` is acceptable here). `.strict()` rejects unknown keys
    (V5 / T-03-V5). Add `parseHousehold(input: unknown): Household` returning
    `HouseholdSchema.parse(input) as Household` (mirrors `parseScenarioInputs`).
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/engine/engine-input.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `engine-input.ts` exports `Household`, `HouseholdSchema`, `parseHousehold`.
    - Source: `Household` has exactly the 8 string fields above incl. `currentAnnualSavings` (D-17)
      and `reserve`; no bare-`number` dollar field.
    - Source: `targetSavingsRate` uses the `[0,1)` `.refine` idiom copied from `downPaymentPct`.
    - Source: `HouseholdSchema` ends in `.strict()`.
    - `npx vitest run packages/core/src/engine/engine-input.test.ts` passes (Task 3 cases).
  </acceptance_criteria>
  <done>Household interface + HouseholdSchema + parseHousehold exist and compile; the boundary
  test file (Task 3) passes against them.</done>
</task>

<task type="auto">
  <name>Task 2: Wire household OPTIONALLY into EngineInput and engineInput()</name>
  <files>packages/core/src/engine/engine-input.ts</files>
  <read_first>
    - packages/core/src/engine/engine-input.ts (the `EngineInput` interface L111-115 and the
      `engineInput()` factory L122-134 — note how `scenario` is validated at assembly via
      `Object.freeze(parseScenarioInputs(parts.scenario))` on L132)
    - .planning/phases/03-affordability-engine/03-RESEARCH.md (§Runtime State Inventory + A3 — the
      optional-on-input, required-by-affordability recommendation; the requirement that the
      existing `tco-golden-snapshot.json` stay byte-identical)
  </read_first>
  <action>
    Add `readonly household?: Household` to the `EngineInput` interface (alongside `asOf`,
    `assumptions`, `scenario`). Add `readonly household?: ...` to the `engineInput()` `parts`
    parameter. In the factory body, validate ONLY when present, mirroring the scenario line:
    `household: parts.household ? Object.freeze(parseHousehold(parts.household)) : undefined`.
    Keep `asOf` clock-free and the whole object `Object.freeze`d. Do NOT make `household` required
    — TCO-only callers (`computeTco`, `rentVsBuy`, the golden `fixedInput()`) must keep working
    untouched, and the existing TCO golden snapshot must remain byte-identical (A3). The
    affordability solvers in Waves 2-3 will require `household` at their own entry points.
  </action>
  <verify>
    <automated>npm run -w @house/core typecheck && npx vitest run packages/core/src/golden.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `EngineInput` has `readonly household?: Household` (optional).
    - Source: `engineInput()` validates household when present via `parseHousehold` and freezes it,
      passing `undefined` when absent.
    - Behavior: `npx vitest run packages/core/src/golden.test.ts` passes with NO change to
      `tco-golden-snapshot.json` (TCO ignores household; a no-diff `UPDATE_GOLDEN` confirms it).
    - `npm run -w @house/core typecheck` passes (tsc -b graph, incl. the type-test from Task 4).
  </acceptance_criteria>
  <done>EngineInput/engineInput() accept an optional, boundary-validated household; all existing
  TCO/golden tests stay green and byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 — household boundary validation tests (extend engine-input.test.ts)</name>
  <files>packages/core/src/engine/engine-input.test.ts</files>
  <behavior>
    - parseHousehold ACCEPTS a well-formed VALID_HOUSEHOLD (returns the parsed object)
    - parseHousehold REJECTS a non-canonical decStr leaf (e.g. "100,000", "1e5", "  5 ")
    - parseHousehold REJECTS an unknown extra key (.strict() — forged-snapshot V5 control)
    - parseHousehold REJECTS a missing required field (e.g. omit currentAnnualSavings)
    - parseHousehold REJECTS targetSavingsRate >= 1 and < 0; ACCEPTS a value in [0,1)
    - engineInput({...,household}) freezes household and round-trips it; engineInput WITHOUT
      household yields household === undefined and still freezes the object
  </behavior>
  <read_first>
    - packages/core/src/engine/engine-input.test.ts (the `parseScenarioInputs` describe-block
      L90-184: the `VALID_SCENARIO` const L25-36, accepts L91-94, rejects non-canonical decStr
      L144-160, rejects `.strict()` extra key L163-167, rejects missing field L174-178, and the
      `downPaymentPct ∈ [0,1)` range cases L126-142 — mirror each for household; plus the
      `engineInput` factory tests L44-88)
    - .planning/phases/03-affordability-engine/03-VALIDATION.md (the two `boundary` rows: `.strict()`
      rejects extra keys / decStr leaves / range refines; round-trip lossless)
  </read_first>
  <action>
    Add a `VALID_HOUSEHOLD` const (mirror `VALID_SCENARIO`) with realistic decimal-string values
    incl. `currentAnnualSavings`. Add a `parseHousehold` describe-block copying the
    `parseScenarioInputs` block's exact shape: accepts well-formed; rejects a non-canonical decStr
    leaf; rejects an unknown extra key (`.strict()`); rejects a missing required field; and a
    `targetSavingsRate ∈ [0,1)` range block mirroring the `downPaymentPct` cases (reject `>= 1`,
    reject `< 0`, accept a mid value). Extend the `engineInput` factory tests to pass a `household`
    and assert it is present + frozen, and a case WITHOUT `household` asserting `household ===
    undefined`. Use plain string/object literals as untrusted input (cast to `unknown` for the
    reject cases) — do not import any value that bypasses the parse.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/engine/engine-input.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: all six behaviors above are asserted and pass.
    - Behavior: the `.strict()` extra-key case and the non-canonical-decStr case both throw
      (V5 / T-03-V5 forged-snapshot control).
    - Behavior: the no-household engineInput case yields `household === undefined`.
    - `npx vitest run packages/core/src/engine/engine-input.test.ts` passes.
  </acceptance_criteria>
  <done>The household trust boundary is proven by accept + reject cases mirroring the scenario
  boundary; the factory round-trips/freezes household when present and tolerates its absence.</done>
</task>

<task type="auto">
  <name>Task 4: Wave 0 — household type-test (extend engine-input.type-test.ts)</name>
  <files>packages/core/src/engine/engine-input.type-test.ts</files>
  <read_first>
    - packages/core/src/engine/engine-input.type-test.ts (the existing SCENARIO/EngineInput
      `@ts-expect-error` guards — extend, do not rewrite)
    - packages/core/src/tco/tco.type-test.ts (the no-bare-number guard idiom: `declare const`
      handles + `@ts-expect-error` asserting a string/Money field is not assignable from/to a bare
      number) — apply the analogous guard for the household decimal-string fields
  </read_first>
  <action>
    Extend the type-test (which stays in the `tsc -b` graph, NOT a `*.test.ts`) with a `declare
    const h: Household` handle and `@ts-expect-error` assertions that a household dollar field
    (e.g. `grossAnnualIncome`, `currentAnnualSavings`) cannot be SET from a bare `number` (it is a
    canonical decimal STRING), and that `EngineInput.household` is optional (assigning
    `undefined` is allowed). Mirror the existing scenario guards exactly in structure; add no
    runtime code.
  </action>
  <verify>
    <automated>npm run -w @house/core typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source: type-test references `Household` and asserts a dollar field is not bare-number
      assignable (CORE-02 no-bare-number discipline at the type level).
    - `npm run -w @house/core typecheck` (tsc -b) passes with the `@ts-expect-error` lines honored.
  </acceptance_criteria>
  <done>The household shape is type-guarded against bare-number dollars in the tsc -b graph,
  mirroring the scenario guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted JSON → `household` block | A persisted-or-forged snapshot becomes a trusted in-memory `Household` only via `parseHousehold` / `engineInput()` assembly. This is the single applicable ASVS-L1 surface for Phase 3 (V5 Input Validation), mirroring the existing scenario/assumptions boundary (T-07-01..03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `household` snapshot smuggling extra/forged fields | mitigate | `.strict()` `HouseholdSchema` rejects unknown keys; `decStr` rejects non-canonical/thousands/exponent strings; `parseHousehold` is the only entry, called at `engineInput()` assembly (mirrors T-07-03). Proven by Task 3 reject cases. |
| T-03-02 | Tampering / DoS | non-finite / out-of-range numbers poisoning later solves (e.g. `targetSavingsRate >= 1`, negative dollars) | mitigate | `decStr` (`CANONICAL_DECIMAL_RE`) rejects `NaN`/`Infinity`/exponent; `targetSavingsRate` `[0,1)` `.refine` boundary guard. The solver low-bound guard against `downPaymentCash`-driven `pct >= 1` lands with the solvers (Plans 02-03). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs ZERO new packages (RESEARCH §Package Legitimacy Audit: N/A — `zod`/`decimal.js`/`vitest` vetted + lockfile-pinned in Phases 1-2). No new supply-chain surface; slopcheck moot. |
</threat_model>

<verification>
- `npx vitest run packages/core/src/engine/engine-input.test.ts` — household accept + reject
  boundary cases (V5).
- `npm run -w @house/core typecheck` — household type-test (no bare-number dollars) in the tsc -b graph.
- `npx vitest run packages/core/src/golden.test.ts` — TCO golden unaffected (household optional).
- Per-wave merge: `npm run -w @house/core test` + `npm run -w @house/core typecheck`.
</verification>

<success_criteria>
- `Household` interface + `HouseholdSchema` (`.strict()`, decStr leaves, `targetSavingsRate ∈
  [0,1)`) + `parseHousehold` exist on `engine-input.ts` and include `currentAnnualSavings` (D-17).
- `household` is OPTIONAL on `EngineInput`/`engineInput()`, validated-when-present, and the
  existing TCO golden snapshot stays byte-identical (A3).
- The boundary rejects forged input (extra key, non-canonical decStr, out-of-range
  `targetSavingsRate`) — proven by tests (V5 / T-03-V5).
- Type-test proves no bare-number dollar field on household.
</success_criteria>

<output>
Create `.planning/phases/03-affordability-engine/03-01-SUMMARY.md` when done.
</output>
