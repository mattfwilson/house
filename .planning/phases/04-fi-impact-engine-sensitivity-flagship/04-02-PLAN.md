---
phase: 04-fi-impact-engine-sensitivity-flagship
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/core/src/fi/fi-target.ts
  - packages/core/src/fi/fi-target.test.ts
  - packages/core/src/fi/projection.ts
  - packages/core/src/fi/projection.test.ts
  - packages/core/src/fi/oracle.test.ts
autonomous: true
requirements: [FI-01, FI-02, FI-05, FI-06]
must_haves:
  truths:
    - "The renter FI target surfaces perpetual rent and the owner FI target surfaces perpetual tax+ins+maint, both as explicit Money result fields (D-02 fairness fulcrum, visible not buried)"
    - "Net worth projects month by month from current investable NW, compounding at the shared monthly real factor + adding the monthly contribution, returning the first month NW >= the path target — accumulation-only: the loop stops at the FI target/horizon cap with NO decumulation, withdrawal, or Monte-Carlo modeling (D-06)"
    - "A scenario that never reaches its target within the max-horizon cap returns a first-class discriminated unreached verdict (no Infinity, no -1 sentinel)"
    - "The iterative engine FI-date agrees EXACTLY with an independent closed-form FV-of-annuity oracle for the 0% case, and the high-inflation case routes through Fisher (toReal) — FI-05 is satisfied by this independent derivation serving as the oracle (not the engine validating itself), since no usable external retirement model exists (D-09)"
  artifacts:
    - path: "packages/core/src/fi/fi-target.ts"
      provides: "renterTarget/ownerTarget arithmetic (D-01/D-02), Money-returning"
      contains: "export function"
    - path: "packages/core/src/fi/projection.ts"
      provides: "projectFiDate monthly loop + FiOutcome discriminated union (D-03/D-07)"
      contains: "kind: 'unreached'"
    - path: "packages/core/src/fi/oracle.test.ts"
      provides: "independent closed-form FV-of-annuity oracle (D-10), 0% exact + high-inflation Fisher"
      contains: "toReal"
  key_links:
    - from: "packages/core/src/fi/projection.ts"
      to: "packages/core/src/tco/compounding.ts"
      via: "import { monthlyGrowthFactor }"
      pattern: "monthlyGrowthFactor"
    - from: "packages/core/src/fi/projection.ts"
      to: "packages/core/src/tco/rent-vs-buy.ts"
      via: "import { buyMonthlyOutflowAt }"
      pattern: "buyMonthlyOutflowAt"
    - from: "packages/core/src/fi/oracle.test.ts"
      to: "packages/core/src/tco/rent-vs-buy.ts"
      via: "import { toReal } for the high-inflation case"
      pattern: "toReal"
---

<objective>
Build the two load-bearing pure-calc pieces of the FI engine, gated by an independent oracle:

1. **`fi-target.ts` (D-01/D-02):** the asymmetric FI targets — renter target = `(spend + annualRent)/swr`, owner target = `(spend + annual(propertyTax + insurance + maintenance))/swr` — computed in `Dec`, surfaced as `Money`, with BOTH targets and their housing components exposed (the fairness fulcrum must be visible).
2. **`projection.ts` (D-03/D-07):** the monthly net-worth projection loop that compounds through the SHARED `monthlyGrowthFactor` (matching `rentVsBuy`'s contribute-then-compound order), detects the FI date (first month NW >= target), terminates at the stored max-horizon cap, and encodes "not reached" as a first-class discriminated `FiOutcome` variant (NOT a sentinel).
3. **`oracle.test.ts` (D-10 / FI-05):** an INDEPENDENT closed-form FV-of-annuity solve-for-n, asserting EXACT agreement with the engine for the 0%-return linear anchor, and a high-inflation case that routes a NOMINAL rate + inflation through `toReal` (D-11).

This is the flagship-correctness plan. Build it TDD: the oracle + the 0% convention anchor are written FIRST and gate the loop (Pitfall 1 — the intra-month convention must be locked by a passing 0% test before the compounding case is trusted).

Purpose: Lock the asymmetric targets and the termination-guaranteed loop against an independent check. Plans 03 (orchestrator/ranking) and 04 (tornado/golden) compose these without re-deriving any math.
Output: `fi-target.ts`, `projection.ts`, and their tests + the oracle, all green under `npx vitest run packages/core/src/fi`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-CONTEXT.md
@.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-RESEARCH.md
@.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-PATTERNS.md
@.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `fi/fi-target.ts` — NEW: `renterFiTarget(input): Money`, `ownerFiTarget(input, tco, fiYear): Money` (or a single `fiTargets(input)` returning `{ renterTarget, ownerTarget, renterHousingAnnual, ownerHousingAnnual }` — all `Money`). The closed `FiTargets` result type.
- `fi/projection.ts` — NEW: `type FiOutcome = { kind: 'reached'; month: number; years: number } | { kind: 'unreached'; cappedAtMonth: number }`; `projectFiDate(seed, contributionFor, target, input): FiOutcome` (signature locked in Task 2). The buy-path NW composition helper (liquid + liquidated equity, A5).
- `fi/oracle.test.ts`, `fi/fi-target.test.ts`, `fi/projection.test.ts` — NEW test files.
</artifacts_this_phase_produces>

<locked_discretionary_decisions>
Per the planner directive, these Claude's-discretion items are LOCKED here and cited so the Decision Coverage Gate sees them:
- **A5 (buy-path NW composition):** LOCKED to **liquid investments + liquidated home equity** (consistent with `rentVsBuy`'s `buyEndingNetWorth = liquidatedEquity + buyPortfolio`, rent-vs-buy.ts lines 261/279). Documented in `projection.ts`.
- **A1 (owner-target housing basis):** LOCKED to **year-0 (today's) value** for the owner perpetual tax+ins+maint basis — the simpler all-real treatment that avoids the target↔FI-year fixed point (RESEARCH Open Q1 / L7; `ownerFiTarget` takes `fiYear` but Plan callers pass `0`). Documented inline; revisitable to the 2-pass appreciated-at-FI-year basis later without an API change.
- **A6 (monthlyGrowthFactor promotion):** consumed here (imported from `tco/compounding.ts`, promoted in Plan 01).
</locked_discretionary_decisions>

<tasks>

<task type="tdd">
  <name>Task 1: RED→GREEN the independent FV-of-annuity oracle + 0% convention anchor (D-10 / FI-05)</name>
  <files>packages/core/src/fi/oracle.test.ts, packages/core/src/fi/projection.ts</files>
  <behavior>
    - **0%-return linear anchor (exact):** for seed S, monthly contribution C>0, target T, `monthlyGrowthFactor("0")` yields factor 1; the engine FI month MUST equal `ceil((T − S)/C)` EXACTLY (`===` on whole months, NO toBeCloseTo). This locks the contribute-then-compound convention (Pitfall 1).
    - **Compounding case (exact):** for a real annual rate r>0, the engine FI month MUST equal the closed-form `n = ceil( ln(A/B) / ln(f) )` with `f=(1+r)^(1/12)`, `A=T(f−1)+Cf`, `B=S(f−1)+Cf` (RESEARCH §1). Assert exact month agreement (the convention is shared, so the gap is 0).
    - **High-inflation case (D-11 / L2):** supply a NOMINAL return + high inflation; compute `r_real = toReal(nominal, inflation)` (imported from rent-vs-buy.ts); feed `r_real` to BOTH the oracle and the engine; assert the FI date the REAL rate produces. The test documents that its whole purpose is exercising Fisher — a version with no `toReal` call would be vacuous.
    - **Unreachable agreement:** when C <= 0 (premium swallows savings), the oracle returns its unreachable signal and the engine returns `{ kind: 'unreached' }` — assert they agree on unreachability.
  </behavior>
  <read_first>
    - 04-RESEARCH.md §1 lines 351-356 and the "Closed-form FV-of-annuity oracle" code example lines 293-319 (the EXACT formula + edge handling)
    - packages/core/src/tco/rent-vs-buy.ts lines 75-77 (`toReal` signature) and lines 228-247 (the contribute-then-compound order the oracle MUST match)
    - packages/core/src/tco/compounding.ts (the shared factor, from Plan 01)
    - packages/core/src/golden.test.ts lines 28-44 (test imports/structure idiom: Vitest `describe`/`test`/`expect`, node env)
    - 04-PATTERNS.md §"oracle.test.ts" lines 197-210
  </read_first>
  <action>
    Write `oracle.test.ts` FIRST (RED). Implement an in-test `oracleFiMonths(seed, contribution, target, realAnnual): number` exactly per RESEARCH §1 — closed-form via `Dec.ln` (verified available), with the `f.equals(1)` 0%-linear branch (`ceil((T−S)/C)`) and the `C <= 0` / `A/B <= 0` unreachable branch returning `Infinity`. Import `Dec` from `../money/decimal-config.js` and `toReal` from `../tco/rent-vs-buy.js`. The oracle is an INDEPENDENT analytic identity living in the test — NOT an iterative copy of the loop (that would not be independent, D-10).
    Then create `projection.ts` with the minimal `projectFiDate` (full signature in Task 2) just enough to make the 0% anchor pass GREEN: the contribute-then-compound loop `nw = nw.plus(contribution); nw = nw.times(factor);` then test `nw.gte(target)`, returning `{ kind: 'reached', month, years }` or (at cap) `{ kind: 'unreached', cappedAtMonth }`. Use the SHARED `monthlyGrowthFactor` from `tco/compounding.js`. Read `maxHorizonYears` from `input.assumptions.projection.maxHorizonYears` via `Number()` at the loop bound only.
    Commit RED (`test(04-02): failing oracle + 0% convention anchor`), then GREEN (`feat(04-02): projectFiDate matching the oracle convention`). The 0% case asserts EXACT `===`; treat any >0-month gap in the compounding case as a real bug (match conventions), not a tolerance.
  </action>
  <verify>
    <automated>cd packages/core && npx vitest run src/fi/oracle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `oracle.test.ts` contains a `toReal` call in the high-inflation case (`grep -c 'toReal' packages/core/src/fi/oracle.test.ts` >= 1) — the Fisher path is exercised (L2).
    - The 0% test asserts engine FI month `===` `ceil((T−S)/C)` (exact, no `toBeCloseTo`); `grep -c 'toBeCloseTo' packages/core/src/fi/oracle.test.ts` returns 0.
    - The compounding case asserts exact `===` agreement between engine and `oracleFiMonths`.
    - The unreachable case asserts the engine returns a `{ kind: 'unreached' }` object (not a number) and the oracle returns `Infinity`, and they agree on unreachability.
    - `npx vitest run src/fi/oracle.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: RED→GREEN projectFiDate — termination, unreached verdict, buy-path NW composition (D-03/D-07, A5)</name>
  <files>packages/core/src/fi/projection.ts, packages/core/src/fi/projection.test.ts</files>
  <behavior>
    - **Reached:** a reachable path returns `{ kind: 'reached', month, years }` where `years` is the decimal-string `month/12` (computed in `Dec`), `month` the first month NW >= target.
    - **Unreached (D-07):** a path whose contribution drives NW below target for the whole horizon returns `{ kind: 'unreached', cappedAtMonth }` with `cappedAtMonth === maxHorizonYears × 12` — NO Infinity, NO -1, NO unbounded loop.
    - **Buy-path NW = liquid + liquidated equity (A5):** the buy-path NW each month includes the liquidated home equity = `(homeValueAt(price, appr, year) − scheduleBalance) × (1 − sellCostPct)`, with the schedule index-clamp for holds past the amortization term (verbatim from rent-vs-buy.ts 253-261). A liquid-only projection (no equity) reaches LATER than the liquid+equity one — assert the equity inclusion moves the FI date earlier.
    - **Determinism:** no `Date.now`/`Math.random`; the loop bound + all rates come from `EngineInput`.
  </behavior>
  <read_first>
    - packages/core/src/tco/rent-vs-buy.ts lines 228-264 (the loop order + the year-boundary equity composition + the schedule index-clamp at 257) and lines 170-220 (how `rentVsBuy` reads scenario/assumptions, builds the schedule, seeds portfolios)
    - packages/core/src/tco/carrying-costs.ts lines 36-38 (`homeValueAt`) ; packages/core/src/tco/amortization.ts (`amortizationSchedule` row `.balance` shape)
    - packages/core/src/tco/rent-vs-buy.ts lines 127-160 (`buyMonthlyOutflowAt` — the monthly premium source for the buy path's reduced contribution, D-04)
    - 04-PATTERNS.md §"projection.ts" lines 81-127 (the locked convention, forced-equity reuse, FiOutcome encoding, L4 do-not-double-convert)
    - 04-RESEARCH.md §2 lines 358-361 and §4 lines 368-371
  </read_first>
  <action>
    Finalize `projection.ts`. Export `type FiOutcome = { readonly kind: 'reached'; readonly month: number; readonly years: string } | { readonly kind: 'unreached'; readonly cappedAtMonth: number }` (years as a decimal STRING — months/12 in Dec; L3: no non-finite numbers, discriminated `kind`).
    Lock the signature: `projectFiDate(opts: { seedDollars: string; target: Money; contributionFor: (month: number) => InstanceType<typeof Dec>; factor: InstanceType<typeof Dec>; equityFor?: (month: number) => InstanceType<typeof Dec>; maxHorizonMonths: number }): FiOutcome`. The loop: `nw = nw.plus(contributionFor(month)); nw = nw.times(factor);` then if `equityFor` is provided add `equityFor(month)` to the comparison NW (buy path, A5), then test `>= target`. The `seedDollars` for the buy path is already net of DP+closing (computed by the caller in Plan 03); for the renter path the seed is current NW and `equityFor` is omitted.
    A5 LOCKED: document inline that the buy path's NW toward its target = liquid side-portfolio + liquidated home equity (matching `rentVsBuy` lines 261/279); cite the rationale (the renter has no house to sell, so comparing total liquidatable NW is apples-to-apples). Reuse the equity composition + index-clamp from rent-vs-buy.ts 253-261 — do NOT re-derive `(1+appr)^year` or a second amortization.
    L4: feed `input.assumptions.returns.realAnnual` DIRECTLY into `monthlyGrowthFactor` — it is ALREADY real; NEVER pass it through `toReal`.
    Write `projection.test.ts` covering reached, unreached (cap), and the liquid-vs-liquid+equity earlier-FI assertion. Build inputs via `engineInput({ asOf: calendarDate(...), assumptions: DEFAULT_ASSUMPTIONS, scenario, household })`.
    Commit RED then GREEN.
  </action>
  <verify>
    <automated>cd packages/core && npx tsc -b && npx vitest run src/fi/projection.test.ts src/fi/oracle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `FiOutcome` is a discriminated union with `kind: 'reached' | 'unreached'`; `grep -c "kind: 'unreached'" packages/core/src/fi/projection.ts` >= 1; no `Infinity` or `-1` sentinel anywhere in `projection.ts` (`grep -c 'Infinity' packages/core/src/fi/projection.ts` returns 0).
    - The loop bound is `Number(input.assumptions.projection.maxHorizonYears) * 12` (read from V3 data, not hardcoded); `grep -c 'maxHorizonYears' packages/core/src/fi/projection.ts` >= 1; no `while (true)` (`grep -c 'while' packages/core/src/fi/projection.ts` returns 0).
    - `returns.realAnnual` is NOT passed through `toReal` in `projection.ts` (`grep -c 'toReal' packages/core/src/fi/projection.ts` returns 0 — L4).
    - The test proves an unreachable path returns `{ kind: 'unreached', cappedAtMonth: <maxHorizonYears*12> }` and that liquid+equity reaches no later than liquid-only.
    - `tsc -b` + the two test files exit 0.
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 3: RED→GREEN the asymmetric FI targets (D-01/D-02, A1 year-0 basis)</name>
  <files>packages/core/src/fi/fi-target.ts, packages/core/src/fi/fi-target.test.ts</files>
  <behavior>
    - `renterFiTarget = (targetAnnualRetirementSpend + annualRent) / swr.rate` (annualRent = household.currentRent × 12, today's dollars).
    - `ownerFiTarget = (targetAnnualRetirementSpend + (annualPropertyTax + insurance + maintenance)) / swr.rate`, the owner housing evaluated at the **year-0 basis** (A1 LOCKED) — `assessedValueAt(price, ratio, appr, 0)`, `homeValueAt(price, appr, 0)`, flat insurance.
    - Division lives in `Dec` (Money has no `div`); the result crosses to `Money` once via `Money.of(d.toFixed())`.
    - The result surfaces BOTH targets AND both housing components: `{ renterTarget, ownerTarget, renterHousingAnnual, ownerHousingAnnual }` — all `Money` (D-02 visibility; the fairness fulcrum is never buried).
    - With a higher SWR (e.g. 0.04 vs 0.033) the targets are LOWER (smaller denominator divides into a fixed numerator) — sanity assertion that the SWR knob is live (so the tornado can sweep it).
  </behavior>
  <read_first>
    - packages/core/src/affordability/evaluate-scenario.ts lines 55-60 (the `savingsRateAt` Dec-then-Money division idiom to mirror) — read the whole file's header for the Dec/Money discipline comment
    - packages/core/src/tco/property-tax.ts (`annualPropertyTax`, `assessedValueAt` signatures) and packages/core/src/tco/carrying-costs.ts lines 27-38 (`maintenanceAnnual`, `homeValueAt`)
    - packages/core/src/tco/tco.ts lines 64-88 (`TcoBreakdown` — `resolvedMillRate` at 88, `insurance` line at 67, `pmiApplies` at 80) — the owner target reuses `tco.resolvedMillRate` + `tco.insurance.annualized`
    - 04-RESEARCH.md §3 lines 363-366 and the "Asymmetric FI targets" code example lines 272-291
    - 04-PATTERNS.md §"fi-target.ts" lines 40-77
  </read_first>
  <action>
    Export `FiTargets` (closed `readonly` interface, all `Money`: `renterTarget`, `ownerTarget`, `renterHousingAnnual`, `ownerHousingAnnual`) and `fiTargets(input: EngineInput, tco: TcoBreakdown): FiTargets`. Require `input.household` (throw a clear error if absent — mirror `affordabilityGap`'s guard at gap.ts 80-86, since `targetAnnualRetirementSpend` lives on household).
    Build the renter housing = `Money.of(household.currentRent).mul('12')`. Build the owner housing (A1 year-0 LOCKED) = `annualPropertyTax(assessedValueAt(price, assumptions.tax.assessmentRatio, appreciation.realAnnual, 0), tco.resolvedMillRate).add(tco.insurance.annualized).add(maintenanceAnnual(homeValueAt(price, appreciation.realAnnual, 0), maintenance.annualPctOfValue))`.
    Build the target via the Dec-then-Money idiom: `numerator = spend.add(housing)` (Money); lift `new Dec(numerator.toDecimalString())`, `.div(new Dec(swr.rate))`, return `Money.of(d.toFixed())`. Money has NO `div` — divide in Dec (do NOT widen the Money API; the Phase 2/3 precedent).
    Document A1 inline with the `[ASSUMED] … year-0 basis (D-02); appreciated-at-FI-year basis deferred to avoid the target↔FI-year fixed point (RESEARCH L7)` comment style.
    Write `fi-target.test.ts`: assert renter vs owner asymmetry (owner target reflects tax+ins+maint, renter reflects rent), both housing components surfaced, and the SWR-knob sanity (higher SWR → lower target). Commit RED then GREEN.
  </action>
  <verify>
    <automated>cd packages/core && npx tsc -b && npx vitest run src/fi/fi-target.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `FiTargets` exposes all four `Money` fields (`renterTarget`, `ownerTarget`, `renterHousingAnnual`, `ownerHousingAnnual`); the test asserts each is present and non-buried (D-02).
    - Division happens in `Dec`, not via a (nonexistent) `Money.div` (`grep -c '\.div(' packages/core/src/fi/fi-target.ts` >= 1 referencing `Dec`; no `Money` `div` call).
    - The owner housing uses year-0 basis (`assessedValueAt(...,0)` / `homeValueAt(...,0)` — A1 LOCKED, documented).
    - The test proves the SWR knob is live (higher swr → lower target).
    - `tsc -b` + `fi-target.test.ts` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EngineInput → FI calc | The frozen, already-validated snapshot is the only input; no ambient state crosses into the projection |
| FI result → serialization | `FiOutcome` + `FiTargets` will feed `canonicalJson` (Plan 04) — must contain no non-finite numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `FiOutcome` unreached encoding | mitigate | Discriminated `{ kind: 'unreached'; cappedAtMonth }` variant, never an `Infinity`/`-1` sentinel (L3) — `canonicalJson` throws on non-finite, so the discriminated variant is the only serializable encoding. Asserted by grep gate (no `Infinity` in projection.ts). |
| T-04-05 | Tampering | float re-entering the money path | mitigate | All dollars `Money`, all FI/target/projection math in `Dec`; `Money.of(d.toFixed())` at the boundary; `Dec` unexported. The fi.type-test.ts guard (Plan 04) pins it at the type level. |
| T-04-06 | Repudiation | non-determinism breaking reproducibility | mitigate | No `Date.now`/`Math.random` (the core determinism guard throws); the max-horizon cap, all rates, and the spend field are explicit data on `EngineInput`. |
| T-04-07 | Tampering | double-converting an already-real rate | mitigate | `returns.realAnnual` consumed directly into the shared `monthlyGrowthFactor`; `toReal` appears ONLY in the oracle's high-inflation case (L4), asserted by grep gate. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan; RESEARCH Package Legitimacy Audit confirms zero new dependencies. |
</threat_model>

<verification>
- `cd packages/core && npx vitest run packages/core/src/fi` green (oracle + projection + fi-target).
- `npx tsc -b` exits 0.
- Oracle agrees EXACTLY with the engine for 0% and the compounding case; the high-inflation case routes through `toReal` (FI-05 / D-11).
- Unreached verdict is a discriminated variant with no non-finite numbers (D-07 / L3).
</verification>

<success_criteria>
- Asymmetric FI targets computed + surfaced (D-01/D-02) — FI-01/FI-02 foundation.
- Monthly projection loop terminates at the stored cap and detects the FI date (FI-02).
- Unreached verdict is first-class (D-07) — the "don't buy" signal (FI-06).
- Independent oracle reconciles 0% (exact) + high-inflation-via-Fisher (FI-05).
</success_criteria>

<output>
Create `.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-02-SUMMARY.md` when done.
</output>
