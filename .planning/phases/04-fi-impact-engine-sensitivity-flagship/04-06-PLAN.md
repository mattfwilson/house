---
phase: 04-fi-impact-engine-sensitivity-flagship
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [FI-05]
files_modified:
  - packages/core/src/fi/fi-impact.ts
  - packages/core/src/fi/fi-impact.test.ts

must_haves:
  truths:
    - "The buy-path liquidated-equity year index and rentVsBuy's equity-snapshot year index are reconciled to a single, deliberate convention (no silent divergence in a money engine)."
    - "The in-file comments in fi-impact.ts no longer falsely claim the equity math is 'verbatim' from rent-vs-buy.ts — they state the actual, intentional convention."
    - "A test pins the buy path's intended month-12 (year-boundary) home value so the two engines cannot be silently 're-synced' later in a way that shifts FI dates."
    - "The FI golden fixture still recomputes cent-identically (the reconciliation is either a no-op for the golden's reached months, or a deliberate, gated regeneration with a reviewed diff)."
  artifacts:
    - path: "packages/core/src/fi/fi-impact.ts"
      provides: "equityFor with a reconciled, documented year-index convention + corrected comments"
      contains: "equityFor"
    - path: "packages/core/src/fi/fi-impact.test.ts"
      provides: "a month-12 home-value pin proving the chosen equity-year convention"
  key_links:
    - from: "packages/core/src/fi/fi-impact.ts"
      to: "packages/core/src/tco/rent-vs-buy.ts"
      via: "equityFor year-index convention reconciled against the rentVsBuy equity snapshot (rent-vs-buy.ts:242-245)"
      pattern: "homeValueAt"
---

<objective>
Close GAP 3 (WARNING / WR-01): `fi-impact.ts` `equityFor` (lines 153-159) computes the buy-path liquidated-equity HOME-VALUE year index as `Math.floor((month - 1) / 12)` (so month 12 → year 0, no appreciation), while `rentVsBuy`'s equity snapshot (rent-vs-buy.ts:242-245) uses `year = month / 12` (so month 12 → year 1, one year of appreciation). The in-file comments at fi-impact.ts:118-119 and 151-152 falsely claim the logic is "verbatim from rent-vs-buy.ts 246-253." This is a real correctness/maintenance trap in a money engine: the two instruments value the same hold-month-12 home differently, and a future maintainer "reconciling" them per the false comment could silently shift FI dates.

Fix: pick the correct, consistent convention; make the two genuinely AGREE (or DELIBERATELY differ with that difference documented and test-pinned); correct the misleading comments. The schedule-BALANCE index (`month - 1`) already agrees between the two and must stay — only the appreciation YEAR differs.

Purpose: remove a silent modeling inconsistency between the FI-date instrument and the rent-vs-buy instrument, and make the chosen convention explicit and locked by a test.
Output: a reconciled `equityFor`, honest comments, a pinning test, and a green suite with the FI golden either unchanged or deliberately regenerated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-REVIEW.md
@.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-04-SUMMARY.md

# The two instruments to reconcile (exact lines already in context):
@packages/core/src/fi/fi-impact.ts
@packages/core/src/tco/rent-vs-buy.ts
@packages/core/src/tco/carrying-costs.ts
@packages/core/src/__fixtures__/fi-golden-snapshot.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile the equityFor year index and correct the false "verbatim" comments (WR-01)</name>
  <files>packages/core/src/fi/fi-impact.ts, packages/core/src/fi/fi-impact.test.ts</files>
  <read_first>
    - packages/core/src/fi/fi-impact.ts — `equityFor` (lines 153-159): `const year = Math.floor((month - 1) / 12)` for `homeValueAt`, and `schedule.rows[month - 1]` for the balance with an index clamp (`month - 1 < schedule.rows.length ? ... : undefined → balance 0`). The comments at 118-119 and 151-152 claim "verbatim from rent-vs-buy.ts 246-253."
    - packages/core/src/tco/rent-vs-buy.ts:242-253 — the equity snapshot runs ONLY at `month % 12 === 0`, uses `year = month / 12` for `homeValueAt(price, appr, year)`, and `schedule.rows[month - 1]` for the balance (SAME balance index as fi-impact — that part genuinely agrees). At month 12: year 1 (appreciated), balance row index 11.
    - packages/core/src/04-REVIEW.md WR-01 + IN-04 — the divergence detail and the two acceptable fixes: (a) align the year derivation with rentVsBuy, OR (b) keep the monthly-cadence valuation but correct the comments AND pin a test. Also IN-02 (projection.ts month-0 seed calls equityFor(0) → year = floor(-1/12) = -1) is RELATED: whatever convention you pick, ensure month 0 does not value the home at a NEGATIVE year. Clamp the year to >= 0 (`Math.max(0, ...)`) regardless of which option you choose — this also closes IN-02 cheaply since it is in the same function you are already editing.
    - packages/core/src/__fixtures__/fi-golden-snapshot.json — the committed FI golden: buy reached month 175, baseline 217, fiDeltaMonths -42. If your chosen convention changes the buy month for THIS input, the golden MUST be regenerated via the gated path (see action).
    - packages/core/src/golden.test.ts — the gated `UPDATE_GOLDEN=1` FI golden path (the ONLY sanctioned way to rewrite fi-golden-snapshot.json; never toMatchSnapshot).
  </read_first>
  <behavior>
    - fi-impact.test.ts: pin the buy path's home value at the year boundary. Pick the convention (recommended: option (a) — align with rentVsBuy so month 12 values the home at YEAR 1, the same as the rent-vs-buy instrument the comments claim equivalence to — this makes the two genuinely agree, which is the stronger fix for "make them share/agree"). Assert that for a known scenario, `equityFor(12)` uses `homeValueAt(price, appr, 1)` (year 1), matching the rent-vs-buy snapshot at month 12. If instead you keep the monthly cadence (option b), assert `equityFor(12)` uses year 0 AND add a comment-locked test asserting the DELIBERATE divergence.
    - fi-impact.test.ts: assert `equityFor(0)` does NOT use a negative year (clamp to year 0) — closes IN-02.
    - fi-impact.test.ts: the schedule-balance index stays `month - 1` (unchanged) — assert the balance at month 12 equals `schedule.rows[11].balance` (the part that already agreed must not regress).
  </behavior>
  <action>
    Choose the reconciliation. RECOMMENDED: option (a) — make `equityFor`'s home-value year match rentVsBuy's snapshot basis so the two instruments genuinely AGREE at year boundaries (the prompt asks to "make them genuinely share/agree"). Concretely: derive the year so that month 12 → year 1, month 24 → year 2, etc., consistent with rentVsBuy's `month / 12` at the boundary, while keeping a sensible monthly value between boundaries. The simplest reconciliation that agrees at every year boundary and never goes negative: `const year = Math.max(0, Math.ceil(month / 12) - 1)`? — NO: verify the exact basis against rentVsBuy first. rentVsBuy only valuates AT boundaries with `year = month/12`. For a monthly projection you must choose a per-month year basis that (i) equals `month/12` whenever `month % 12 === 0`, and (ii) is >= 0 at month 0. Use `const year = Math.max(0, Math.floor(month / 12));` — at month 12 this gives year 1 (agrees with rentVsBuy), at months 1-11 year 0, at month 0 year 0 (IN-02 closed), at month 13-23 year 1. Confirm this against rentVsBuy by reading the snapshot line; if a different basis is needed to truly agree at boundaries, use that and document the derivation. Keep the schedule-balance index `month - 1` with the existing clamp EXACTLY (it already agrees — do not touch it).

    Correct the comments: replace the "verbatim from rent-vs-buy.ts 246-253" claims at fi-impact.ts:118-119 and 151-152 with an accurate statement — e.g. "the home-value year basis is reconciled with rentVsBuy's year-boundary snapshot (rent-vs-buy.ts:242-245): at each year boundary the two value the home at the SAME appreciated year; the schedule-balance index (month-1) is shared. This is NOT a blind copy — the FI path values monthly, agreeing with rentVsBuy at boundaries." Remove any "the SAME"/"verbatim" overclaim (IN-04).

    Run the FI golden test. If the chosen convention CHANGES the buy month for the golden's fixed input (the prior month-12-year-0 basis biased equity slightly low → a corrected higher-equity basis may move the reached buy month EARLIER), regenerate the golden via the GATED path ONLY: `UPDATE_GOLDEN=1 npx vitest run src/golden.test.ts`, then review the diff in fi-golden-snapshot.json (it is a committed reproducibility artifact — the diff MUST be a small, explainable shift in buy.month / fiDeltaMonths, NOT a structural change). Call out the regeneration explicitly in the SUMMARY with the before/after months and the one-line reason. If the buy month is UNCHANGED (the golden's reached months may not straddle a year boundary in a way that moves the discrete month), no regen is needed — state that.

    Keep the calc core PURE (decimal.js only; equity in `Dec`; dollars as `Money`/strings; no React/Next/DOM). Write the test pins FIRST (RED for the corrected year basis), then implement (GREEN).
  </action>
  <verify>
    <automated>cd packages/core && npx tsc -b && npx vitest run src/fi/fi-impact.test.ts src/golden.test.ts src/fi</automated>
  </verify>
  <acceptance_criteria>
    - `equityFor`'s home-value year is reconciled with rentVsBuy (agree at year boundaries) OR deliberately divergent-and-documented; the schedule-balance index `month - 1` is unchanged.
    - `equityFor(0)` never uses a negative year (IN-02 closed).
    - The false "verbatim"/"the SAME" comments are corrected to the actual convention.
    - A test pins the month-12 home-value year so the two engines cannot be silently re-synced; tsc -b + the fi suite green.
    - The FI golden is either unchanged OR regenerated via the gated UPDATE_GOLDEN path with a reviewed, explained diff (called out in the SUMMARY).
  </acceptance_criteria>
  <done>The buy-path equity year basis genuinely agrees with rentVsBuy, the comments are honest, the convention is test-pinned, and the FI golden is intact (or deliberately + transparently regenerated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed golden fixture → recompute | the FI golden is a reproducibility artifact; any change to equity math may alter it and MUST go through the gated, reviewed regeneration path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-G4 | Tampering | silent re-sync of the two equity conventions later shifts FI dates | mitigate | pin the month-12 home-value year with a test so a future blind "reconcile" fails CI |
| T-04-G5 | Repudiation | undocumented golden regeneration hides a math change | mitigate | regenerate ONLY via gated UPDATE_GOLDEN with a reviewed git diff + an explicit SUMMARY call-out of before/after months |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed in this plan |
</threat_model>

<verification>
- `cd packages/core && npx tsc -b` → exit 0.
- `cd packages/core && npm test` → all green. The FI golden is byte-identical OR regenerated via the gated path with a reviewed diff (no other golden touched — this plan does not alter assumptions).
- `grep -n "verbatim" packages/core/src/fi/fi-impact.ts | grep -v "^#"` → 0 false "verbatim" equity claims remain (or only an accurate, scoped use).
- A fi-impact.test.ts assertion pins the chosen month-12 equity-year convention.
</verification>

<success_criteria>
- GAP 3 closed: the FI buy-path equity year index and rentVsBuy's are reconciled (agree at year boundaries) with honest comments and a locking test.
- IN-02 closed cheaply (no negative year at month 0) since it is in the same function.
- The FI golden is intact or deliberately + transparently regenerated; suite + tsc -b green.
</success_criteria>

<output>
Create `.planning/phases/04-fi-impact-engine-sensitivity-flagship/04-06-SUMMARY.md` when done.
</output>
