---
phase: 07-web-shell
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/fi/fi-trajectory.ts
  - packages/core/src/fi/fi-trajectory.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [SC-2]

must_haves:
  truths:
    - "fiTrajectory returns a month-by-month net-worth series for the buy path AND the rent-baseline path over one EngineInput"
    - "fiTrajectory's buyFiMonth/rentFiMonth agree exactly with projectFiDate on the same input (reconciliation by construction)"
    - "Every dollar in the result is Money (no bare number); the series is canonicalJson-safe (finite)"
  artifacts:
    - path: "packages/core/src/fi/fi-trajectory.ts"
      provides: "pure fiTrajectory entry point + FiTrajectoryResult type, reusing the projection loop + fi-impact path bundles"
      contains: "export function fiTrajectory"
    - path: "packages/core/src/fi/fi-trajectory.test.ts"
      provides: "reconciliation + finiteness + Money-only unit tests"
      contains: "fiTrajectory"
    - path: "packages/core/src/index.ts"
      provides: "barrel export of fiTrajectory + FiTrajectoryResult"
      contains: "fiTrajectory"
  key_links:
    - from: "packages/core/src/fi/fi-trajectory.ts"
      to: "packages/core/src/fi/projection.ts"
      via: "reuse the contribute-then-compound loop (no re-derived trajectory math)"
      pattern: "monthlyGrowthFactor|comparisonNw|contributeThenCompound"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/fi/fi-trajectory.js"
      via: "export { fiTrajectory, type FiTrajectoryResult }"
      pattern: "fiTrajectory"
---

<objective>
Add a pure `fiTrajectory` entry point to `@house/core` that emits the month-by-month net-worth series for both the buy path and the keep-renting baseline (the D-07 hero trajectory chart needs a series the core does not currently expose — RESEARCH Open Q1, user-confirmed resolution). It MUST reuse the existing contribute-then-compound projection machinery and the existing buy/renter path bundles, never re-derive trajectory math, so the emitted series and the FI-date crossing agree by construction.

Purpose: D-07 (approved, load-bearing) requires a net-worth-over-time line overlaying this scenario against the no-purchase baseline with FI-threshold + crossover markers. `projectFiDate` already computes month-by-month NW for both paths but discards every point except the crossing. Surfacing that discarded series in the core keeps the trajectory math where CORE-01/02 requires it (NOT hand-rolled in the web layer). This is locked: ADD a pure core entry point, do not descope, do not hand-roll.
Output: `fiTrajectory` + `FiTrajectoryResult`, barrel-exported, with a reconciliation golden/unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-PATTERNS.md
@packages/core/src/fi/projection.ts
@packages/core/src/fi/fi-impact.ts
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — reconciliation + finiteness tests for fiTrajectory</name>
  <read_first>
    - packages/core/src/fi/projection.test.ts (oracle + 0%-return linear anchor test style)
    - packages/core/src/fi/fi-impact.ts (buyPath/renterBaselinePath bundles lines 159-228; fiImpact composition lines 237-279; maxHorizonMonths from input.assumptions.projection.maxHorizonYears line 260)
    - packages/core/src/fi/projection.ts (FiOutcome + the loop lines 78-116)
  </read_first>
  <behavior>
    - Test 1: For a reached fixture (reuse the fi-impact golden input — household + scenario), `fiTrajectory(input).buyFiMonth === projectFiDate(buyPath).month` and `rentFiMonth === projectFiDate(renterBaselinePath).month` (the series and the scalar FI date agree by construction).
    - Test 2: `points[last].month === maxHorizonMonths` (or the sample stride's last sampled month) and `points.length >= 1`; every `buyNetWorth`/`rentNetWorth` is a Money whose `toDecimalString()` parses finite (canonicalJson(result) does not throw).
    - Test 3: `fiThreshold` equals the owner FI target Money (the D-07 threshold line); an unreachable strained input (e.g. the fi-impact don't-buy fixture) yields `buyFiMonth === null` while the series still emits to the cap.
  </behavior>
  <action>Create `packages/core/src/fi/fi-trajectory.test.ts` asserting the three behaviors above against an in-test `engineInput(...)` built through `parseHousehold`/`parseScenarioInputs` (no bare cast). Use `canonicalJson` from the barrel to prove finiteness. Tests MUST fail RED (fi-trajectory.ts does not exist yet). Commit `test(07-02): add failing fiTrajectory reconciliation tests`.</action>
  <verify>
    <automated>npx vitest run packages/core -t "fiTrajectory" 2>&1 | grep -qi "fail\|cannot find\|no test" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>fi-trajectory.test.ts exists and the suite is RED (import/module-not-found or assertion failure) because fiTrajectory is not yet implemented.</acceptance_criteria>
  <done>Failing tests committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement fiTrajectory by reusing the projection loop + path bundles</name>
  <read_first>
    - packages/core/src/fi/projection.ts (the locked intra-month order lines 89-99; comparisonNw lines 104-111; ProjectFiDateOptions lines 52-70)
    - packages/core/src/fi/fi-impact.ts (buyPath lines 159-204; renterBaselinePath lines 213-228; fiTargets + factor wiring lines 248-260; Money.of(dec.toFixed()) discipline lines 37-39, 271-276)
    - packages/core/src/money/money.ts (Money.of + toDecimalString/toString exits)
  </read_first>
  <action>Create `packages/core/src/fi/fi-trajectory.ts` exporting `fiTrajectory(input: EngineInput): FiTrajectoryResult` and the closed `FiTrajectoryResult` interface `{ readonly points: readonly { month: number; buyNetWorth: Money; rentNetWorth: Money }[]; readonly fiThreshold: Money; readonly buyFiMonth: number | null; readonly rentFiMonth: number | null }`. Build the SAME two PathBundles `fiImpact` builds — reuse `buyPath`/`renterBaselinePath` (export them from fi-impact.ts if not already, or factor the shared builders into a helper imported by both; do NOT duplicate the seed/premium/equity math). Run a single series-emitting loop that keeps the LOCKED contribute-then-compound order (`nw = nw.plus(contributionFor(month)); nw = nw.times(factor)`) and emits one point per month using the SAME `comparisonNw` (liquid + liquidated equity for buy via `equityFor`; liquid-only for rent) so the emitted series and the FI crossing agree. Record `buyFiMonth`/`rentFiMonth` as the first month each comparison NW >= its target (null if never within `maxHorizonMonths`). `fiThreshold` = the owner FI target (`fiTargets(input, computeTco(input)).ownerTarget`). All compounding in the frozen `Dec` clone; dollars cross OUT as `Money` via `Money.of(dec.toFixed())`; `Dec` is NOT re-exported. Determinism: cap from `input.assumptions.projection.maxHorizonYears` (Number() only at the loop bound), no Date.now/Math.random. Year-sampling is acceptable (every 12th month) but keep the FI-crossover months EXACT regardless of stride. Commit `feat(07-02): implement pure fiTrajectory series entry`.</action>
  <verify>
    <automated>npx vitest run packages/core -t "fiTrajectory"</automated>
  </verify>
  <acceptance_criteria>`npx vitest run packages/core -t "fiTrajectory"` exits 0 (all three behaviors green); fi-trajectory.ts contains `export function fiTrajectory`; no `Date.now`/`Math.random` in the file; the series reuses the projection loop convention (no second amortization or `(1+appr)^year` re-derivation in this file beyond the reused `buyEquityAt`/path bundles).</acceptance_criteria>
  <done>fiTrajectory implemented and green; reconciliation with projectFiDate proven.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel export + full-suite regression</name>
  <read_first>
    - packages/core/src/index.ts (the FI engine block lines 107-134; .js NodeNext specifiers)
  </read_first>
  <action>Add `export { fiTrajectory, type FiTrajectoryResult } from './fi/fi-trajectory.js';` to the FI block in `packages/core/src/index.ts`, alongside `fiImpact`/`compareScenarios`/`projectFiDate`, following the block's documented discipline (closed result type, Money dollars, `Dec` NOT re-exported). If `buyPath`/`renterBaselinePath` were promoted to a shared helper, ensure that helper is NOT re-exported from the barrel (it returns/consumes the internal Dec type). Run the full core suite to confirm the four existing goldens remain byte-identical (fiTrajectory adds a new entry, it does not change any existing computed result).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>`npm test` exits 0 with all prior core/app tests green and the four committed goldens byte-identical (no UPDATE_GOLDEN regen); `packages/core/src/index.ts` exports `fiTrajectory` and `FiTrajectoryResult`.</acceptance_criteria>
  <done>fiTrajectory published from the barrel; full suite green; goldens unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core public barrel | New symbol crossing the stable @house/core import boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Tampering (correctness) | fiTrajectory dollar fields | mitigate | Result dollars cross as Money only; `Dec` not re-exported; canonicalJson-finiteness test prevents a non-serializable/float-leaking series |
| T-7-05 | Information Disclosure | internal Dec/path-builder leak | accept→mitigate | Shared path builders (if extracted) stay unexported; barrel exports only the closed result type |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-02)
- `packages/core/src/fi/fi-trajectory.ts` — `fiTrajectory(input: EngineInput): FiTrajectoryResult`
- Type `FiTrajectoryResult` (`points[]` with `buyNetWorth`/`rentNetWorth` Money, `fiThreshold` Money, `buyFiMonth`/`rentFiMonth` number|null)
- `packages/core/src/fi/fi-trajectory.test.ts`
- Barrel export `fiTrajectory` + `FiTrajectoryResult` in `packages/core/src/index.ts`
</artifacts_produced>

<verification>
- `npx vitest run packages/core -t "fiTrajectory"` green
- Reconciliation: buyFiMonth/rentFiMonth === projectFiDate on the same input
- `npm test` full suite green; four goldens byte-identical
</verification>

<success_criteria>
The core exposes a pure, tested net-worth trajectory series for both paths that reconciles with projectFiDate and feeds the D-07 chart through the Money-string boundary; no financial logic is pushed to the web layer.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-02-SUMMARY.md` when done.
</output>
