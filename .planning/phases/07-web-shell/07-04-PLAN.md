---
phase: 07-web-shell
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - apps/web/src/lib/dto/town.ts
  - apps/web/src/lib/dto/sensitivity.ts
  - apps/web/src/lib/dto/trajectory.ts
  - apps/web/src/lib/dto/sensitivity.test.ts
  - apps/web/src/app/actions/towns.ts
  - apps/web/src/app/actions/sensitivity.ts
  - apps/web/src/app/actions/trajectory.ts
autonomous: true
requirements: [SC-2, SC-3]

must_haves:
  truths:
    - "scoreTowns / tornado / fiTrajectory each reach the UI only through a thin Server Action that validates input and maps results to plain DTOs"
    - "The tornado DTO carries finite swingMonths + top-3 drivers (no Infinity); the trajectory DTO carries Money as decimal strings for the single chart-edge conversion"
    - "The town heatmap DTO preserves the 05-UI-SPEC encoding (Bucket enum, decimal-string composite, MaFlag enums, explicit no-data)"
  artifacts:
    - path: "apps/web/src/app/actions/towns.ts"
      provides: "'use server' scoreTowns wrapper (budget in as Money string)"
      contains: "scoreTowns"
    - path: "apps/web/src/app/actions/sensitivity.ts"
      provides: "'use server' tornado wrapper"
      contains: "tornado"
    - path: "apps/web/src/app/actions/trajectory.ts"
      provides: "'use server' fiTrajectory wrapper feeding the D-07 chart"
      contains: "fiTrajectory"
  key_links:
    - from: "apps/web/src/app/actions/trajectory.ts"
      to: "@house/core fiTrajectory"
      via: "parse → fiTrajectory → toTrajectoryDTO (Money→decimal string)"
      pattern: "fiTrajectory"
    - from: "apps/web/src/lib/dto/sensitivity.ts"
      to: "TornadoResult"
      via: "finite swingMonths assertion (no Infinity)"
      pattern: "swingMonths"
---

<objective>
Build the Server-Action layer for the data-dense views: the town heatmap (`scoreTowns`), the sensitivity tornado (`tornado`), and the D-07 hero trajectory (`fiTrajectory`, from 07-02). Each is a thin `'use server'` wrapper that validates input through the core schemas, calls one core entry point, and maps the result to a plain DTO — town composite/bucket pass through as the locked 05-UI-SPEC encoding, the tornado swings cross as finite numbers, and the trajectory dollars cross as decimal strings for the single chart-edge conversion.

Purpose: SC-3 (heatmap + sensitivity views) and SC-2 (the trajectory chart inside the expanded scenario) need server endpoints that surface the existing core outputs without re-deriving any math. The tornado runs `fiImpact` ~12× inside the core (live recompute is cheap and synchronous — D-08 cadence applies; no separate "Run" trigger needed for the locked stack).
Output: `towns.ts`, `sensitivity.ts`, `trajectory.ts` actions + their DTO mappers and the tornado finiteness test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-RESEARCH.md
@.planning/phases/07-web-shell/07-PATTERNS.md
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sensitivity + trajectory DTOs + finiteness test</name>
  <read_first>
    - packages/core/src/fi/sensitivity.ts (TornadoResult/TornadoRow: finite swingMonths + discriminated FiOutcomes + topDrivers lines ~46-68)
    - packages/core/src/fi/fi-trajectory.ts (FiTrajectoryResult: points buyNetWorth/rentNetWorth Money, fiThreshold Money, buyFiMonth/rentFiMonth — from 07-02)
    - packages/core/src/money/money.ts (toDecimalString exit)
    - .planning/phases/07-web-shell/07-RESEARCH.md (test map FI-05 no-Infinity; Pitfall 5 float confined to chart edge; Code Examples TrajectoryChart number[])
  </read_first>
  <behavior>
    - Test 1 (tornado finite): `toTornadoDTO(result)` carries every `swingMonths` as a finite number; `JSON.stringify(dto)` does not throw and contains no `"Infinity"`/`null`-where-number-expected; `topDrivers` length ≤ 3.
    - Test 2 (trajectory string boundary): `toTrajectoryDTO(result)` carries every `buyNetWorth`/`rentNetWorth` as a decimal STRING (not a number, not a Money); `fiThreshold` is a string; `buyFiMonth`/`rentFiMonth` pass through as number|null.
  </behavior>
  <action>Create `apps/web/src/lib/dto/sensitivity.ts` with `toTornadoDTO(r: TornadoResult)` — pass through `swingMonths` (already finite), the discriminated `FiOutcome`s, driver names, and `topDrivers`; assert no Money lives in TornadoResult (it is already plain-serializable, the mapper exists to guarantee the contract). Create `apps/web/src/lib/dto/trajectory.ts` with `toTrajectoryDTO(r: FiTrajectoryResult)` mapping each point's `buyNetWorth`/`rentNetWorth` and `fiThreshold` via `.toDecimalString()` (NEVER `Number()` here — the single float conversion happens later in components/charts/**). Create `apps/web/src/lib/dto/sensitivity.test.ts` covering both behaviors against real core fixtures built through `engineInput(...)`.</action>
  <verify>
    <automated>npx vitest run apps/web/src/lib/dto/sensitivity.test.ts</automated>
  </verify>
  <acceptance_criteria>Test exits 0; `JSON.stringify(toTornadoDTO(...))` contains no `Infinity`; every trajectory DTO dollar field is `typeof === 'string'`; grep finds no `Number(` in `apps/web/src/lib/dto/trajectory.ts`.</acceptance_criteria>
  <done>Tornado + trajectory DTOs proven finite and string-bounded.</done>
</task>

<task type="auto">
  <name>Task 2: towns.ts + town DTO (05-UI-SPEC encoding pass-through)</name>
  <read_first>
    - packages/core/src/towns/score-towns.ts (TownScoreboard: composite/budget decimal STRINGS lines ~52-70; Bucket + MaFlag string enums; MetricContribution; explicit no-data)
    - packages/core/src/index.ts (scoreTowns, TownScoringInput, TownScoreboard exports lines 143-153)
    - .planning/phases/05-town-scoring-heatmap/05-UI-SPEC.md (the bucket palette + heatmap encoding + missing-metric contract this DTO must preserve)
  </read_first>
  <action>Create `apps/web/src/lib/dto/town.ts` with `toScoreboardDTO(r: TownScoreboard)` — mostly a pass-through since `composite`/`budget` are already decimal strings and `Bucket`/`MaFlag` are plain string enums (RSC-safe); flatten per-town `MetricContribution[]` for the heatmap tooltip and keep the explicit no-data marker (never a silent 0/blank). Create `apps/web/src/app/actions/towns.ts` with `'use server'`: parse `raw.assumptions` (the V4 townScoring config lives here) and the budget as a canonical decimal string into `Money.of(budgetString)`, build the `TownScoringInput`, call `scoreTowns(input)` ONCE, return `toScoreboardDTO`. The active budget is inherited from the cockpit context (D-02) and crosses as a decimal string (no bare number).</action>
  <verify>
    <automated>npx eslint apps/web/src/app/actions/towns.ts && npx vitest run apps/web</automated>
  </verify>
  <acceptance_criteria>`npx eslint apps/web/src/app/actions/towns.ts` exits 0; the action is `'use server'`, calls `scoreTowns` exactly once, and contains no bucketing/composite arithmetic (every encoding value is a core output); `npx vitest run apps/web` stays green.</acceptance_criteria>
  <done>scoreTowns wrapped thinly; the heatmap encoding crosses faithfully.</done>
</task>

<task type="auto">
  <name>Task 3: sensitivity.ts + trajectory.ts Server Actions</name>
  <read_first>
    - packages/core/src/index.ts (tornado, TornadoResult lines 129-134; fiTrajectory from 07-02)
    - packages/core/src/engine/engine-input.ts (engineInput, parse* signatures)
    - apps/web/src/lib/dto/sensitivity.ts + trajectory.ts (from Task 1)
  </read_first>
  <action>Create `apps/web/src/app/actions/sensitivity.ts` with `'use server'`: parse household/assumptions/scenario through the core schemas, build the `EngineInput`, call `tornado(input)` ONCE, return `toTornadoDTO`. Create `apps/web/src/app/actions/trajectory.ts` with `'use server'`: same parse→build, call `fiTrajectory(input)` ONCE, return `toTrajectoryDTO` (feeds the D-07 cockpit chart). Both follow the thin parse→one-call→DTO shape; no perturbation loops or trajectory math in the web layer (those are core outputs — Don't-Hand-Roll).</action>
  <verify>
    <automated>npx eslint apps/web/src/app/actions/sensitivity.ts apps/web/src/app/actions/trajectory.ts && npx vitest run apps/web</automated>
  </verify>
  <acceptance_criteria>Both action files are `'use server'`, each calls its single core entry (`tornado` / `fiTrajectory`) exactly once, and contain no driver-perturbation or net-worth-loop logic; eslint + `npx vitest run apps/web` exit 0.</acceptance_criteria>
  <done>Sensitivity + trajectory actions wired thinly to their core entry points.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Public POST endpoints; budget/assumptions/scenario payloads untrusted |
| Server Action → client (RSC) | Plain data only; tornado/trajectory/town DTOs must be serializable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering / DoS | towns/sensitivity/trajectory actions | mitigate | Parse all input through parseAssumptionSet/parseHousehold/parseScenarioInputs before any core call (D-16); budget crosses as a decimal string, not a bare number |
| T-7-04 | Tampering (correctness) | trajectory Money fields / tornado swings | mitigate | Trajectory dollars cross as decimal strings (Number() confined to charts); tornado finiteness test forbids Infinity (FI-05) |
| T-7-06 | Information Disclosure | town no-data cells | accept | Explicit no-data marker preserved (never silent 0); low-risk local data |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-04)
- `apps/web/src/app/actions/towns.ts` — `scoreTownsAction`
- `apps/web/src/app/actions/sensitivity.ts` — `tornadoAction`
- `apps/web/src/app/actions/trajectory.ts` — `fiTrajectoryAction`
- `apps/web/src/lib/dto/town.ts` — `toScoreboardDTO`
- `apps/web/src/lib/dto/sensitivity.ts` — `toTornadoDTO`
- `apps/web/src/lib/dto/trajectory.ts` — `toTrajectoryDTO`
- Test: `dto/sensitivity.test.ts`
</artifacts_produced>

<verification>
- `npx vitest run apps/web/src/lib/dto/sensitivity.test.ts` green (finite tornado, string trajectory)
- `npx eslint apps/web` exits 0
- Each action calls exactly one core entry point
</verification>

<success_criteria>
The heatmap, tornado, and trajectory views have thin server endpoints that surface existing core outputs faithfully; no math is duplicated in the web layer; the chart-edge float conversion is deferred to the chart components only.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-04-SUMMARY.md` when done.
</output>
