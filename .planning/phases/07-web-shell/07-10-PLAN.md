---
phase: 07-web-shell
plan: 10
type: execute
wave: 5
depends_on: ["07-07", "07-08", "07-09", "07-11"]
files_modified:
  - .planning/phases/07-web-shell/07-VALIDATION.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4]

must_haves:
  truths:
    - "A clean `next build` succeeds (proves no client-bundle leak / no transpile↔externalize conflict) and the full Vitest suite is green across packages/core, packages/app, and apps/web"
    - "A human verifies the flight-simulator loop end-to-end: create/edit a profile, edit an assumption/scenario, watch the cockpit re-fly live, and confirm the anti-funnel presentation (rent baseline can win, bank shown only as a gap, no success-green)"
    - "All four ROADMAP success criteria are demonstrably satisfied through the running UI"
  artifacts:
    - path: ".planning/phases/07-web-shell/07-VALIDATION.md"
      provides: "verified per-task verification map + phase-gate sign-off"
      contains: "next build"
  key_links:
    - from: "apps/web (full app)"
      to: "@house/core + @house/app"
      via: "clean next build + full suite green"
      pattern: "next build"
---

<objective>
Close the phase with the gate: a clean `next build` (proving no native module leaked into the client bundle and no transpile↔externalize conflict), the full Vitest suite green across all three projects, and a human end-to-end verification of the flight-simulator loop and the anti-funnel presentation against all four ROADMAP success criteria.

Purpose: This is the phase gate before `/gsd-verify-work`. The build proves the load-bearing boundary held; the human-verify confirms the qualitative anti-funnel product constraints (which no unit test can fully assert) — the rent baseline can win and reads as "don't buy," bank affordability never reads as headroom, and editing a knob re-flies the instruments live. This is also the wave/phase gate where the full `next build` runs (per-task verifies in plans 05-09/11 use the fast `tsc --noEmit` typecheck; the build is reserved for here and the 07-01 scaffold).
Output: a green build + suite and a verified validation sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-VALIDATION.md
@.planning/ROADMAP.md
@.planning/phases/07-web-shell/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — clean next build + full suite + boundary grep</name>
  <read_first>
    - .planning/phases/07-web-shell/07-VALIDATION.md (the per-task verification map populated during planning — VERIFY it, do not author from scratch; Sampling Rate: phase gate = full suite green AND next build clean)
    - .planning/phases/07-web-shell/07-RESEARCH.md (Pitfall 2 client-bundle leak; Pitfall 3 transpile↔externalize conflict)
    - eslint.config.ts (the apps/web boundary block + Number() rule allowing charts/** + lib/format.ts from 07-01)
  </read_first>
  <action>Run `npm run build -w apps/web` and confirm it completes with no "better-sqlite3" in the client bundle and no transpilePackages↔serverExternalPackages conflict. Run `npm test` (all three Vitest projects) and confirm green, with the four core goldens byte-identical. Run `npx eslint .` and confirm the apps/web boundary guards pass (no @house/app in client modules; `Number(` confined to components/charts/** and lib/format.ts). VERIFY and refresh `.planning/phases/07-web-shell/07-VALIDATION.md`: the per-task verification map was populated during planning — confirm each row's `<automated>` command matches the executed plan, mark each task's Status to ✅ green (or ⚠️/❌ with a note), record the actual phase-gate runtimes against the Estimated runtime / Max feedback latency placeholders, and confirm `nyquist_compliant: true` and `wave_0_complete: true` hold. Do NOT author the map from scratch — it already exists; this task validates it against the real run.</action>
  <verify>
    <automated>npm run build -w apps/web && npm test && npx eslint .</automated>
  </verify>
  <acceptance_criteria>`npm run build -w apps/web` exits 0 with no better-sqlite3 in the client bundle; `npm test` exits 0 across packages/core + packages/app + apps/web with the four goldens byte-identical; `npx eslint .` exits 0; 07-VALIDATION.md retains `nyquist_compliant: true`, every map row Status is resolved (✅/⚠️/❌ with notes), and the runtime placeholders are replaced with measured values.</acceptance_criteria>
  <done>Build clean, full suite green, boundary guards pass, validation map verified against the real run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end flight-simulator + anti-funnel verification</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 7 — the four success criteria)
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Key Interaction Contracts; §Color color-honesty rules; §Copywriting anti-funnel locked strings + empty-state "Create a profile to get started")
  </read_first>
  <what-built>
    The full thin Next.js shell: the profile editor (/profile route — create the first profile + edit/delete, all nine Household leaves), cockpit (ranked comparison + pinned rent baseline + trajectory hero chart + inline editor), persistent assumptions rail with live debounced recompute, town heatmap, and sensitivity tornado — all over the proven core, holding zero financial logic.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev -w apps/web`. On first run (no profiles), confirm the empty-state "Create a profile to get started" entry path is reachable from the header/cockpit.
    2. SC-1 (forms): On the /profile route, CREATE the first profile through the form (enter the nine Household leaves: income, savings rate, FI/retirement-spend target, available net worth, current rent, reserve, down-payment cash, monthly debt, current annual savings); confirm an invalid field (e.g. a savings rate ≥ 1) surfaces a field-level error from the core parse. Then edit a scenario through the inline editor; confirm the numbers come back (math ran in the core). Confirm delete-profile shows the destructive-red confirmation.
    3. SC-2: Confirm the comparison table is ranked by FI-date delta and the "Rent & invest the difference" baseline is a pinned, distinct row; construct a strained scenario set and confirm the baseline can sort #1 with the locked "...buying any of these delays it." copy — the don't-buy signal.
    4. SC-3: Open the heatmap (confirm the locked bucket palette + hatched no-data + per-metric tooltips) and the sensitivity tornado (top drivers labeled, "No headline number without a range."); confirm the assumptions rail is visible/editable beside both, and editing a knob re-flies the instruments live (~300ms, no Apply button).
    5. SC-4: Confirm bank affordability appears ONLY as the amber "A bank would approve ~$X more than your FI plan can absorb." caution — never as headroom/target; confirm there is no success-green and no "buy this" nudge anywhere; confirm the FI-date delay reads amber and "earlier" reads neutral (not green).
  </how-to-verify>
  <action>Pause for the developer to walk the five checks above against the running app. Capture any gaps as follow-ups; do not mark the phase complete until the profile-creation entry path, the anti-funnel presentation, and the live loop are confirmed.</action>
  <verify>
    <human-check>Developer confirms all four success criteria are demonstrably satisfied — including creating the first profile through the form — and the anti-funnel constraints hold in the running UI.</human-check>
  </verify>
  <acceptance_criteria>Developer explicitly confirms SC-1..SC-4 pass in the running app, including creating/editing a profile through the form (PROF-01/02/03), the rent-baseline-can-win don't-buy signal, and the bank-as-gap (no headroom, no green) presentation.</acceptance_criteria>
  <done>Human verification of the flight-simulator loop + profile forms + anti-funnel presentation recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full client bundle | The built bundle must contain no server-only native module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02 | Information Disclosure | better-sqlite3 / @house/app in client bundle | mitigate | Clean `next build` is the gate that proves the server-only + eslint boundary held end-to-end |
| T-7-10 | Tampering (product integrity) | buy-funnel regression | mitigate | Human anti-funnel verification against the locked UI-SPEC color/copy contract |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-10)
- Verified `.planning/phases/07-web-shell/07-VALIDATION.md` (`nyquist_compliant: true`, map rows resolved against the real run, measured runtimes)
- Phase gate evidence: clean `next build` + full-suite-green record
</artifacts_produced>

<verification>
- `npm run build -w apps/web` clean (no client-bundle leak)
- `npm test` green across all three projects; four goldens byte-identical
- `npx eslint .` exits 0
- Human SC-1..SC-4 + profile-creation + anti-funnel confirmation
</verification>

<success_criteria>
The phase ships: the thin shell builds cleanly with no boundary leak, the full suite is green, and a human has confirmed the flight-simulator loop (including creating the first profile) and the anti-funnel presentation satisfy all four ROADMAP success criteria.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-10-SUMMARY.md` when done.
</output>
