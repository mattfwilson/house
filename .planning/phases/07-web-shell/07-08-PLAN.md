---
phase: 07-web-shell
plan: 08
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - apps/web/src/app/page.tsx
  - apps/web/src/components/cockpit/ComparisonTable.tsx
  - apps/web/src/components/cockpit/ScenarioRow.tsx
  - apps/web/src/components/cockpit/ExpandedScenario.tsx
  - apps/web/src/components/cockpit/InlineScenarioEditor.tsx
  - apps/web/src/components/charts/TrajectoryChart.tsx
autonomous: true
requirements: [SC-2, SC-4]

must_haves:
  truths:
    - "The cockpit landing shows the ranked-by-FI-date comparison table (all scenarios + the pinned rent-baseline row); the table is ranked by the core's FI-date delta, not a client sort (D-03/D-04)"
    - "The rent-and-invest baseline is a pinned, visually distinct row that competes in the ranking; when it sorts #1 that IS the don't-buy signal — no separate verdict chip (D-05)"
    - "Bank affordability appears only as the amber 'gap' warning, never as headroom or a target (D-06); the FI-date delta hero is color-honest (amber delay, never green)"
    - "Expanding a row shows the trajectory-vs-baseline chart (Recharts) with the FI-threshold line + crossover markers (D-07); Add/Edit is an inline expanding editor row (D-15)"
  artifacts:
    - path: "apps/web/src/components/cockpit/ComparisonTable.tsx"
      provides: "ranked comparison table consuming toCompareDTO (core ranking preserved)"
      contains: "ComparisonTable"
    - path: "apps/web/src/components/charts/TrajectoryChart.tsx"
      provides: "Recharts net-worth trajectory vs baseline + FI-threshold + crossover markers (the ONLY Number() site)"
      contains: "LineChart"
    - path: "apps/web/src/components/cockpit/InlineScenarioEditor.tsx"
      provides: "inline expanding add/edit editor row (D-14/D-15)"
      contains: "InlineScenarioEditor"
  key_links:
    - from: "apps/web/src/components/cockpit/ComparisonTable.tsx"
      to: "apps/web/src/app/actions/scenarios.ts recompareAction"
      via: "consumes toCompareDTO rows in core ranking order"
      pattern: "recompare|CompareRowDTO"
    - from: "apps/web/src/components/charts/TrajectoryChart.tsx"
      to: "trajectory DTO decimal strings"
      via: "Number(decimalString) ONCE at the chart edge"
      pattern: "Number\\("
---

<objective>
Build the cockpit — the home route that IS the comparison view (D-03). Landing shows the ranked-by-FI-date-delta comparison table with the pinned rent-and-invest baseline row (D-04/D-05); selecting a row expands that scenario's FI instruments and the trajectory-vs-baseline hero chart inline (D-07); Add/Edit is an inline expanding editor row (D-14/D-15). Bank affordability appears only as the amber "gap" caution (D-06). The cockpit leads with FI-impact / true affordability and never nudges toward a buy.

Purpose: SC-2 (ranked comparison + first-class rent baseline) and SC-4 (FI-lead, bank-as-gap, anti-funnel) are delivered here — this is the flagship screen. Every number comes from a core output via the Server Actions (07-03/07-04); the ranking is the core's, not a client sort; the only float conversion in the whole web layer's display path is the single `Number()` at the chart edge.
Output: `page.tsx` (cockpit), the comparison table + row + expanded-scenario + inline-editor components, and the Recharts `TrajectoryChart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-UI-SPEC.md
@.planning/phases/07-web-shell/07-RESEARCH.md
@apps/web/src/app/actions/scenarios.ts
@apps/web/src/lib/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ranked comparison table + pinned rent-baseline row + bank-gap caution</name>
  <read_first>
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Key Interaction Contracts Cockpit=comparison D-03, Ranking D-04, Rent baseline D-05, Hero metric; §Copywriting anti-funnel locked strings — rent-baseline label "Rent & invest the difference", wins-#1 copy, bank-gap copy; §Color color-honesty rules)
    - apps/web/src/lib/dto/scenario.ts (CompareRowDTO shape; toEvaluateDTO gap fields)
    - apps/web/src/lib/format.ts (fiDeltaLabel color-honest copy; formatUSD)
    - apps/web/src/store/selection.ts (expandedScenarioId)
  </read_first>
  <action>Create `apps/web/src/components/cockpit/ComparisonTable.tsx` + `ScenarioRow.tsx` rendering the `toCompareDTO` rows IN THE ORDER THE CORE RETURNS THEM (rows[0]=baseline when it wins; unreached last — never re-sort client-side, that is core logic). The hero per-row metric is the FI-date delta via `fiDeltaLabel` (amber for delay, neutral for earlier — never green); net-worth-at-horizon + monthly housing delta are secondary readouts in Geist Mono tabular-nums. The rent-baseline row (`isBaseline`) is pinned + visually distinct (subtle teal left-border / slate-800 emphasis, NO success styling); when it ranks #1 render the locked copy "Renting and investing the difference reaches FI soonest — buying any of these delays it." (no separate verdict chip — D-05). Render the bank-vs-true gap from `toEvaluateDTO` as the amber caution string "A bank would approve ~{amount} more than your FI plan can absorb." — never labeled headroom/target (D-06). Use shadcn Table; selecting a row sets `expandedScenarioId` in the selection store.</action>
  <verify>
    <automated>npx tsc -p apps/web/tsconfig.json --noEmit && npx eslint apps/web/src/components/cockpit</automated>
  </verify>
  <acceptance_criteria>`npx tsc -p apps/web/tsconfig.json --noEmit` typechecks clean; the table renders rows in DTO order with no client `.sort(` on the rows (grep confirms); the rent-baseline row is pinned/distinct with no success-green token; the bank gap uses the amber caution copy and never the word "headroom"; grep finds no `Number(` in cockpit/* (formatting goes through format.ts / charts only); `npx eslint apps/web/src/components/cockpit` exits 0. (Full `next build` runs at the 07-10 phase gate.)</acceptance_criteria>
  <done>Ranked comparison table with the pinned anti-funnel baseline and the bank-gap caution.</done>
</task>

<task type="auto">
  <name>Task 2: Expanded scenario + Recharts trajectory chart (D-07)</name>
  <read_first>
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Key Interaction Contracts Hero chart D-07; §Typography Display role for the hero FI-date delta)
    - .planning/phases/07-web-shell/07-RESEARCH.md (§Code Examples TrajectoryChart — points number[], ReferenceLine threshold, ReferenceDot crossover; Pitfall 5 single Number() site)
    - apps/web/src/lib/dto/trajectory.ts (toTrajectoryDTO — buyNetWorth/rentNetWorth/fiThreshold as decimal STRINGS)
    - apps/web/src/app/actions/trajectory.ts (fiTrajectoryAction)
  </read_first>
  <action>Create `apps/web/src/components/charts/TrajectoryChart.tsx` (`'use client'`) — a Recharts LineChart over `{ month, buy, rent }` points, with a `ReferenceLine` at the FI threshold and `ReferenceDot`s at `buyFiMonth`/`rentFiMonth` (D-07). This is a sanctioned `Number()` site in the display path (the eslint rule confines `Number(` to components/charts/** and lib/format.ts): convert the trajectory DTO's decimal strings to numbers ONCE here, at the chart-data build step, after all math is done in the core (Pitfall 5). The buy line vs rent-baseline line divergence IS the cost of the house — render with color-honest, non-green styling. Create `apps/web/src/components/cockpit/ExpandedScenario.tsx` — shown when a row is expanded (D-03): the Display-role hero FI-date delta, the FI instruments (targets, outcome), and the TrajectoryChart fed by `fiTrajectoryAction` for the selected scenario.</action>
  <verify>
    <automated>npx tsc -p apps/web/tsconfig.json --noEmit && npx eslint apps/web</automated>
  </verify>
  <acceptance_criteria>`npx tsc -p apps/web/tsconfig.json --noEmit` typechecks clean; `TrajectoryChart.tsx` contains the `ReferenceLine` (threshold) + `ReferenceDot` (crossover) and is the ONLY file with a `Number(` call in the cockpit display path (grep: in cockpit/charts, `Number(` appears under components/charts/** only); ExpandedScenario renders the Display-role hero delta + the chart; `npx eslint apps/web` exits 0 (the charts/format Number() rule passes). (Full `next build` runs at the 07-10 phase gate.)</acceptance_criteria>
  <done>Expanding a scenario shows the trajectory-vs-baseline hero chart with threshold + crossover markers.</done>
</task>

<task type="auto">
  <name>Task 3: Inline add/edit scenario editor row (D-14/D-15)</name>
  <read_first>
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Key Interaction Contracts Scenario builder D-14, Inline editor D-15; §Copywriting "Add scenario"/"Save scenario"; §Color destructive red for delete only)
    - apps/web/src/app/actions/scenarios.ts (computeAndSaveScenarioAction, deleteScenarioAction, recompareAction)
    - packages/core/src/towns/town-table.ts (the curated 24-town set for the town selector)
    - .planning/phases/07-web-shell/07-RESEARCH.md (D-16 — field errors surfaced from the core parse, no UI schema)
  </read_first>
  <action>Create `apps/web/src/components/cockpit/InlineScenarioEditor.tsx` — an inline expanding editor row within the comparison table (NOT a modal, NOT a route — D-15). Manual entry is the default path (D-14): price, down-payment %, rate, term, town (Select from the curated 24-town table), insurance, HOA, etc., as plain numerics converted to canonical decimal strings. Offer the OPTIONAL "prefill from a sample listing" affordance (MockListingsProvider browse via a listings action) — listings are never the required entry point. On "Add scenario"/"Save scenario", call `computeAndSaveScenarioAction` (freezes the working set — D-09) and the table re-flies + re-ranks immediately via `recompareAction`; field-level errors are surfaced from the core Zod parse result (D-16 — no duplicated validation). Delete uses the shadcn alert-dialog with the destructive-red confirmation copy from the UI-SPEC. Wire the cockpit page `apps/web/src/app/page.tsx` to compose ComparisonTable + ExpandedScenario + InlineScenarioEditor, reading the active profile/scenario from the selection store.</action>
  <verify>
    <automated>npx tsc -p apps/web/tsconfig.json --noEmit && npx eslint apps/web</automated>
  </verify>
  <acceptance_criteria>`npx tsc -p apps/web/tsconfig.json --noEmit` typechecks clean; the editor is an inline row (no Dialog/modal wrapper around the whole form, no separate /new route); the town selector is sourced from the curated 24-town table; save calls `computeAndSaveScenarioAction` then re-ranks; the editor contains no validation schema (errors come from the action's parse result); delete uses the destructive-red alert-dialog copy verbatim; `npx eslint apps/web` exits 0. (Full `next build` runs at the 07-10 phase gate.)</acceptance_criteria>
  <done>Inline add/edit/delete editor that re-flies and re-ranks on save, finances-first by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor form → Server Action | Scenario form inputs cross to a public POST action |
| trajectory DTO → chart | Decimal strings become numbers at the single chart edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | scenario editor inputs | mitigate | Validation surfaced from the core Zod parse at computeAndSaveScenarioAction/recompareAction (D-16); editor holds no schema |
| T-7-04 | Tampering (correctness) | float re-entry at the chart | mitigate | Single `Number()` site in TrajectoryChart (charts/format eslint rule); cockpit display uses format.ts over strings |
| T-7-10 | Tampering (product integrity) | buy-funnel nudge | mitigate | Ranking is the core's (no client sort); rent baseline pinned + can win; bank shown only as amber gap; no success-green — enforced by review + UI-SPEC color contract |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-08)
- `apps/web/src/app/page.tsx` — cockpit route (D-03)
- `apps/web/src/components/cockpit/ComparisonTable.tsx`, `ScenarioRow.tsx`, `ExpandedScenario.tsx`, `InlineScenarioEditor.tsx`
- `apps/web/src/components/charts/TrajectoryChart.tsx` — Recharts trajectory + threshold + crossover (a sanctioned Number() display site)
</artifacts_produced>

<verification>
- `npx tsc -p apps/web/tsconfig.json --noEmit` + `npx eslint apps/web` clean (full `next build` at the 07-10 phase gate)
- Ranking comes from the DTO (no client `.sort` on rows); rent baseline pinned + win-copy present
- Bank gap rendered as amber caution (never "headroom")
- `Number(` confined to components/charts/** + lib/format.ts (eslint green)
</verification>

<success_criteria>
The cockpit is the ranked comparison view: FI-date-led, anti-funnel (rent baseline can win = don't-buy signal), bank-as-gap caution, with the trajectory hero chart on expand and inline finances-first scenario editing — SC-2 and SC-4 delivered.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-08-SUMMARY.md` when done.
</output>
