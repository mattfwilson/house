---
phase: 07-web-shell
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-03"]
files_modified:
  - apps/web/components.json
  - apps/web/src/components/ui/.gitkeep
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/lib/format.ts
  - apps/web/src/lib/format.test.ts
  - apps/web/src/components/chrome/Header.tsx
autonomous: false
requirements: [SC-1, SC-4]

must_haves:
  truths:
    - "shadcn/ui is initialized (New York / slate / dark) using ONLY the official registry; the registry safety gate runs at init"
    - "A persistent header bar carries the profile + scenario switcher and is present on every route (D-02)"
    - "Dollar/FI-date formatting happens only at the display edge via Intl.NumberFormat over decimal strings; the FI-date delta copy is color-honest (amber delay, neutral earlier — never green)"
  artifacts:
    - path: "apps/web/src/lib/format.ts"
      provides: "formatUSD(decimalString) + fiDeltaLabel(months) display-edge helpers"
      contains: "Intl.NumberFormat"
    - path: "apps/web/src/components/chrome/Header.tsx"
      provides: "persistent profile + scenario switcher (D-02)"
      contains: "Header"
    - path: "apps/web/src/app/layout.tsx"
      provides: "Geist fonts, dark slate base, header mounted on every route"
      contains: "Header"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/chrome/Header.tsx"
      via: "header rendered above the route children slot"
      pattern: "Header"
    - from: "apps/web/src/lib/format.ts"
      to: "decimal strings from DTOs"
      via: "Number() as the LAST step feeding only the formatter"
      pattern: "Intl.NumberFormat"
---

<objective>
Stand up the client-side app chrome the cockpit, heatmap, and sensitivity views all sit inside: initialize shadcn/ui (New York / slate / dark, official registry only), build the persistent header with the profile + scenario switcher (D-02), the dark instrument-panel base layout with Geist fonts (D-12), and the display-edge formatting helpers (`formatUSD`, color-honest `fiDeltaLabel`).

Purpose: SC-1/SC-4 — the IA and the FI-honest presentation start in the chrome. The header is the always-present profile/scenario context that the heatmap and sensitivity routes inherit (D-02). `format.ts` is the display edge where decimal strings become formatted dollars (the LAST `Number()` step, feeding only the formatter) and where the FI-date delta renders amber-for-delay / neutral-for-earlier (never success-green — the load-bearing anti-funnel color rule).
Output: shadcn init, `layout.tsx`, `Header.tsx`, `format.ts` + its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-UI-SPEC.md
@.planning/phases/07-web-shell/07-RESEARCH.md
@apps/web/src/app/actions/profiles.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: shadcn init (official registry only) + registry safety gate</name>
  <read_first>
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Design System preset; §Registry Safety — official blocks only, gate runs at init)
    - .planning/phases/07-web-shell/07-RESEARCH.md (§Standard Stack: `npx shadcn@canary init` for TW v4 + React 19; A2)
  </read_first>
  <what-built>
    Run `npx shadcn@canary init` inside apps/web (New York style, base color slate, dark mode, CSS variables on) and add the official blocks named in the UI-SPEC: button, card, table, dialog, alert-dialog, slider, tabs, tooltip, select, popover, input, form, label, badge, sidebar, sonner. This pulls Radix primitives (first-party shadcn defaults) into the lockfile.
  </what-built>
  <how-to-verify>
    1. Confirm `components.json` shows style "new-york", baseColor "slate", rsc true.
    2. Confirm every block was fetched from the OFFICIAL shadcn registry (no `--registry <url>` third-party flag used — UI-SPEC declares none).
    3. Confirm the new Radix dependencies in apps/web/package.json are @radix-ui/* + class-variance-authority/clsx/tailwind-merge (shadcn defaults), nothing unexpected.
  </how-to-verify>
  <action>Pause after `shadcn init` + block install. Present components.json + the new dependency list. Per UI-SPEC Registry Safety, vetting is N/A for official-only blocks — but confirm no third-party registry was introduced before continuing.</action>
  <verify>
    <automated>npx eslint apps/web 2>&1 | head -5; test -f apps/web/components.json && echo components.json-present</automated>
  </verify>
  <acceptance_criteria>`apps/web/components.json` exists with style "new-york" / baseColor "slate"; the developer confirms only the official registry was used and the new deps are shadcn/Radix defaults.</acceptance_criteria>
  <done>shadcn initialized with official blocks; registry surface confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: format.ts display-edge helpers + color-honesty test</name>
  <read_first>
    - .planning/phases/07-web-shell/07-RESEARCH.md (§Code Examples format.ts: Intl.NumberFormat over decimal strings; fiDeltaLabel tone delay/earlier/none)
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Color color-honesty rules; §Copywriting anti-funnel locked strings "+{Y} yr {M} mo later" / "{Y} yr {M} mo earlier")
  </read_first>
  <behavior>
    - Test 1: `formatUSD('1234.5')` returns `$1,234.50`; the only `Number()` call is the final formatter input (no further math).
    - Test 2: `fiDeltaLabel(40)` returns `{ text: '+3 yr 4 mo later', tone: 'delay' }`; `fiDeltaLabel(-14)` returns `{ text: '1 yr 2 mo earlier', tone: 'earlier' }`; `fiDeltaLabel(0)` returns tone `'none'`; `fiDeltaLabel(null)` returns `{ text: '—', tone: 'none' }`. Tone 'delay' maps to amber and 'earlier' is NEVER 'good'/green.
  </behavior>
  <action>Create `apps/web/src/lib/format.ts` (a CLIENT display-edge module — this is one of the few sanctioned `Number()` sites alongside components/charts/**, so confirm the eslint rule from 07-01 permits `Number()` here or scope the rule to allow `lib/format.ts` as a display-edge exception). Export `formatUSD(decimalString: string)` using `Intl.NumberFormat('en-US', { style:'currency', currency:'USD' })` with `Number(decimalString)` as the LAST step feeding only the formatter. Export `fiDeltaLabel(months: number | null): { text: string; tone: 'delay'|'earlier'|'none' }` per the UI-SPEC locked strings, color-honest (delay→amber tone, earlier→neutral tone, never green). Create `apps/web/src/lib/format.test.ts` covering both behaviors.</action>
  <verify>
    <automated>npx vitest run apps/web/src/lib/format.test.ts</automated>
  </verify>
  <acceptance_criteria>Test exits 0; `fiDeltaLabel` returns the exact UI-SPEC copy and never a 'good'/green tone for 'earlier'; `formatUSD` produces 2dp currency; the `Number()` usage is confined to the formatter input.</acceptance_criteria>
  <done>Display-edge formatting + color-honest FI delta copy proven.</done>
</task>

<task type="auto">
  <name>Task 3: layout.tsx (dark base, Geist fonts) + persistent Header (D-02)</name>
  <read_first>
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Design System fonts; §Color 60/30/10 chrome; §Key Interaction Contracts Cockpit IA D-01/D-02; §Typography roles)
    - apps/web/src/app/actions/profiles.ts (listProfilesAction — header populates the profile switcher)
    - apps/web/src/app/actions/scenarios.ts (listScenariosAction — header populates the scenario switcher)
  </read_first>
  <action>Modify `apps/web/src/app/layout.tsx` to load Geist Sans + Geist Mono via `next/font`, apply the dark slate-900 base (`#0F172A`) with slate-50 text, and render `<Header/>` above the route `children` slot so it is present on every route (D-02). Add `tabular-nums` utility for numeric readouts in `globals.css`. Create `apps/web/src/components/chrome/Header.tsx` — a slate-800 (`#1E293B`) header bar with a profile switcher (Select populated from `listProfilesAction`) and a scenario switcher (Select populated from `listScenariosAction` for the active profile); the active scenario selection lives in the Zustand selection store (07-06) — for this plan, wire the switchers to call the list actions and expose `onChange` props (the store binding lands when 07-06 + 07-07 mount). Icon-only controls carry `aria-label` matching the action verb (UI-SPEC a11y contract). Use teal (`#0F766E`) only for the active/selected affordance per the UI-SPEC accent-reserved list.</action>
  <verify>
    <automated>npm run build -w apps/web && npx eslint apps/web</automated>
  </verify>
  <acceptance_criteria>`npm run build -w apps/web` exits 0 with Header rendered in layout.tsx; `npx eslint apps/web` exits 0; Header.tsx contains profile + scenario switcher controls and uses teal only for the active affordance; no success-green token appears in the chrome.</acceptance_criteria>
  <done>Persistent dark instrument-panel chrome with the profile/scenario switcher present on every route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry → codebase | Copy-in component source enters the repo |
| client component → display edge | Decimal strings converted to formatted numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-SC | Tampering | shadcn block install | mitigate | Task 1 human-verify; official registry only (UI-SPEC declares no third-party registry); registry safety gate runs at init |
| T-7-04 | Tampering (correctness) | format.ts Number() | mitigate | Number() is the LAST step feeding only Intl.NumberFormat; never re-enters math; confined to the display edge |
| T-7-07 | Information Disclosure | header reads profile/scenario lists | accept | Local 2-user tool, no auth surface; lists come from validated server actions |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-05)
- shadcn/ui initialized: `apps/web/components.json` + `apps/web/src/components/ui/*` (official blocks)
- `apps/web/src/app/layout.tsx` — Geist fonts, dark slate base, persistent Header
- `apps/web/src/components/chrome/Header.tsx` — profile + scenario switcher (D-02)
- `apps/web/src/lib/format.ts` — `formatUSD`, `fiDeltaLabel`
- `apps/web/src/app/globals.css` — tabular-nums + palette tokens
- Test: `lib/format.test.ts`
</artifacts_produced>

<verification>
- `npm run build -w apps/web` clean
- `npx vitest run apps/web/src/lib/format.test.ts` green (color-honest FI delta)
- Header present on every route; teal reserved for active affordance only
</verification>

<success_criteria>
The dark instrument-panel chrome with a persistent profile/scenario switcher is in place, shadcn is initialized from the official registry, and the display-edge formatting renders FI deltas color-honestly (never green) — the anti-funnel presentation starts in the chrome.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-05-SUMMARY.md` when done.
</output>
