---
phase: 07-web-shell
plan: 09
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - apps/web/src/app/heatmap/page.tsx
  - apps/web/src/components/heatmap/HeatmapGrid.tsx
  - apps/web/src/components/heatmap/HeatmapCell.tsx
  - apps/web/src/app/sensitivity/page.tsx
  - apps/web/src/components/charts/TornadoChart.tsx
autonomous: true
requirements: [SC-3, SC-4]

must_haves:
  truths:
    - "The town heatmap renders as a CSS-grid towns×metrics table-heatmap (D-13), cells colored by the locked 05-UI-SPEC bucket palette + lightness intensity, with hatched explicit no-data cells and per-metric tooltips — no chart dependency"
    - "The heatmap inherits the active profile/scenario budget context (D-02) which drives the bucketing"
    - "The sensitivity route renders the tornado FI-date swing with the top drivers labeled and 'No headline number without a range' framing (FI-05); the assumptions rail is visible next to both results (SC-3/D-10)"
  artifacts:
    - path: "apps/web/src/components/heatmap/HeatmapGrid.tsx"
      provides: "CSS-grid towns×metrics heatmap honoring the 05-UI-SPEC encoding contract"
      contains: "grid"
    - path: "apps/web/src/components/charts/TornadoChart.tsx"
      provides: "Recharts tornado bars over finite swingMonths, top-3 labeled"
      contains: "BarChart"
    - path: "apps/web/src/app/sensitivity/page.tsx"
      provides: "sensitivity route with FI-05 framing"
      contains: "sensitivity"
  key_links:
    - from: "apps/web/src/components/heatmap/HeatmapCell.tsx"
      to: "05-UI-SPEC bucket palette"
      via: "Bucket enum → teal/amber/slate + hatched no-data"
      pattern: "Bucket|Realistic|Stretch|Fantasy"
    - from: "apps/web/src/app/sensitivity/page.tsx"
      to: "apps/web/src/app/actions/sensitivity.ts tornadoAction"
      via: "consumes toTornadoDTO finite swings"
      pattern: "tornado"
---

<objective>
Build the two data-dense routes: the town affordability heatmap (CSS-grid table-heatmap, D-13) and the sensitivity tornado view (FI-05). Both inherit the active profile/scenario context and sit alongside the persistent assumptions rail (D-10), so assumptions are visible and editable next to every result.

Purpose: SC-3 — "view the town affordability heatmap and the sensitivity (tornado) view, with assumptions visible and editable next to every result." The heatmap renders the locked 05-UI-SPEC encoding contract exactly (bucket palette, lightness intensity, explicit hatched no-data, per-metric tooltips) with no chart dependency; the tornado labels the top drivers and always shows a range (FI-05, SC-4 — no headline number without a range). All values are core outputs (`scoreTowns`, `tornado`) — no bucketing or perturbation math in the web layer.
Output: heatmap route + grid/cell components, sensitivity route + Recharts `TornadoChart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-UI-SPEC.md
@.planning/phases/05-town-scoring-heatmap/05-UI-SPEC.md
@apps/web/src/app/actions/towns.ts
@apps/web/src/app/actions/sensitivity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-grid town heatmap (05-UI-SPEC encoding) + budget context</name>
  <read_first>
    - .planning/phases/05-town-scoring-heatmap/05-UI-SPEC.md (the LOCKED bucket palette teal #0F766E / amber #B45309 / slate #64748B / hatched-gray #94A3B8 no-data; per-metric explainable tooltip shape; MA-flag chip styling; verbatim bucket labels + empty/error/missing-metric copy)
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Key Interaction Contracts Heatmap render D-13; §Spacing heatmap cell 40-48px on 4px gap)
    - apps/web/src/lib/dto/town.ts (toScoreboardDTO — Bucket enum, decimal-string composite, MetricContribution, no-data marker)
    - apps/web/src/store/working-set.ts (the budget control value driving bucketing)
  </read_first>
  <action>Create `apps/web/src/components/heatmap/HeatmapCell.tsx` — a single towns×metrics cell colored by the locked bucket palette (Realistic teal / Stretch amber / Fantasy slate) with lightness intensity scaled by the normalized composite scalar; an explicit hatched-gray cell for no-data (never a silent 0/blank); a shadcn Tooltip showing the per-metric breakdown (MetricContribution) per the 05-UI-SPEC explainable contract. MA-flag chips render as neutral/informational badges (never destructive/accent). Create `apps/web/src/components/heatmap/HeatmapGrid.tsx` — a CSS-grid (no Recharts/visx — D-13) towns(rows)×metrics(cols) matrix of cells at the dense 40-48px cell size on a 4px gap, sufficient for the 24 towns. Create `apps/web/src/app/heatmap/page.tsx` — calls `scoreTownsAction` with the active budget (inherited from the cockpit context via the working-set/selection store — D-02; budget crosses as a decimal string), renders HeatmapGrid, and reuses the verbatim 05-UI-SPEC empty/error copy. The assumptions rail (mounted in the shared layout, 07-07) is present beside the result.</action>
  <verify>
    <automated>npm run build -w apps/web && npx eslint apps/web/src/components/heatmap</automated>
  </verify>
  <acceptance_criteria>`npm run build -w apps/web` exits 0; HeatmapGrid uses CSS grid (no recharts/visx import in heatmap/*); cells use the exact locked palette hex values; no-data renders the hatched marker (not blank/0); the heatmap performs no bucketing math (buckets come from the DTO — grep finds no budget-vs-price comparison logic in heatmap/*); eslint exits 0.</acceptance_criteria>
  <done>CSS-grid heatmap rendering the locked 05-UI-SPEC encoding, budget inherited from context.</done>
</task>

<task type="auto">
  <name>Task 2: Sensitivity route + Recharts tornado (FI-05)</name>
  <read_first>
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Copywriting anti-funnel "No headline number without a range."; §Color amber for delay emphasis; §Typography Heading/Label)
    - apps/web/src/lib/dto/sensitivity.ts (toTornadoDTO — finite swingMonths, discriminated FiOutcomes, topDrivers ≤3)
    - apps/web/src/app/actions/sensitivity.ts (tornadoAction)
    - .planning/phases/07-web-shell/07-RESEARCH.md (Open Q2 — live recompute cadence is acceptable for the tornado; planner's call: recompute live on rail edits, it is cheap/synchronous)
  </read_first>
  <action>Create `apps/web/src/components/charts/TornadoChart.tsx` (`'use client'`) — a Recharts horizontal BarChart of per-driver FI-date swing, rows sorted DESC by swing (the core already ranks them), the top-3 drivers labeled/emphasized; swing values are finite numbers (no Infinity — guaranteed by the DTO). The `Number()` conversion for the bar data lives here (charts-only edge). Use amber for delay-direction emphasis, never success-green. Create `apps/web/src/app/sensitivity/page.tsx` — calls `tornadoAction` for the active scenario (inherited context), renders the TornadoChart with the verbatim "No headline number without a range." framing (FI-05), and recomputes live on assumptions-rail edits (D-08 cadence; cheap synchronous core — no separate "Run" trigger). The rail (shared layout) is present beside the result (SC-3).</action>
  <verify>
    <automated>npm run build -w apps/web && npx eslint apps/web</automated>
  </verify>
  <acceptance_criteria>`npm run build -w apps/web` exits 0; the tornado renders drivers in the DTO's ranked order with ≤3 labeled top drivers; the "No headline number without a range." string is present on the route; `Number(` appears only under components/charts/** (eslint charts-only rule passes); no success-green token in the sensitivity view.</acceptance_criteria>
  <done>Sensitivity route shows the ranked FI-date-swing tornado with top drivers and the no-range-no-number framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| budget/assumptions → Server Action | Heatmap budget + tornado assumptions cross to public POST actions |
| tornado/town DTO → chart/grid | Finite swings + bucket encoding cross to the client render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | budget / assumptions inputs | mitigate | scoreTownsAction / tornadoAction validate through core Zod (D-16); budget crosses as a decimal string |
| T-7-04 | Tampering (correctness) | tornado swing floats | mitigate | Single Number() site in TornadoChart; finite swings guaranteed by the DTO (no Infinity, FI-05) |
| T-7-06 | Information Disclosure | heatmap no-data cells | accept | Explicit hatched no-data marker (never silent 0/imputed); local data only |
| T-7-10 | Tampering (product integrity) | buy-funnel nudge in data views | mitigate | Locked 05-UI-SPEC palette (no success-green); MA-flags neutral; tornado always shows a range |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-09)
- `apps/web/src/app/heatmap/page.tsx` — town heatmap route (D-13)
- `apps/web/src/components/heatmap/HeatmapGrid.tsx`, `HeatmapCell.tsx` — CSS-grid table-heatmap
- `apps/web/src/app/sensitivity/page.tsx` — sensitivity route (FI-05)
- `apps/web/src/components/charts/TornadoChart.tsx` — Recharts tornado (single Number() site)
</artifacts_produced>

<verification>
- `npm run build -w apps/web` clean
- Heatmap is CSS-grid (no chart dep), locked palette, hatched no-data, explainable tooltips
- Tornado ranked DESC, top-3 labeled, "No headline number without a range." present
- `Number(` confined to components/charts/** (eslint green)
</verification>

<success_criteria>
The heatmap and sensitivity routes surface the town scoreboard and the FI-date tornado faithfully over the locked encoding, with the assumptions rail visible next to every result — SC-3 delivered, anti-funnel preserved.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-09-SUMMARY.md` when done.
</output>
