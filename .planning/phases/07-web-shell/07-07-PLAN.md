---
phase: 07-web-shell
plan: 07
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - apps/web/src/components/rail/AssumptionsRail.tsx
  - apps/web/src/components/rail/KnobRow.tsx
  - apps/web/src/app/layout.tsx
autonomous: true
requirements: [SC-1, SC-3]

must_haves:
  truths:
    - "A persistent docked assumptions rail is visible on every route (cockpit, heatmap, sensitivity) with knobs inline next to the instruments (D-10)"
    - "Editing a knob updates the shared working set and triggers a debounced (~300ms) live recompute with no Apply button (D-08)"
    - "Knob inputs are plain numerics converted to canonical decimal strings; validation is surfaced from the core Zod parse at the Server Action, not duplicated in the rail (D-16)"
  artifacts:
    - path: "apps/web/src/components/rail/AssumptionsRail.tsx"
      provides: "docked rail of assumption knobs wired to working-set + debounced recompute"
      contains: "AssumptionsRail"
    - path: "apps/web/src/components/rail/KnobRow.tsx"
      provides: "single tunable knob (label + input/slider) emitting decimal strings"
      contains: "KnobRow"
  key_links:
    - from: "apps/web/src/components/rail/AssumptionsRail.tsx"
      to: "apps/web/src/store/recompute.ts"
      via: "knob edit → updateKnob → requestRecompute (debounced)"
      pattern: "requestRecompute"
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/rail/AssumptionsRail.tsx"
      via: "rail mounted in the shared layout, echoed on every route"
      pattern: "AssumptionsRail"
---

<objective>
Build the persistent docked assumptions rail (D-10) and wire the live debounced recompute loop (D-08): a sidebar of assumption knobs, always visible and echoed on every route, where editing a tunable updates the shared working set and re-flies the instruments after a ~300ms debounce with no Apply button. Mount the rail in the shared layout so the cockpit, heatmap, and sensitivity routes all inherit it.

Purpose: SC-3 — "assumptions visible and editable next to every result" is the most literal reading of the rail. SC-1 — the rail is where the user edits assumptions, with all math running in the core via the recompute Server Action. Knob inputs convert to canonical decimal strings and validate through the core Zod schemas at the action boundary (D-16); the rail holds no validation logic and no money math.
Output: `AssumptionsRail.tsx`, `KnobRow.tsx`, and the layout mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-UI-SPEC.md
@.planning/phases/07-web-shell/07-RESEARCH.md
@apps/web/src/store/recompute.ts
@apps/web/src/store/working-set.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: KnobRow (decimal-string input) + AssumptionsRail</name>
  <read_first>
    - .planning/phases/07-web-shell/07-UI-SPEC.md (§Key Interaction Contracts Assumptions rail D-10; §Typography Label role; §Spacing rail knob rows; §Color teal reserved for active/budget control)
    - apps/web/src/store/working-set.ts (updateKnob(path, decimalString))
    - apps/web/src/components/ui/* (shadcn slider/input/label/sidebar from 07-05)
    - .planning/phases/07-web-shell/07-RESEARCH.md (D-16 plain numerics → decimal strings; no UI-side validation)
  </read_first>
  <action>Create `apps/web/src/components/rail/KnobRow.tsx` — a single tunable row (Label + shadcn Input or Slider) that takes a current decimal-string value and emits an `onChange(decimalString)` (convert the numeric input to a canonical decimal string at the edge; never hold bare-number money). Use the Label typography role (12px/600) and dense spacing per the UI-SPEC. Create `apps/web/src/components/rail/AssumptionsRail.tsx` — a docked sidebar (shadcn Sidebar surface, slate-800) listing the editable tunables (return, SWR, inflation, appreciation, maintenance, tax band — the V3/V4 sensitivity + projection knobs), each rendered via KnobRow reading the working-set store. The budget control (driving heatmap bucketing) uses the teal active affordance per the UI-SPEC accent-reserved list. The rail holds NO validation and NO math.</action>
  <verify>
    <automated>npm run build -w apps/web && npx eslint apps/web/src/components/rail</automated>
  </verify>
  <acceptance_criteria>`npm run build -w apps/web` exits 0; KnobRow emits decimal strings (grep finds no Money import and no bare-number arithmetic in rail/*); eslint exits 0; the rail reads tunables from the working-set store.</acceptance_criteria>
  <done>Rail of knobs rendering the working set, emitting decimal strings.</done>
</task>

<task type="auto">
  <name>Task 2: live debounced recompute wiring + mount rail in layout</name>
  <read_first>
    - apps/web/src/store/recompute.ts (requestRecompute + latest-wins settle)
    - apps/web/src/app/actions/scenarios.ts (recompareAction) + apps/web/src/app/actions/trajectory.ts (fiTrajectoryAction)
    - apps/web/src/app/layout.tsx (the shared layout from 07-05 — children slot)
    - .planning/phases/07-web-shell/07-UI-SPEC.md (D-08 live recompute, no Apply button; rail echoed on every route)
  </read_first>
  <action>Wire the rail's `onChange` → `working-set.updateKnob(...)` → `recompute.requestRecompute(() => recompareAction(currentWorkingSet + scenarios))` so a knob edit re-flies the instruments after the ~300ms debounce with no Apply button (D-08). The recompute result lands in the shared comparison state via the latest-wins `settle` guard (from 07-06). Mount `<AssumptionsRail/>` in `apps/web/src/app/layout.tsx` so it is docked on every route (cockpit, heatmap, sensitivity all inherit it — D-10/D-02). Show a subtle "recomputing" affordance from the store `pending` flag (no blocking spinner — the core is synchronous and fast). On a recompute error, surface the UI-SPEC error copy ("Couldn't run the numbers...").</action>
  <verify>
    <automated>npm run build -w apps/web && npx eslint apps/web</automated>
  </verify>
  <acceptance_criteria>`npm run build -w apps/web` exits 0 with `AssumptionsRail` referenced in layout.tsx; a knob edit path calls `requestRecompute` (grep confirms the rail wires to `recompute` store, not a direct un-debounced action call); no "Apply" button exists in the rail; `npx eslint apps/web` exits 0.</acceptance_criteria>
  <done>Editing a knob re-flies the instruments live via the debounced latest-wins loop; rail present on every route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rail knob input → Server Action | Numeric input becomes a decimal string crossing to a public POST action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | knob values reaching the engine | mitigate | Validation happens at the recompareAction boundary via core Zod (D-16); the rail holds no validation and emits decimal strings only |
| T-7-09 | Tampering (correctness) | rapid knob edits racing | mitigate | recompute coordinator's latest-wins guard (07-06) discards stale results |
| T-7-04 | Tampering (correctness) | bare-number money in the rail | mitigate | KnobRow converts to canonical decimal strings at the edge; grep gate confirms no Money/float math in rail/* |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-07)
- `apps/web/src/components/rail/AssumptionsRail.tsx` — docked assumptions rail (D-10), wired to working-set + debounced recompute (D-08)
- `apps/web/src/components/rail/KnobRow.tsx` — single tunable knob emitting decimal strings
- Modified `apps/web/src/app/layout.tsx` — rail mounted on every route
</artifacts_produced>

<verification>
- `npm run build -w apps/web` clean
- Rail present on every route; knob edit → debounced recompute (no Apply button)
- `npx eslint apps/web` exits 0 (no money math in rail/*)
</verification>

<success_criteria>
The persistent assumptions rail makes every assumption visible and editable next to the results, and editing a knob re-flies the instruments live and race-safely — the flight-simulator loop is real.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-07-SUMMARY.md` when done.
</output>
