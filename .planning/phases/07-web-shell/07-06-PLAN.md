---
phase: 07-web-shell
plan: 06
type: execute
wave: 3
depends_on: ["07-01"]
files_modified:
  - apps/web/src/store/working-set.ts
  - apps/web/src/store/selection.ts
  - apps/web/src/store/recompute.ts
  - apps/web/src/store/recompute.test.ts
autonomous: true
requirements: [SC-1, SC-2]

must_haves:
  truths:
    - "A shared working assumption set drives the whole comparison (apples-to-apples) and is held in Zustand keyed to the active scenario, never silently written back to a saved snapshot on navigation (PROF-04)"
    - "Selection/expansion state (which scenario row is expanded, which scenarios are compared) lives in ephemeral client state"
    - "A latest-wins guard (monotonic request id) prevents an out-of-order debounced recompute from overwriting newer results"
  artifacts:
    - path: "apps/web/src/store/working-set.ts"
      provides: "Zustand store for the shared working AssumptionSet (load frozen set on scenario open; never auto-persist)"
      contains: "create"
    - path: "apps/web/src/store/recompute.ts"
      provides: "debounce buffer + monotonic request-id latest-wins guard"
      contains: "requestId"
    - path: "apps/web/src/store/selection.ts"
      provides: "active profile/scenario + expanded-row selection state"
      contains: "create"
  key_links:
    - from: "apps/web/src/store/recompute.ts"
      to: "apps/web/src/store/working-set.ts"
      via: "debounced recompute reads the working set"
      pattern: "working"
---

<objective>
Build the ephemeral client-state layer in Zustand: the shared working assumption set (D-09 — one set drives the whole comparison; loaded from a saved scenario's frozen snapshot on open; NEVER auto-persisted back on navigation), the selection/expansion state (active profile/scenario, which row is expanded — D-03), and the debounced-recompute coordinator with a latest-wins guard (D-08 + Pitfall 6).

Purpose: SC-1/SC-2 — the live flight-simulator loop (edit a knob → re-fly the instruments) and the apples-to-apples ranked comparison both depend on a single shared working set and a race-safe recompute coordinator. Server-truth lives in SQLite; these stores hold only transient UI state. The PROF-04 guarantee requires that the working set is written to a snapshot ONLY on explicit Save (freeze), never on navigation.
Output: `working-set.ts`, `selection.ts`, `recompute.ts` stores + the latest-wins test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-shell/07-RESEARCH.md
@.planning/phases/07-web-shell/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: working-set + selection stores</name>
  <read_first>
    - .planning/phases/07-web-shell/07-RESEARCH.md (Pattern 5 live debounced recompute; Open Q3 working-set keyed to active scenario, persist only on Save-freeze; Anti-pattern "re-reading a mutable assumption set at view time breaks snapshot reproducibility")
    - .planning/phases/07-web-shell/07-CONTEXT.md (D-09 shared working set + freeze-on-save; D-10 rail; D-03 expand-row)
    - packages/core/src/assumptions/schema.js (the AssumptionSet shape the working set holds — held as the plain DTO form, not a Money/class instance)
  </read_first>
  <action>Create `apps/web/src/store/working-set.ts` — a Zustand store holding the shared working AssumptionSet (in its plain decimal-string DTO form, never Money instances). Actions: `loadFrozenSet(scenarioSnapshotAssumptions)` (called when a saved scenario is opened — D-09; replaces the working set), `updateKnob(path, decimalString)` (edits one tunable), and a read selector. Document that this store is NEVER auto-persisted to a snapshot on navigation (PROF-04 — persistence happens only via the explicit save action in 07-03). Create `apps/web/src/store/selection.ts` — active `profileId`, active `scenarioId`, and `expandedScenarioId` (D-03 inline expand) plus the set of scenarioIds in the comparison; setters for the header switchers + row expansion (07-11's profile editor sets the active `profileId` after a save).</action>
  <verify>
    <automated>npx eslint apps/web/src/store && npx tsc -p apps/web/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>Both stores typecheck (`npx tsc -p apps/web/tsconfig.json --noEmit` exits 0) and `npx eslint apps/web/src/store` exits 0; `working-set.ts` exposes `loadFrozenSet` + `updateKnob` and holds decimal strings (no Money import); neither store writes to a Server Action / SQLite on a navigation or knob edit (grep finds no `computeAndSaveScenario`/`saveProfile` call inside store/*). (The full `next build` runs at the wave/phase gate in 07-10.)</acceptance_criteria>
  <done>Working-set + selection ephemeral stores in place; no auto-persist on navigation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recompute coordinator + latest-wins guard</name>
  <read_first>
    - .planning/phases/07-web-shell/07-RESEARCH.md (Pattern 5 debounce; Pitfall 6 out-of-order debounced recompute — monotonic request id / latest-wins)
  </read_first>
  <behavior>
    - Test 1 (latest-wins): given two in-flight recomputes where the earlier (id N) resolves AFTER the later (id N+1), the store keeps the N+1 result and DISCARDS the stale N result.
    - Test 2 (debounce): rapid `requestRecompute()` calls within the debounce window coalesce to a single pending request id.
  </behavior>
  <action>Create `apps/web/src/store/recompute.ts` — a Zustand store with a `requestId` counter, a `pending` flag, a `~300ms` debounce buffer (D-08), and a `settle(resultId, result)` action that applies a result ONLY if `resultId === currentRequestId` (latest-wins — Pitfall 6); a stale resolution is dropped. Expose `requestRecompute(fn)` that bumps `requestId`, debounces, and on resolution calls `settle`. Keep the actual Server Action call injectable (pass `recompareAction`/`trajectoryAction` from the caller) so the coordinator is engine-agnostic and unit-testable without a server. Create `apps/web/src/store/recompute.test.ts` covering both behaviors with fake timers + manually-ordered promise resolution.</action>
  <verify>
    <automated>npx vitest run apps/web/src/store/recompute.test.ts</automated>
  </verify>
  <acceptance_criteria>Test exits 0; the stale-resolution test proves the N result is discarded when N+1 is newer; the debounce test proves coalescing to a single request id.</acceptance_criteria>
  <done>Race-safe debounced recompute coordinator proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ephemeral store → saved snapshot | The working set must not silently overwrite a reproducible snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-08 | Tampering | working-set auto-persist breaking PROF-04 | mitigate | working-set store performs NO persistence; freeze happens only via the explicit save Server Action (07-03); grep gate confirms no save call in store/* |
| T-7-09 | Tampering (correctness) | out-of-order recompute results | mitigate | monotonic request-id latest-wins guard; stale resolutions discarded (Pitfall 6) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-06)
- `apps/web/src/store/working-set.ts` — shared working AssumptionSet store (`loadFrozenSet`, `updateKnob`)
- `apps/web/src/store/selection.ts` — active profile/scenario + expanded-row + comparison-set state
- `apps/web/src/store/recompute.ts` — debounce buffer + monotonic latest-wins coordinator
- Test: `store/recompute.test.ts`
</artifacts_produced>

<verification>
- `npx vitest run apps/web/src/store/recompute.test.ts` green (latest-wins + debounce)
- `npx tsc -p apps/web/tsconfig.json --noEmit` + `npx eslint apps/web/src/store` clean
- No persistence side-effects in store/* (grep gate)
</verification>

<success_criteria>
The ephemeral client-state layer holds one shared working set (never auto-persisted), tracks selection/expansion, and coordinates debounced recompute race-safely — the substrate for the live cockpit loop.
</success_criteria>

<output>
Create `.planning/phases/07-web-shell/07-06-SUMMARY.md` when done.
</output>
